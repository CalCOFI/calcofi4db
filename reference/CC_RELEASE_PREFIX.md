# Release layout prefixes (bucket-relative)

Release layout prefixes (bucket-relative)

## Usage

``` r
CC_RELEASE_PREFIX

CC_TABLES_PREFIX
```

## Format

An object of class `character` of length 1.

An object of class `character` of length 1.
