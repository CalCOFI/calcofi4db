# Namespaced `sample_key` expression: `dataset_key:sample_type:id`

Namespaced `sample_key` expression: `dataset_key:sample_type:id`

## Usage

``` r
ns_key(dataset_key, sample_type, id_sql)
```

## Arguments

- dataset_key, sample_type, id_sql:

  components

## Value

a SQL expression string
