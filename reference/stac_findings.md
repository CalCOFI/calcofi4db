# The findings [`check_stac()`](https://calcofi.io/calcofi4db/reference/check_stac.md) can report

The findings
[`check_stac()`](https://calcofi.io/calcofi4db/reference/check_stac.md)
can report

## Usage

``` r
stac_findings()
```

## Value

A named character vector, finding -\> level (`error` \| `warn`).
