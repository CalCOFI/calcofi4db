# Direction of a CTD cast `sample_key`

Direction of a CTD cast `sample_key`

## Usage

``` r
qc_cast_direction(sample_key)
```

## Arguments

- sample_key:

  character vector of cast keys

## Value

`"down"`, `"up"`, or `NA` for a key with no direction suffix
