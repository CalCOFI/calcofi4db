# The empty registry shape, used by [`register_variables()`](https://calcofi.io/calcofi4db/reference/register_variables.md) to bootstrap a `metadata/variable.csv` that does not exist yet.

The empty registry shape, used by
[`register_variables()`](https://calcofi.io/calcofi4db/reference/register_variables.md)
to bootstrap a `metadata/variable.csv` that does not exist yet.

## Usage

``` r
.variable_registry_cols()
```
