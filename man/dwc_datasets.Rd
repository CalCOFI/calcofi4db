% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_datasets}
\alias{dwc_datasets}
\title{The biological datasets a Darwin Core Archive can be built for}
\usage{
dwc_datasets(con)
}
\arguments{
\item{con}{a DBI connection to the release (\code{calcofi4r::cc_get_db()})}
}
\value{
A data frame, one row per candidate dataset: \code{dataset_key}, \code{n_obs},
\code{n_taxa}, \code{n_worms}, \code{n_no_worms}, \code{n_no_taxon}, \code{absence_rule}.
}
\description{
Decision 21 (Ben, 2026-09-05): \strong{one IPT resource per dataset whose taxa resolve
to WoRMS}. This measures that from the release rather than listing it: a dataset
is a candidate when it has rows in \code{obs_bio} and at least one of the taxa it
observed carries a WoRMS id. \code{cce-lter_picoplankton-bacteria} (flow-cytometry
groups, environmental realm) and \code{sio_pic-zooplankton} (no taxa) have no \code{obs_bio}
rows at all, so they fall out by construction rather than by a hard-coded exclusion.
}
\details{
\code{n_no_worms} is the count of observed taxa with no WoRMS id: those occurrences ship
with \code{scientificNameID} empty (never a guessed LSID), and \code{\link[=dwc_check]{dwc_check()}} reports it.
}
\seealso{
\code{\link[=dwc_absence_rule]{dwc_absence_rule()}}, \code{\link[=dwc_event]{dwc_event()}}, \code{\link[=dwc_occurrence]{dwc_occurrence()}}
}
\concept{dwc}
