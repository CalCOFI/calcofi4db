% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shards.R
\name{assemble_core}
\alias{assemble_core}
\title{Assemble the whole consolidated core from the ingest shards}
\usage{
assemble_core(
  con,
  root = ".",
  supplemental = TRUE,
  parquet_dir = cc_stage_path("parquet"),
  exclude = release_excluded_datasets(root)
)
}
\arguments{
\item{con}{a DuckDB connection}

\item{root}{workflows repo root}

\item{supplemental}{\code{TRUE} (default) to include every supplemental
full-resolution table the ingests declare, \code{FALSE} for none, or an explicit
character vector of table names. See \code{\link[=supplemental_core_tables]{supplemental_core_tables()}}.}

\item{parquet_dir}{directory holding the per-dataset output dirs. Defaults
to the local staging root (see \code{\link[=cc_stage_dir]{cc_stage_dir()}}), where the bulk parquet
lives; an absolute path is used as-is, a relative one is resolved against
\code{root}. The JSON sidecars stay in the repo and are found separately.}

\item{exclude}{dataset dir names to skip; defaults to the ingests declaring
\code{calcofi.in_release: false} (see \code{\link[=release_excluded_datasets]{release_excluded_datasets()}}). Resolved
once here and threaded to every shard read.}
}
\value{
(invisibly) a named list of row counts
}
\description{
Convenience wrapper: \code{sample}, \code{obs}, \code{obs_attribute}, \code{sample_measurement}
(surrogate ids renumbered globally), the supplemental \code{obs_ctd_full}, and the
taxa references (\code{taxon} merged in dataset order; \code{dataset_taxon} /\code{taxon_group}
deduplicated). Errors if \code{sample_key} is not globally unique — the namespacing
guarantee the whole model rests on.
}
\concept{shards}
