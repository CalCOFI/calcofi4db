% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{obs_view_sql}
\alias{obs_view_sql}
\title{\code{obs} as a view over \code{obs_bio} + \code{obs_env}}
\usage{
obs_view_sql(bio = "{{obs_bio}}", env = "{{obs_env}}")
}
\arguments{
\item{bio, env}{what to put after \code{FROM} for each realm: a token, a quoted table name, or a
\code{read_parquet(...)} expression.}
}
\value{
A length-one SQL string (no trailing semicolon; wrap in parentheses to use it in a
\code{FROM}).
}
\description{
The UNION ALL that reconstructs \code{obs} — its 18 columns, in \link{OBS_VIEW_COLUMNS} order, under their
original names — from the bifurcated pair (pre-release plan D-S1): \code{realm} is the constant each
branch contributes, \code{value} becomes \code{measurement_value}. The default sources are the \strong{tokens}
\code{{{obs_bio}}} / \code{{{obs_env}}}, which is how the SQL is stored in a release's \code{catalog.json}
(\code{views.obs}): every resolver — \code{calcofi4r::cc_get_db()}, \code{calcofi4py.cc_get_db()}, db-query's
\verb{__TBL:obs__} — substitutes its own way of reading each table (\code{\link[=substitute_view_tables]{substitute_view_tables()}}), a
quoted table name inside a connection or a \code{read_parquet(...)} over the catalog's objects.
}
\examples{
cat(obs_view_sql())
cat(obs_view_sql('"obs_bio"', '"obs_env"'))
}
\concept{release}
