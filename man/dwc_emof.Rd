% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_emof}
\alias{dwc_emof}
\title{Build the ExtendedMeasurementOrFact extension for one dataset}
\usage{
dwc_emof(
  con,
  dataset_key,
  occurrence = NULL,
  measurement_type = NULL,
  env = TRUE
)
}
\arguments{
\item{con}{a DBI connection to the release}

\item{dataset_key}{the dataset}

\item{occurrence}{the frame from \code{\link[=dwc_occurrence]{dwc_occurrence()}} — needed to resolve
\code{occurrenceID}; without it the \code{obs_attribute} grain is skipped}

\item{measurement_type}{the registry from \code{\link[=read_measurement_type]{read_measurement_type()}}, or NULL}

\item{env}{include \code{obs_env} rows on the dataset's own events}
}
\value{
A data frame of eMoF rows, all-NA columns dropped.
}
\description{
Three grains, all with \code{measurementTypeID} / \code{measurementUnitID} from
\code{measurement_type.csv}'s \code{nerc_p01} / \code{units_nerc_p06} — \strong{empty where the
registry states no exact concept, never invented}:
}
\details{
\itemize{
\item \strong{event} — \code{sample_measurement} rows for the dataset, minus \code{volume_sampled}
(which is the event's \code{sampleSizeValue}, not a repeat measurement).
\item \strong{occurrence} — \code{obs_attribute} rows, joined to their occurrence on
\verb{(sample_key, taxon_key, life_stage)}. Which of a bin's two numbers is the
\code{measurementValue} is decided by the REGISTRY, not per dataset: a type with a
physical \code{units} (\code{body_length} mm, \code{carapace_length} mm) puts \code{bin_value} in
\code{measurementValue} and the bin's \code{count} in \code{measurementRemarks}; a type with no
unit (\code{stage}, \code{behavior}) is a categorical bin, so the \code{count} is the value and
the \code{bin_label} is the remark.
\item \strong{event, environmental} — \code{obs_env} rows sitting on one of this dataset's own
events (\code{sample_key}). Empty for a dataset that measures no environment itself.
}
}
\seealso{
\code{\link[=dwc_event]{dwc_event()}}, \code{\link[=dwc_occurrence]{dwc_occurrence()}}
}
\concept{dwc}
