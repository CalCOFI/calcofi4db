% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{match_by_site_datetime}
\alias{match_by_site_datetime}
\title{Match Records to a Reference Table by Key + Datetime Window}
\usage{
match_by_site_datetime(
  con,
  data_tbl,
  ref_tbl,
  fk_col = "cast_id",
  ref_pk = "cast_id",
  key_col = "site_key",
  datetime_col = "datetime_start_utc",
  ref_key_col = NULL,
  ref_datetime_col = NULL,
  window_days = 3,
  return_stats = TRUE
)
}
\arguments{
\item{con}{DBI connection to DuckDB.}

\item{data_tbl}{Character. Table to add the FK column to (e.g. "dic_sample").}

\item{ref_tbl}{Character. Reference table to match against (e.g. "casts").}

\item{fk_col}{Character. FK column to create on \code{data_tbl} (default "cast_id").}

\item{ref_pk}{Character. Primary-key column on \code{ref_tbl} (default "cast_id").}

\item{key_col}{Character. Shared key matched exactly on both tables
(default "site_key").}

\item{datetime_col}{Character. Timestamp column on \code{data_tbl}, compared by
date (default "datetime_start_utc").}

\item{ref_key_col}{Character. Key column on \code{ref_tbl} (default: same as
\code{key_col}). Set when the reference table names the key differently.}

\item{ref_datetime_col}{Character. Timestamp column on \code{ref_tbl} (default:
same as \code{datetime_col}). Set when the two tables name their timestamp
differently — e.g. matching a dataset on \code{datetime_start_utc} to a not-yet-
normalized reference still on \code{datetime_utc}.}

\item{window_days}{Numeric. Maximum absolute date difference, in days
(default 3).}

\item{return_stats}{Logical. If TRUE (default), return a stats list;
otherwise return invisible NULL.}
}
\value{
If \code{return_stats}, a list with \code{matched}, \code{total}, \code{pct}, and an
\code{unmatched} data frame of distinct unmatched key/date rows. Side effect:
adds and populates \code{fk_col} on \code{data_tbl}.
}
\description{
Adds a foreign-key column to \code{data_tbl} and populates it with the primary
key of the nearest-in-time row of \code{ref_tbl} that shares the same key column
(e.g. \code{site_key}) and falls within \code{window_days} of the data row's datetime.
This is the standard cross-dataset bridge for datasets that lack an explicit
cast/cruise FK (issue #47). Extracted from \code{ingest_calcofi_dic.qmd} so every
ingest reuses the same logic rather than re-writing inline SQL.
}
\examples{
\dontrun{
s <- match_by_site_datetime(con, "dic_sample", "casts")
cat(glue::glue("matched {s$matched}/{s$total} ({s$pct}\%)"), "\n")
}
}
\concept{spatial}
