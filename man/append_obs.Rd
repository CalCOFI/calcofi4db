% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{append_obs}
\alias{append_obs}
\title{Append occurrence-headline rows into the core \code{obs} table}
\usage{
append_obs(con, select_sql, obs_tbl = "obs", res_max = CC_H3_RES_MAX)
}
\arguments{
\item{con}{a DuckDB connection (open via \code{\link[=get_duckdb_con]{get_duckdb_con()}})}

\item{select_sql}{a SELECT producing the canonical \code{obs} columns by name}

\item{obs_tbl}{target table name (\code{"obs"}, or \code{"obs_ctd_full"} for full CTD)}

\item{res_max}{finest H3 resolution stored in \code{hex_id}}
}
\value{
(invisibly) the total row count of \code{obs_tbl} after the append
}
\description{
Wraps a caller-supplied projection \code{select_sql} (which must yield the canonical
\code{obs} columns \emph{by name} — \code{realm}, \code{dataset_key}, \code{sample_key}, \code{grid_key},
\code{cruise_key}, \code{latitude}, \code{longitude}, \code{datetime}, \code{depth_min_m}, \code{depth_max_m},
\code{taxon_id}, \code{life_stage}, \code{measurement_type}, \code{measurement_value},
\code{measurement_qual}, \code{measurement_prec}), mints a surrogate \code{obs_id} (offset from
the current max so repeated calls stay unique within one connection) and computes
\code{hex_id} at H3 resolution \code{res_max}. The same helper serves the central Phase-2
materialization (\code{release_database.qmd}) and each per-dataset ingest (Phase 3);
release assembly renumbers \code{obs_id} globally across the reassembled shards.
}
\concept{model}
