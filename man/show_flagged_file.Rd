% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display.R
\name{show_flagged_file}
\alias{show_flagged_file}
\title{Show Flagged File Result}
\usage{
show_flagged_file(
  invalid_rows,
  output_path,
  description,
  repo = "CalCOFI/workflows",
  branch = "main"
)
}
\arguments{
\item{invalid_rows}{Tibble of invalid rows that were flagged}

\item{output_path}{Path to the flagged CSV file}

\item{description}{Description of what was flagged}

\item{repo}{GitHub repository (default: "CalCOFI/workflows")}

\item{branch}{Git branch (default: "main")}
}
\value{
HTML string with flagged file summary and link
}
\description{
Displays the result of \code{flag_invalid_rows()} with a GitHub link to the
flagged file and summary statistics.
}
\examples{
\dontrun{
invalid_stages <- validate_egg_stages(con, "egg_stage", "stage")
if (nrow(invalid_stages) > 0) {
  output_path <- flag_invalid_rows(invalid_stages, "data/flagged/invalid_egg_stages.csv", "Invalid egg stages")
  show_flagged_file(invalid_stages, output_path, "Invalid egg stages")
}
}
}
\concept{display}
