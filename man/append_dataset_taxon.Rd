% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{append_dataset_taxon}
\alias{append_dataset_taxon}
\title{Stage a dataset's taxon vocabulary in \code{dataset_taxon} (taxon plan D1)}
\usage{
append_dataset_taxon(con, dataset_key, df, ds_prefix = dataset_key)
}
\arguments{
\item{con}{a DuckDB connection}

\item{dataset_key}{\code{provider_dataset} of the observing dataset (what \code{obs}
joins on)}

\item{df}{the vocabulary, one row per local taxon (columns above)}

\item{ds_prefix}{prefix of \code{ds_taxon_key} (\code{"<ds_prefix>:<ds_taxa_code>"});
defaults to \code{dataset_key}. \code{swfsc_ichthyo} uses \code{"calcofi"}, the shared
CalCOFI species list.}
}
\value{
(invisibly) the number of rows staged
}
\description{
The ingest declares its vocabulary; the package resolves it. This writes one
row per local taxon into \code{dataset_taxon} with \code{taxon_key} \strong{empty} — filled
later, in place, by \code{\link[=resolve_dataset_taxon]{resolve_dataset_taxon()}} from the authorities — and
replaces any rows the table already holds for \code{dataset_key}.
}
\details{
The column contract is explicit, and a deviation is a hard stop at ingest
rather than an \code{NA} at release (which is how dropping \code{itis_id} from a
species table would have un-keyed every seabird without an error anywhere):\tabular{lll}{
   column \tab required \tab meaning \cr
   \code{ds_taxa_code} \tab yes; unique; non-NA \tab the code \code{obs} stores — verbatim, never cleaned \cr
   \code{ds_scientific_name} \tab yes (NA allowed for an operational class) \tab the source's name; the lookup query after \code{\link[=clean_taxon_name]{clean_taxon_name()}} \cr
   \code{ds_common_name} \tab no \tab  \cr
   \code{worms_id}, \code{itis_id}, \code{gbif_id}, \code{rank} \tab no; ids integer \tab what \strong{the source supplied} — hints to resolution, stored together as \code{ds_source_json} \cr
}


Errors on a missing required column, an unknown column, a duplicate or NA
code, an id that does not coerce to an integer, or an empty frame.

\code{ds_source_json} is one JSON object of whatever ids / rank the source
supplied (e.g. \code{{"itis_id":174715}}), \code{NULL} when it supplied nothing. It sits
beside \code{taxon.worms_id} / \code{itis_id} so "what did the source claim?" can be
audited against "what does the authority say?" with
\code{json_extract(ds_source_json, '$.itis_id')}. The notebook never writes JSON by
hand.
}
\examples{
\dontrun{
append_dataset_taxon(con, "farallon_bird-mammal", d_species |>
  transmute(ds_taxa_code = species, ds_scientific_name = scientific_name,
            ds_common_name = common_name, itis_id))
ensure_taxon_xref(con, mt_taxon, tx_over, cache_csv = here("metadata/taxon_xref.csv"))
ensure_taxon_lineage(con, mt_taxon, tx_over, cache_csv = here("metadata/taxon_lineage.csv"))
resolve_dataset_taxon(con, mt_taxon, tx_over)
}
}
\seealso{
\code{\link[=resolve_dataset_taxon]{resolve_dataset_taxon()}}, \code{\link[=check_dataset_taxon]{check_dataset_taxon()}}
}
\concept{taxonomy}
