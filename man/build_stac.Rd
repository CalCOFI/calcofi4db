% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{build_stac}
\alias{build_stac}
\title{Build the static STAC catalog of a release}
\usage{
build_stac(
  record,
  catalog = NULL,
  spatial_layers = NULL,
  dir,
  base_url = CC_STAC_HTTPS,
  metadata = NULL,
  include_layers = TRUE
)
}
\arguments{
\item{record}{\code{datasets.json}: a path/URL or the parsed list}

\item{catalog}{the release \code{catalog.json} (path/URL or list), or NULL — used
only for the release version/date when \code{record$release} lacks them}

\item{spatial_layers}{\code{spatial_layers.json} (path/URL or list), or NULL}

\item{dir}{the directory to write into (created)}

\item{base_url}{the HTTPS root the catalog will be served from; the \code{self},
\code{root}, \code{parent}, \code{child} and \code{item} links are absolute against it. Pass the
staging root for a staging run.}

\item{metadata}{\code{metadata.json} (path/URL or list), or NULL — the source of
the column and table descriptions}

\item{include_layers}{write the spatial-layer collections (default TRUE)}
}
\value{
The paths written, invisibly, \code{catalog.json} first.
}
\description{
Writes STAC 1.0.0 into \code{dir}: a root \code{catalog.json}, one
\code{collections/{dataset_key}/collection.json} per \strong{public} dataset with its
extent, licence, providers, keywords, \code{table:tables} and \code{sci:doi} /
\code{sci:citation}, one Item per release at
\verb{collections/\{dataset_key\}/items/\{version\}.json} whose assets are the
dataset's parquet objects (\code{application/x-parquet}, \verb{roles: [data]}, with
\code{table:columns} from \code{metadata.json} and \code{file:size} / \code{file:checksum}), its
CF netCDF, the ERDDAP pages (\verb{roles: [overview]}) and the ISO 19115 record
(\verb{roles: [metadata]}); and one \verb{collections/layer_\{key\}/collection.json} per
spatial layer with its PMTiles asset. Everything is read from the record —
no service is asked anything here.
}
\concept{catalog}
