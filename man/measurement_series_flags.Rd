% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_measurements.R
\name{measurement_series_flags}
\alias{measurement_series_flags}
\title{The \code{flags[]} a \code{measurements.json} series can carry}
\usage{
measurement_series_flags()
}
\value{
A character vector, the \verb{series[].flags} enum of
\code{measurements.schema.json}.
}
\description{
A \code{series[]} entry is one \verb{measurement_type × dataset} of \code{obs_env} — what a
dataset calls a measurement. A flag says something about that series a reader
of the page needs in order to read its numbers correctly, so a measurement
page can show a quiet pill rather than a footnote nobody writes:
}
\details{
\itemize{
\item \code{sensor_mean} — the registry's \code{derivation} says the series is a mean of a
sensor pair (\code{temperature_ave}, "Mean of the two temperature sensors"). The
per-sensor series ride the full-resolution product.
\item \code{replicate} — a \verb{…_rep1} / \verb{…_rep2} replicate beside a mean of the same
quantity (\code{alkalinity_rep1}), never itself the mean.
\item \code{reported_pre_qc} — an \verb{r_*} series: the value the source reported before
quality control, kept beside the QC'd one.
\item \code{no_bound} — the registry declares neither \code{valid_min} nor \code{valid_max}, so
nothing in the pipeline can call one of its values impossible.
\item \code{sentinel_suspected} — the observed maximum exceeds the declared
\code{valid_max}, or (with no bound declared at all) reaches
\code{CC_MEASUREMENT_SENTINEL} (99), the shape a fill value takes. On
v2026.09.06 the bottle's \code{temperature} reads 99.00 degC — one corrupt 2020
cast, fixed at the ingest for the next release.
\item \code{no_flag_at_grain} — the registry names no \verb{_qual_column}, so the series
reaches the release carrying no quality code. The CTD's \code{temperature_ave}
is the headline case: the sensor flags ride \code{temperature_1} / \code{temperature_2}
in \code{obs_ctd_full}.
\item \code{no_p01} — no \code{nerc_p01} concept. Under the registry's exact-match rule
that means "no concept says exactly this", never "not looked at".
}
}
\examples{
measurement_series_flags()
}
\concept{release}
