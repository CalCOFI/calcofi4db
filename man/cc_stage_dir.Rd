% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage.R
\name{cc_stage_dir}
\alias{cc_stage_dir}
\title{Local Staging Root for Bulk Data}
\usage{
cc_stage_dir()
}
\value{
Absolute path, with \code{~} expanded. Not created.
}
\description{
Directory where bulk parquet is written on its way to GCS. Read from the
\code{CALCOFI_STAGE_DIR} environment variable, falling back to \verb{~/_big/calcofi}.
}
\details{
Set this in \verb{~/.Renviron} to a path that is \strong{neither a git working tree nor
a synced cloud folder} — a 24 GB tree inside either one is a problem for
that tool, not a feature:

\if{html}{\out{<div class="sourceCode">}}\preformatted{CALCOFI_STAGE_DIR=/Users/bbest/_big/calcofi
}\if{html}{\out{</div>}}

The fallback keeps a fresh clone working without setup. It is deliberately
outside the repo, so the failure mode of forgetting to set the variable is
"writes to an unexpected but harmless place", never "writes 24 GB back into
git".
}
\examples{
cc_stage_dir()
}
\concept{cloud}
