% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{check_seafloor_nulls}
\alias{check_seafloor_nulls}
\title{Classify the samples whose \code{seafloor_depth_m} is NULL, by cause}
\usage{
check_seafloor_nulls(
  con,
  source_bbox = c(-180, 0, -90, 90),
  sample_tbl = "sample",
  lon_col = "longitude",
  lat_col = "latitude"
)
}
\arguments{
\item{con}{DBI connection holding \code{sample_tbl} after \code{\link[=add_sample_seafloor]{add_sample_seafloor()}}.}

\item{source_bbox}{\code{c(w, s, e, n)} of the GEBCO source that was sampled —
the full sub-ice tile \code{c(-180, 0, -90, 90)} by default.}

\item{sample_tbl, lon_col, lat_col}{Table and columns to read.}
}
\value{
data.frame \code{cause}, \code{n} (plus a \code{datasets} summary column for the
inside-tile rows); attribute \code{n_inside_null} carries the gate value.
}
\description{
After \code{\link[=add_sample_seafloor]{add_sample_seafloor()}}, a \code{NULL} seafloor can mean four different
things, and only one of them is acceptable to ship silently. This returns one
row per cause with its count — \code{no_coordinates} (lon or lat NULL),
\code{nan_coordinate}, \code{outside_source_tile} (the position genuinely falls off the
GEBCO tile that was sampled) — and \code{inside_tile_null}, a position \strong{inside}
the tile that still reads NULL, which can only be a regression in the
sampling itself. Gate on that one:
\code{stopifnot(attr(x, "n_inside_null") == 0)} (D29, 2026-08-31).
}
\concept{validation}
