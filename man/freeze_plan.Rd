% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{freeze_plan}
\alias{freeze_plan}
\title{Decide, per object, whether to upload or reuse}
\usage{
freeze_plan(
  objects,
  prev_catalog,
  version,
  layout = c("compat", "canonical"),
  release_prefix = CC_RELEASE_PREFIX
)
}
\arguments{
\item{objects}{tibble from \code{\link[=release_objects]{release_objects()}} (all tables, row-bound).}

\item{prev_catalog}{previous release's catalog (list) or NULL.}

\item{version, release_prefix}{the release and its prefix.}

\item{layout}{\code{"compat"} (objects live under the release prefix; unchanged
ones are server-side copied from the previous release) or \code{"canonical"}
(objects live under \link{CC_TABLES_PREFIX}; unchanged ones already exist there).}
}
\value{
\code{objects} plus \code{path} (bucket-relative destination), \code{action}
(\code{upload} | \code{copy} | \code{exists}) and \code{source} (bucket-relative path copied from,
or NA). For a \code{copy}/\code{exists} object \code{bytes} and \code{sha256} describe the object
that will actually sit at \code{path} — the previous release's — and the local
export's values move to \code{bytes_local}/\code{sha256_local} (4.0.3): identity is the
row signature, and a row-identical re-export is not byte-identical for every
table (v2026.09.04 shipped 173 catalog entries whose size/sha256 described
the local file while the path held the copied object).
}
\description{
Decide, per object, whether to upload or reuse
}
\concept{release}
