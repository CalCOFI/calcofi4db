% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{release_sort_keys}
\alias{release_sort_keys}
\title{Sort keys (and partition column) for every released table}
\usage{
release_sort_keys(
  core_sort = c("grid_key NULLS LAST", "depth_min_m NULLS LAST", "measurement_type",
    "datetime")
)
}
\arguments{
\item{core_sort}{clustering sort for the long observation tables.}
}
\value{
Named list: \verb{table -> list(partition_by = <col or NULL>, order_by = <chr>)}.
}
\description{
The ORDER BY that makes an export deterministic must be a unique total order:
partition column first, then the clustering sort, then the primary key as the
tiebreak. A released table missing from this registry — and without a primary
key in \code{\link[=core_relationships]{core_relationships()}} — makes \code{\link[=export_release_parquet]{export_release_parquet()}} refuse to
write, rather than write non-deterministically.
}
\concept{release}
