% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_upload.R
\name{ctd_upload_to_core}
\alias{ctd_upload_to_core}
\title{Project an uploaded file into the core \code{obs} / \code{sample} shape}
\usage{
ctd_upload_to_core(
  d,
  mapping,
  header = list(),
  dataset_key = "upload",
  cruise_key = NULL,
  site_key = NULL,
  cast_id = NULL
)
}
\arguments{
\item{d}{the parsed upload}

\item{mapping}{output of \code{\link[=ctd_map_columns]{ctd_map_columns()}}}

\item{header}{the Sea-Bird header list, if any}

\item{dataset_key}{stamped on every row}

\item{cruise_key, site_key, cast_id}{override the header-derived values}
}
\value{
list with \code{sample}, \code{obs} (both data frames) and \code{n_sentinel}
}
\description{
Once this has run, every QC rule in \verb{metadata/qc_rules/} applies unchanged.
}
\details{
The two source-specific repairs the pipeline already knows about are applied
here too, because a new file is exactly where they arrive: the \code{-99} sentinel
(and \code{-9.99e-29}) are deleted rather than carried as readings, and quality
codes stored as \code{"9.0"} by a double-to-string cast are stripped textually — not
via an integer cast, which would round an unexpected \code{"9.5"}.
}
\concept{upload}
