% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{replace_uuid_with_id}
\alias{replace_uuid_with_id}
\title{Replace UUIDs with Integer Foreign Keys}
\usage{
replace_uuid_with_id(
  con,
  table_name,
  uuid_col,
  new_id_col,
  ref_table,
  ref_uuid_col,
  ref_id_col,
  drop_uuid = TRUE
)
}
\arguments{
\item{con}{DuckDB connection}

\item{table_name}{Table to update}

\item{uuid_col}{Name of UUID column to replace}

\item{new_id_col}{Name of new integer ID column}

\item{ref_table}{Reference table containing UUID-to-ID mapping}

\item{ref_uuid_col}{UUID column in reference table}

\item{ref_id_col}{ID column in reference table}

\item{drop_uuid}{Whether to drop the UUID column after replacement (default: TRUE)}
}
\value{
Invisibly returns the connection
}
\description{
Creates an integer ID column and populates it based on UUID lookups,
then optionally drops the UUID column.
}
\examples{
\dontrun{
# replace site_uuid with site_id FK
replace_uuid_with_id(
  con         = con,
  table_name  = "tow",
  uuid_col    = "site_uuid",
  new_id_col  = "site_id",
  ref_table   = "site",
  ref_uuid_col = "site_uuid",
  ref_id_col  = "site_id")
}
}
\concept{wrangle}
