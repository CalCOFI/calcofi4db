% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{zenodo_metadata}
\alias{zenodo_metadata}
\alias{citation_cff}
\alias{write_citation_files}
\title{Metadata for \code{.zenodo.json} and \code{CITATION.cff} at the workflows repo root}
\usage{
zenodo_metadata(
  dataset_df,
  version = NULL,
  publication_date = NULL,
  curators = CC_RELEASE_CURATORS
)

citation_cff(version, date_released, doi = CC_ZENODO_CONCEPT_DOI)

write_citation_files(
  dir,
  dataset_df,
  version,
  date_released,
  zenodo_version = NULL,
  doi = CC_ZENODO_CONCEPT_DOI
)
}
\arguments{
\item{dataset_df}{the dataset table from \code{\link[=ingest_yaml_to_dataset_df]{ingest_yaml_to_dataset_df()}}
(only \code{pi_names} is read)}

\item{version}{the release tag, when the record is for one release}

\item{publication_date}{\code{"YYYY-MM-DD"}, optional}

\item{curators}{the DataCurator contributors}

\item{date_released}{\code{"YYYY-MM-DD"}}

\item{doi}{the DOI \code{CITATION.cff} carries (default: the concept DOI)}

\item{dir}{the workflows repo root}

\item{zenodo_version}{the \code{version} written into \code{.zenodo.json} (default
\code{NULL}: Zenodo takes it from the tag)}
}
\value{
A list, ready for \code{jsonlite::write_json(auto_unbox = TRUE)}.

\code{write_citation_files()}: the two paths, named.
}
\description{
Zenodo's GitHub integration fills a record from \code{.zenodo.json} at each
release tag; without it the alpha record came out as "CalCOFI/workflows:
initial Zenodo release", MIT, creators = the GitHub contributors (measured
2026-09-03). \code{zenodo_metadata()} makes the record a \strong{dataset}: title
"CalCOFI Integrated Database", creators = the three partners as
organisations, contributors = every dataset's PIs (\code{DataCollector}, from
\code{pi_names}) and the curators (\code{DataCurator}), license \code{cc-by-4.0} for the
record (the code stays MIT in \code{LICENSE}, said in the description), the GCS
release as \code{isSupplementTo} and db-schema as \code{isDocumentedBy}. \code{version} is
omitted unless given, because Zenodo takes it from the tag. \code{citation_cff()}
is the same record for GitHub's "Cite this repository", carrying the concept
DOI. \code{\link[=write_citation_files]{write_citation_files()}} writes both.
}
\concept{release}
