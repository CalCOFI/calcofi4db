% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/display.R
\name{preview_tables}
\alias{preview_tables}
\title{Preview Tables with Head and Tail Rows}
\usage{
preview_tables(con, tables, n = 100, table_header_level = 3)
}
\arguments{
\item{con}{DBI connection to DuckDB}

\item{tables}{Character vector of table names to preview.}

\item{n}{Integer. Number of rows for head and tail (default: 100).}

\item{table_header_level}{Integer. Markdown heading level for each
table (default: 3, i.e. \verb{###}).}
}
\value{
Invisible NULL. Side effect: prints markdown headers and
DT datatable widgets for Quarto/knitr output.
}
\description{
For each table, emits a markdown header and shows the first and last
\code{n} rows as DT datatables. Small tables (≤ 2n rows) are shown in
full. Geometry columns are excluded from display since they render
as unreadable binary.
}
\details{
Use in a chunk with \verb{#| results: asis} so the markdown headers
render correctly.
}
\examples{
\dontrun{
# in a chunk with `#| results: asis`
preview_tables(con, c("casts", "bottle", "grid"))
}
}
\concept{display}
