% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{parse_edi_cite}
\alias{parse_edi_cite}
\alias{parse_erddap_das}
\alias{parse_ncei_landing}
\alias{parse_datacite}
\alias{parse_doi_bibliography}
\title{Parse a resolver's response into the fields the citation cache carries}
\usage{
parse_edi_cite(x)

parse_erddap_das(x)

parse_ncei_landing(x)

parse_datacite(x)

parse_doi_bibliography(x)
}
\arguments{
\item{x}{the response body, one string}
}
\value{
A list (or, for \code{parse_doi_bibliography()}, one string).
}
\description{
Each parser takes the raw text of one response and returns a list; they run
on saved responses in the tests, so no network is needed to pin them.
\itemize{
\item \code{parse_edi_cite()} — EDI's cite service
(\verb{https://cite.edirepository.org/cite/<scope>.<id>.<rev>?style=ESIP}):
the citation verbatim, plus the DOI and year it contains.
\item \code{parse_erddap_das()} — an ERDDAP \code{.das}: the \code{NC_GLOBAL} string attributes
as a named list (\code{title}, \code{institution}, \code{creator_name}, \code{license},
\code{citation} when a dataset declares one, …), multi-line values joined.
\item \code{parse_ncei_landing()} — an NCEI landing page: its "Cite as:" block with
the \verb{[indicate subset used]} / \code{Accessed [date]} placeholders removed, plus
the DOI.
\item \code{parse_datacite()} — \verb{https://api.datacite.org/dois/<doi>}: DOI, title,
creators, publisher, year, URL and the SPDX \code{license} from \code{rightsList}
(upper-cased to the registry's form, \code{CC-BY-4.0}).
\item \code{parse_doi_bibliography()} — doi.org content negotiation
(\verb{Accept: text/x-bibliography; style=apa}): the formatted citation with
markup and entities stripped.
}
}
\concept{registry}
