% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_taxa.R
\name{check_taxa_catalog}
\alias{check_taxa_catalog}
\title{Check a \code{taxa.json} against the release it was built from}
\usage{
check_taxa_catalog(record, con = NULL, dataset_record = NULL)
}
\arguments{
\item{record}{from \code{\link[=build_taxa_catalog]{build_taxa_catalog()}} (or a \code{taxa.json} path or URL)}

\item{con}{a DBI connection holding \code{taxon} and \code{obs_bio}; NULL to check only
what the record can prove about itself}

\item{dataset_record}{the \code{datasets.json} record (or its path) whose
\code{datasets[]} every \code{dataset_key} must appear in; NULL to skip that check}
}
\value{
A \link[tibble:tibble]{tibble}: \code{check}, \code{level}, \code{ok}, \code{expected},
\code{observed}, \code{detail}.
}
\description{
One row per check with an \code{ok} flag, so a release chunk can print the table
whether or not it passes. The counts are re-measured against \code{obs_bio} when
\code{con} is given (the release's own connection, or a promoted release read back);
without it the arithmetic that lives inside the record is still checked.
}
\seealso{
\code{\link[=assert_taxa_catalog]{assert_taxa_catalog()}}, \code{\link[=taxa_catalog_checks]{taxa_catalog_checks()}}
}
\concept{taxonomy}
