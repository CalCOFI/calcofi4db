% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{check_depth_bounds}
\alias{check_depth_bounds}
\title{Check depth coordinates against an absolute range}
\usage{
check_depth_bounds(
  con,
  tbls = c("sample", "obs"),
  depth_cols = c("depth_min_m", "depth_max_m"),
  max_depth_m = CC_DEPTH_MAX_M,
  min_depth_m = 0,
  by = "dataset_key"
)
}
\arguments{
\item{con}{DBI connection.}

\item{tbls}{Tables to check (each needs the depth columns it has of
\code{depth_cols}; a table lacking all of them is skipped with a message).}

\item{depth_cols}{Depth columns to check where present.}

\item{max_depth_m, min_depth_m}{The plausible range.}

\item{by}{Grouping column (default \code{dataset_key}; NULL for none).}
}
\value{
A tibble: \code{table}, \code{dataset_key}, \code{depth_col}, \code{n_total}, \code{n_nan},
\code{n_below}, \code{n_above}, \code{v_min}, \code{v_max}, \code{status} (\code{ok} | \code{out_of_range}).
}
\description{
One row per (table, dataset, depth column): how many depths are NaN, below
\code{min_depth_m} or above \code{max_depth_m}. A non-\code{ok} row is an error in the data
— assert \code{all(status == "ok")} at ingest and at release.
}
\concept{validation}
