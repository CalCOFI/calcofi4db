% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cruise.R
\name{resolve_cruise_key}
\alias{resolve_cruise_key}
\title{Resolve \code{cruise_key} on an event table by span, designation, then month}
\usage{
resolve_cruise_key(
  con,
  table_name,
  datetime_col,
  ship_key_col = "ship_key",
  cruise_ym_col = NULL,
  cruise_tbl = "cruise",
  ship_tbl = "ship",
  tolerance_days = 3L,
  require_in_cruise = FALSE,
  method_col = "cruise_key_method"
)
}
\arguments{
\item{con}{DBI connection to DuckDB holding \code{table_name}, \code{cruise_tbl} (with
\code{date_min}/\code{date_max} from \code{\link[=add_cruise_date_span]{add_cruise_date_span()}}) and \code{ship_tbl}.}

\item{table_name}{Event table to annotate.}

\item{datetime_col}{Timestamp/date column on the event table.}

\item{ship_key_col}{Column holding the matched \code{ship_key} (default
\code{"ship_key"}); rows with NULL get no key.}

\item{cruise_ym_col}{Optional column carrying the source's cruise designation
as YYYYMM (e.g. the bottle database's \code{Cruise}). Values not matching
\verb{^\\\\d\{4\}(0[1-9]|1[0-2])$} are ignored for that row.}

\item{cruise_tbl, ship_tbl}{Reference table names.}

\item{tolerance_days}{Days added to each end of a cruise's observed span
before testing containment (default 3 — a hydrocast can precede the first
plankton tow by a day or two).}

\item{require_in_cruise}{If TRUE, keys from steps 2–3 that do not exist in
\code{cruise_tbl} are left NULL (use for datasets that only join to known
cruises); step-1 keys always exist by construction.}

\item{method_col}{Name of the column recording which step resolved each row
(\code{"span"}, \code{"source"}, \code{"month"}, or NULL). Set to \code{NULL} to not record it.}
}
\value{
A tibble with one row per method: \code{method}, \code{n}, \code{n_in_cruise}.
}
\description{
Writes \code{cruise_key} (and \code{cruise_key_method}) onto \code{table_name} for every row
with a matched \code{ship_key}, trying in order:
\enumerate{
\item \strong{span} — the reference cruise of the same ship whose
\verb{date_min - tolerance_days .. date_max + tolerance_days} contains the
event (nearest span centre on the rare tie);
\item \strong{source} — \code{cruise_ym_col}, the source's own YYYYMM
designation, when supplied and well-formed;
\item \strong{month} — the event's own year-month (the legacy rule).
}
Every key is \verb{YYYY-MM-} + the ship's NODC code from \code{ship_tbl}.
}
\concept{ship}
