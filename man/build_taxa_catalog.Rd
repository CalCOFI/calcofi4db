% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_taxa.R
\name{build_taxa_catalog}
\alias{build_taxa_catalog}
\title{Build the species catalog record (\code{taxa.json})}
\usage{
build_taxa_catalog(con, record, release_version = NULL, release_date = NULL)
}
\arguments{
\item{con}{a DBI connection holding the release tables \code{taxon},
\code{dataset_taxon}, \code{taxon_group} and \code{obs_bio} (and, optionally, \code{dataset})}

\item{record}{the \code{datasets.json} record — a path or the list from
\code{\link[=build_dataset_catalog]{build_dataset_catalog()}} — read for \verb{datasets[].dataset_name_short},
\code{color} and \code{category}, and for the catalog order of \code{datasets[]}}

\item{release_version}{the release version (default: the record's)}

\item{release_date}{the release date, \code{YYYY-MM-DD} (default: the record's)}
}
\value{
A list ready for \code{\link[=write_taxa_catalog]{write_taxa_catalog()}} /
\code{jsonlite::write_json(auto_unbox = TRUE)}, validating against
\code{inst/schema/taxa.schema.json}.
}
\description{
One entry per taxon of the release's \code{taxon} table with an observation at or
below it — the observed taxa plus their ancestors — with its lineage, the ids,
the groups it belongs to, its direct and rolled-up observation counts, and one
block per dataset that observed it carrying that dataset's own name for it.
The 204 vocabulary-only rows of v2026.09.06 get no entry: they are listed
under their dataset in \verb{datasets[].vocabulary_only[]}.
}
\details{
Everything is read from the release: \code{taxon}, \code{dataset_taxon}, \code{taxon_group}
and \code{obs_bio} on \code{con}, the dataset names, colours and categories from
\code{record} (the \code{datasets.json} the release has just written, so the dots on a
species page and on a dataset page cannot disagree). Nothing is authored and
nothing is fetched.

Six grouped queries do the counting and every per-taxon lookup is a split
index, so the builder is linear in the number of \code{obs_bio} groups, not
quadratic in the number of taxa.
}
\seealso{
\code{\link[=write_taxa_catalog]{write_taxa_catalog()}}, \code{\link[=validate_taxa_catalog]{validate_taxa_catalog()}}, \code{\link[=check_taxa_catalog]{check_taxa_catalog()}}
}
\concept{taxonomy}
