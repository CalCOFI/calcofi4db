% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{check_dataset_catalog}
\alias{check_dataset_catalog}
\title{Check every record of the dataset catalog}
\usage{
check_dataset_catalog(
  record,
  registries = NULL,
  network = TRUE,
  probe = NULL,
  timeout = 30
)
}
\arguments{
\item{record}{from \code{\link[=build_dataset_catalog]{build_dataset_catalog()}} (or a \code{datasets.json} path)}

\item{registries}{from \code{\link[=read_catalog_registries]{read_catalog_registries()}}; NULL trusts the record's
own \code{registered} flags}

\item{network}{probe the URLs (default TRUE)}

\item{probe}{the probe function \code{function(url) status}; the tests inject one}

\item{timeout}{seconds per request}
}
\value{
A \link[tibble:tibble]{tibble}: \code{dataset_key}, \code{finding}, \code{level},
\code{detail}, \code{url}, \code{exempt}, \code{question}.
}
\description{
One row per (dataset, finding); a clean dataset has a single \code{ok} row. The
structural half always runs (\code{\link[=catalog_findings]{catalog_findings()}}); the network half
(\code{network = TRUE}, i.e. not \code{CALCOFI_SKIP_LINK_CHECK}) probes every
distribution URL once with a one-byte ranged GET — 404/410/451 is \code{url_dead}
(error), 5xx / no answer is \code{url_unreachable} (warn); \code{retired} and
\code{superseded} rows are not probed (they are expected to be gone). Holdings
are checked for name, category, provider and their links.
}
\seealso{
\code{\link[=assert_dataset_catalog]{assert_dataset_catalog()}}
}
\concept{catalog}
