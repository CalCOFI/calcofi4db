% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{source_accessed_from_git}
\alias{source_accessed_from_git}
\title{When was a dataset's source last read? Measured from git}
\usage{
source_accessed_from_git(dir_parquet, file = "manifest.json")
}
\arguments{
\item{dir_parquet}{one or more sidecar directories
(\verb{data/parquet/\{provider\}_\{dataset\}})}

\item{file}{the sidecar whose history is read (default \code{manifest.json})}
}
\value{
A \link[tibble:tibble]{tibble}: \code{dataset_key} (the directory name),
\code{source_accessed} (Date), \code{source_accessed_method}, \code{source_accessed_ref}
(the commit).
}
\description{
The last commit date of \verb{data/parquet/\{provider\}_\{dataset\}/manifest.json}
— the sidecar every ingest rewrites when it runs — is the best available
record of when the source was read, and it costs no ingest re-run. Method
\code{sidecar_commit}. An untracked sidecar, or a directory outside a repository,
yields \code{NA}.
}
\details{
Prefer \code{\link[=resolve_source_accessed]{resolve_source_accessed()}}, which takes an ingest's own
\code{\link[=stamp_source_access]{stamp_source_access()}} record from \code{metadata.json} when there is one.
}
\concept{registry}
