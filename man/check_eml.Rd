% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eml.R
\name{check_eml}
\alias{check_eml}
\title{Check one dataset's EML document}
\usage{
check_eml(doc, path = NULL, record = NULL, validate = TRUE)
}
\arguments{
\item{doc}{an EML document from \code{\link[=build_eml]{build_eml()}}, or a path to a written \code{.xml}
(the fallback notes are only available from the document)}

\item{path}{the written file to validate; defaults to \code{doc} when \code{doc} is a path}

\item{record}{the dataset's catalog record, for the question-row exemptions}

\item{validate}{run \code{EML::eml_validate()} (default TRUE)}
}
\value{
A \link[tibble:tibble]{tibble}: \code{dataset_key}, \code{finding}, \code{level},
\code{detail}, \code{exempt}, \code{question} — the shape \code{\link[=check_dataset_catalog]{check_dataset_catalog()}} returns.
}
\description{
The schema half is \code{EML::eml_validate()} — EML 2.2's XSDs ship with
\pkg{emld}, so nothing here touches the network. The rest is the required-element
checklist EDI's evaluate applies (see \code{\link[=eml_findings]{eml_findings()}}), read off the built
document rather than off the record, so what is asserted is what was written.
}
\seealso{
\code{\link[=assert_eml]{assert_eml()}}
}
\concept{eml}
