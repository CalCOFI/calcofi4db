% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cruise.R
\name{infer_ship_by_occupation}
\alias{infer_ship_by_occupation}
\title{Infer a ship-less event's ship from the station occupation it matches}
\usage{
infer_ship_by_occupation(
  con,
  table_name,
  datetime_col,
  site_key_col = "site_key",
  occupation_sql = paste("SELECT cruise_key, site_key, datetime FROM sample",
    "WHERE dataset_key = 'swfsc_ichthyo' AND sample_type = 'site'"),
  cruise_tbl = "cruise",
  tolerance_hours = 24,
  ship_key_col = "ship_key",
  method_col = "ship_key_method",
  candidates_col = "cruise_key_candidates"
)
}
\arguments{
\item{con}{DBI connection to DuckDB holding \code{table_name} and \code{cruise_tbl}.}

\item{table_name}{Event table to annotate (updated in place, so it must not
carry a CRS-tagged \code{GEOMETRY} column yet).}

\item{datetime_col}{Timestamp column on the event table.}

\item{site_key_col}{Column holding the CalCOFI \code{site_key} (\code{"LLL.L SSS.S"})
on the event table (default \code{"site_key"}).}

\item{occupation_sql}{A \code{SELECT} returning \code{cruise_key}, \code{site_key} and
\code{datetime}, one row per reference station occupation. Default: the
\code{swfsc_ichthyo} site rows of \code{sample}.}

\item{cruise_tbl}{Cruise reference table, with \code{cruise_key} + \code{ship_key}.}

\item{tolerance_hours}{Hours an occupation's \code{datetime} may differ from the
event's and still count (default 24 — a sorting log's local clock can sit
8 h off the reference's UTC).}

\item{ship_key_col}{Column to fill (created if absent; default \code{"ship_key"}).}

\item{method_col}{Column recording \code{"occupation"} for inferred rows (created
if absent; default \code{"ship_key_method"}).}

\item{candidates_col}{Column listing the candidate cruises of an ambiguous
row, or \code{NULL} to not write it (default \code{"cruise_key_candidates"}).}
}
\value{
A tibble with one row per outcome: \code{outcome} (\code{"source"} = already
had a ship, \code{"occupation"}, \code{"ambiguous"}, \code{"none"}) and \code{n}.
}
\description{
\code{\link[=resolve_cruise_key]{resolve_cruise_key()}} needs a \code{ship_key}: span containment is only
unambiguous within one ship (no two cruises of one ship overlap), and a month
with two or three ships at sea is exactly where a bare \code{YYMM} designation
cannot choose between them (April 2004–2006: 31JD + 32NM + 33LB/33OA). A
source that records no ship but does record a CalCOFI station and a time —
the CDFW Dungeness crab sorting log, whose archived jars ARE CalCOFI tows —
still names its cruise, through the station occupation: at most one ship
occupies a given \code{site_key} within a day.
}
\details{
For every row whose \code{ship_key_col} is NULL, the candidate cruises are the
distinct \code{cruise_key}s of the reference occupations (\code{occupation_sql}) at the
same \code{site_key} whose \code{datetime} is within \code{tolerance_hours} of the event's.
\itemize{
\item exactly one candidate, whose ship \code{cruise_tbl} knows: \code{ship_key_col}
is set to that cruise's ship and \code{method_col} to \code{"occupation"};
\item two or more: nothing is set, and the candidates are written to
\code{candidates_col} (sorted, comma-separated) so the ambiguity can be
reported or asked about — never broken by a guess;
\item one candidate the reference cannot place on a ship: treated as
ambiguous (candidates written, no ship);
\item none: nothing is set.
}
Only the ship is inferred. Run \code{\link[=resolve_cruise_key]{resolve_cruise_key()}} next: its span step
then keys the row to the matched cruise (the occupation lies inside that
cruise's span by construction), and its designation/month steps still apply
where the span does not. A row that already carries a ship is never touched,
and a re-run first clears what an earlier run inferred.
}
\concept{ship}
