% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{parse_qmd_frontmatter}
\alias{parse_qmd_frontmatter}
\title{Parse YAML Frontmatter from Quarto Notebooks}
\usage{
parse_qmd_frontmatter(workflows_dir = here::here(), pattern = "*.qmd")
}
\arguments{
\item{workflows_dir}{Path to the directory containing \code{.qmd} files
(default: current directory via \code{here::here()})}

\item{pattern}{Glob pattern for \code{.qmd} files (default: \code{"*.qmd"})}
}
\value{
Tibble with columns: \code{qmd_file}, \code{target_name}, \code{workflow_type},
\code{dependency} (list column), \code{output}, \code{modifies} (list column of
dependency table names this ingest inserts/modifies)
}
\description{
Reads each \code{.qmd} file in a directory and extracts the \verb{calcofi:} block
from the YAML frontmatter. Returns a tibble describing each workflow's
target name, type, dependencies, and output path.
}
\examples{
\dontrun{
wf <- parse_qmd_frontmatter("workflows/")
wf |> dplyr::filter(workflow_type == "ingest")
}
}
\concept{workflow}
