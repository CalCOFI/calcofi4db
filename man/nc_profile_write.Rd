% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{nc_profile_write}
\alias{nc_profile_write}
\title{Write one chunk of a CF profile file}
\usage{
nc_profile_write(
  nc,
  vars,
  wide,
  profile_cols,
  obs_types,
  profile_id_col = "profile_id",
  start_profile = 1L,
  start_obs = 1L,
  strlen = 64L,
  obs_cols = "depth"
)
}
\arguments{
\item{nc}{Open \code{ncdf4} handle created from \code{\link[=nc_profile_def]{nc_profile_def()}}'s \code{vars}.}

\item{vars}{The \code{vars} element of \code{\link[=nc_profile_def]{nc_profile_def()}}.}

\item{wide}{Wide data.frame, one row per (instance, observation), \strong{ordered by
instance then depth (or time)} so each instance's rows are contiguous. Must
contain \code{profile_id_col}, every \code{obs_cols} entry, and the instance-level and
obs-level columns.}

\item{profile_cols}{Character vector of instance-level column names.}

\item{obs_types}{Character vector of obs-level measurement column names.}

\item{profile_id_col}{Column identifying the instance (profile or trajectory).}

\item{start_profile, start_obs}{1-based write offsets into the profile and obs
dimensions.}

\item{strlen}{Fixed character length, matching \code{\link[=nc_profile_def]{nc_profile_def()}}.}

\item{obs_cols}{Coordinate columns on the observation dimension, matching
\code{\link[=nc_profile_def]{nc_profile_def()}}.}
}
\value{
\code{list(n_profile, n_obs)} — the counts written, for advancing the
offsets on the next chunk.
}
\description{
Writes profile-level variables (one value per profile, taken from each
profile's first row), the \code{rowSize} ragged-array index, and the obs-level
\code{depth} + measurement variables. Call once for a whole dataset, or repeatedly
with advancing offsets to stream a table too large to materialize — the
216M-row \code{obs_ctd_full} is written one cruise partition at a time, holding
~15 MB rather than the whole table.
}
\details{
Non-contiguous profile rows are a \strong{hard stop}, not a warning. A contiguous
ragged array encodes each profile as a run of \code{rowSize} consecutive rows, so
rows interleaved between profiles produce a file that reads cleanly and
assigns depths to the wrong casts. Ordering is the caller's job (\verb{ORDER BY profile, depth}); verifying it is this function's.
}
\concept{publish}
