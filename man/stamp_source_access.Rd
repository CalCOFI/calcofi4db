% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{stamp_source_access}
\alias{stamp_source_access}
\alias{sources_block}
\title{Record when an ingest read its sources}
\usage{
stamp_source_access(files = NULL, urls = NULL)

sources_block(x)
}
\arguments{
\item{files}{local files read as sources}

\item{urls}{URLs the sources were downloaded from}

\item{x}{a stamp table from \code{\link[=stamp_source_access]{stamp_source_access()}}}
}
\value{
A \link[tibble:tibble]{tibble}: \code{source}, \code{method}, \code{accessed} (POSIXct,
UTC), \code{bytes}.

\code{sources_block()}: the list \code{build_metadata_json()} writes as
\code{sources[]}.
}
\description{
Call it at the point the bytes come down (\code{urls}: method \code{download},
accessed now) or, for archives kept on Drive, on the files themselves
(\code{files}: method \code{file_mtime}), and hand the result to
\code{\link[=build_metadata_json]{build_metadata_json()}}'s \code{sources} argument so it lands in the ingest's
\code{metadata.json} as \code{sources[]}. The release then takes the newest stamp as
the dataset's \code{source_accessed} (\code{\link[=resolve_source_accessed]{resolve_source_accessed()}}).
}
\concept{registry}
