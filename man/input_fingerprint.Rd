% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fingerprint.R
\name{input_fingerprint}
\alias{input_fingerprint}
\title{Fingerprint the inputs an ingest's outputs depend on}
\usage{
input_fingerprint(files = character(), values = character())
}
\arguments{
\item{files}{paths to hash by content (missing paths are recorded as
\code{"<missing>"}, which still changes the fingerprint)}

\item{values}{additional values to fold in — a scraped URL list, a package
version, anything not on disk. Coerced to character and hashed in order, so
sort first if the order is not meaningful.}
}
\value{
list with \code{hash} (a single string) and \code{parts} (named character vector,
one entry per input, for reporting \emph{what} changed)
}
\description{
Fingerprint the inputs an ingest's outputs depend on
}
\examples{
\dontrun{
input_fingerprint(
  files  = c("metadata/measurement_type.csv", "metadata/measurement_qual.csv"),
  values = sort(d_zips$url))
}
}
\concept{ingest}
