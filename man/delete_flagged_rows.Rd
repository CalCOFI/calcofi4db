% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{delete_flagged_rows}
\alias{delete_flagged_rows}
\title{Delete Flagged Rows from Database}
\usage{
delete_flagged_rows(
  con,
  validation_results,
  tables_to_clean = NULL,
  dry_run = TRUE
)
}
\arguments{
\item{con}{DuckDB connection}

\item{validation_results}{Results from \code{validate_dataset()}}

\item{tables_to_clean}{Optional character vector of table names to clean.
If NULL (default), cleans all tables with flagged rows.}

\item{dry_run}{If TRUE (default), only reports what would be deleted without
actually deleting. Set to FALSE to perform deletion.}
}
\value{
Tibble with deletion statistics per table
}
\description{
Removes rows that were flagged during validation from the database tables.
Should be called after reviewing flagged files to clean up invalid data.
}
\examples{
\dontrun{
# first, run validation
results <- validate_dataset(con, validations)

# dry run to see what would be deleted
delete_flagged_rows(con, results, dry_run = TRUE)

# actually delete
delete_flagged_rows(con, results, dry_run = FALSE)
}
}
\concept{validate}
