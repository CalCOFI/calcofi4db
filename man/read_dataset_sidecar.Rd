% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{read_dataset_sidecar}
\alias{read_dataset_sidecar}
\title{Read one descriptive sidecar, \code{metadata/{provider}/{dataset}/dataset_meta.yml}}
\usage{
read_dataset_sidecar(path, licenses = NULL)
}
\arguments{
\item{path}{the YAML file}

\item{licenses}{optional character vector of active licence ids}
}
\value{
A named list; \code{NULL} when the file does not exist.
}
\description{
The provider-editable half of a dataset's metadata (plan § D-9): abstract,
methods, creators, contact, keywords, licence, citation, DOI, links — and,
for a \strong{holding} (a dataset with no release yet, § D-11), the structural
keys too plus \code{status: planned | external | archived}, \code{priority}, \code{owner},
\code{next_step}, \code{gh_issue}, \code{module}. \code{visibility} defaults to \code{public}.
Validates the vocabularies; a licence outside \code{license.csv} is refused here
when the registry is given.
}
\concept{catalog}
