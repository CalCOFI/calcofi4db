% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{recheck_pending_urls}
\alias{recheck_pending_urls}
\title{Re-probe the URLs \code{\link[=check_dataset_catalog]{check_dataset_catalog()}} left pending, after their upload}
\usage{
recheck_pending_urls(d, probe = NULL, timeout = 30)
}
\arguments{
\item{d}{the table from \code{\link[=check_dataset_catalog]{check_dataset_catalog()}}}

\item{probe}{the probe function \code{function(url) status}; the tests inject one}

\item{timeout}{seconds per request}
}
\value{
the \code{url_pending} rows of \code{d}, re-classified (0 rows when none were pending).
}
\description{
Takes the check's table, probes exactly its \code{url_pending} rows again and returns them
re-classified: an answer is \code{ok}, a 404/410/451 is now \code{url_dead} (error), anything else
\code{url_unreachable} (warn). Pass the result to \code{\link[=assert_dataset_catalog]{assert_dataset_catalog()}}, which stops on a dead
one — so a new dataset's STAC collection that the upload did NOT write fails the release
before anything is promoted, exactly as a dead URL did before 4.17.1.
}
\seealso{
\code{\link[=check_dataset_catalog]{check_dataset_catalog()}}, \code{\link[=assert_dataset_catalog]{assert_dataset_catalog()}}
}
\concept{catalog}
