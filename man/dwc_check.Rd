% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_check}
\alias{dwc_check}
\title{Check an archive's three tables before it is written}
\usage{
dwc_check(event, occurrence = NULL, emof = NULL, dataset_key = NA_character_)
}
\arguments{
\item{event, occurrence, emof}{the frames from \code{\link[=dwc_event]{dwc_event()}} / \code{\link[=dwc_occurrence]{dwc_occurrence()}} /
\code{\link[=dwc_emof]{dwc_emof()}}}

\item{dataset_key}{named in the findings}
}
\value{
A data frame: \code{dataset_key}, \code{finding}, \code{level}, \code{n}, \code{detail}. One \code{ok}
row when nothing is found.
}
\description{
Runs the \code{obistools} gate (\code{check_eventids()}, \code{check_extension_eventids()},
\code{check_fields()}, \code{check_eventdate()}) plus the referential and
controlled-vocabulary checks the release can make itself. No network: taxon names
are checked against the release's own \code{taxon} table, not \code{obistools::match_taxa()},
which is an interactive WoRMS call.
}
\seealso{
\code{\link[=assert_dwc]{assert_dwc()}}, \code{\link[=dwc_findings]{dwc_findings()}}
}
\concept{dwc}
