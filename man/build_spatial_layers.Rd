% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{build_spatial_layers}
\alias{build_spatial_layers}
\title{The explorer's boundary-layer sidecar: the registry joined with the release's \code{spatial} table}
\usage{
build_spatial_layers(
  con,
  registry_csv,
  version,
  pmtiles_base,
  built = NULL,
  names_max = 200
)
}
\arguments{
\item{con}{DuckDB connection holding \code{spatial} (and, if built, \code{sample_spatial}).}

\item{registry_csv}{Path to \code{metadata/spatial_layers.csv}.}

\item{version}{Release version string, stamped into the sidecar.}

\item{pmtiles_base}{URL prefix of the PMTiles archives (source-layer = \code{dataset_group}).}

\item{built}{When the archives were last built (the \code{ingest_spatial} manifest's mtime) — version
skew between releases and archives is accepted but must be visible.}

\item{names_max}{Above this many distinct names a layer's \code{names} is \code{NULL}.}
}
\value{
A list ready for \code{jsonlite::write_json(auto_unbox = TRUE)}: \code{version}, \code{pmtiles_base},
\code{built}, and \code{layers[]} with \code{id} (the registry \code{dataset_id}), \code{group}, \code{name} (the human
layer name), \code{source}, \code{geom}, \code{filter} (the registry expression verbatim, as parsed JSON),
the symbology defaults, \code{name_field}, \code{description}, \code{attribution}, \code{n_features}, \code{bbox},
\code{names}, \code{n_memberships}.
}
\description{
\code{metadata/spatial_layers.csv} is the registry of the boundary layers (Erin's sheet: one row per
drawable layer with its PMTiles group, default symbology, filter expression and provenance), and
the archives at \verb{\{pmtiles_base\}\{dataset_group\}.pmtiles} carry the features. The explorer must not
hard-code that list nor fetch the CSV from GitHub at runtime (plan 2026-08-31 D23), so each release
ships \code{spatial_layers.json}: the registry verbatim \strong{plus what only the release knows} — each
layer's feature count, bbox, its sorted distinct \code{name}s when there are at most \code{names_max} (the
by-name palette, D24; \code{NULL} above that, and the app falls back to an id-hash palette), and
\code{n_memberships} (distinct root samples in \code{sample_spatial}, so the Regions lens can list exactly
the layers that can summarize something).
}
\concept{explore}
