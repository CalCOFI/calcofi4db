% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_derived.R
\name{ctd_geostrophic}
\alias{ctd_geostrophic}
\title{Relative geostrophic velocity between adjacent CTD stations}
\usage{
ctd_geostrophic(casts, p_ref = 500, dp = 1, min_dx_km = 10)
}
\arguments{
\item{casts}{data frame, one row per sample, ordered or orderable by \code{station_order}: columns
\code{station} (id), \code{latitude}, \code{longitude}, \code{pressure} (dbar), \code{temperature} (deg C),
\code{salinity} (PSS-78); optional \code{q_temperature}, \code{q_salinity}, \code{station_order} (numeric, default
the order of first appearance) and \code{dist_km} (along-line distance; default the great-circle
distance between the pair).}

\item{p_ref}{reference pressure (dbar), default 500.}

\item{dp}{grid spacing (dbar), default 1.}

\item{min_dx_km}{minimum station spacing (km), default 10. A station closer than this to the
last station kept is skipped: the geostrophic shear is \code{1 / dx}, so the extra inshore stations
a few km apart (the SCCOOS 90.27.7 beside 90.28, 93.26.4 beside 93.26.7) turn a small density
difference into metres per second of spurious flow.}
}
\value{
tibble, one row per (station pair x pressure): \code{station_1}, \code{station_2},
\code{dist_mid_km} (from the first station), \code{dx_km}, \code{pressure}, \code{velocity_m_s}, \code{shallow}.
}
\description{
Rasmus Swalethorp's recipe (email 2026-09-22; CalCOFI/workflows#103): per station, absolute
salinity and conservative temperature on a \code{dp} dbar grid to \code{p_ref}, dynamic height anomaly
relative to \code{p_ref} (\code{gsw_geo_strf_dyn_height()}), then for each adjacent pair
\code{v = (dh2 - dh1) / (f * dx)} with \code{f} the Coriolis parameter at the pair's mean latitude and \code{dx}
the distance between the two stations. Positive is 90 degrees to the left of the direction of
increasing station order — for stations ordered nearshore to offshore on a CalCOFI line
(which runs west-south-west) that is equatorward.
}
\details{
The flow is relative to \code{p_ref} (there is no level of known motion), so it has \strong{no anomaly}.
A station shallower than \code{p_ref} has its deepest density carried down (\code{approx(rule = 2)}, as in
the recipe) and the pair is marked \code{shallow = TRUE}. Flagged temperature or salinity samples are
dropped first; a station with fewer than 2 good samples is skipped.
}
\concept{ctd_derived}
