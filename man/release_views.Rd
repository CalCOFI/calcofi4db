% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{release_views}
\alias{release_views}
\title{Views a release carries beside its tables}
\usage{
release_views(removed_in = "next")
}
\arguments{
\item{removed_in}{the release \code{obs}'s objects are dropped in (default \code{"next"}).}
}
\value{
Named list of \code{list(sql, tables, replaces, removed_in)}.
}
\description{
The registry behind \code{catalog.json}'s top-level \code{views} map (3.31.0, pre-release plan D-S1):
one entry per view, with the SQL over \code{{{table}}} tokens (\code{\link[=obs_view_sql]{obs_view_sql()}}), the source
\code{tables} it needs, the physical table it \code{replaces} (marked \code{deprecated} in the catalog while
it still ships) and \code{removed_in}, the release its objects disappear in. \code{\link[=build_release_catalog]{build_release_catalog()}}
consults it by default and includes a view only when every source table is in the release.
}
\details{
\code{"next"} is deliberate for \code{removed_in}: release versions are dates and the next one is not known
at freeze time. A consumer treats any non-NULL \code{removed_in} as "migrate now".
}
\concept{release}
