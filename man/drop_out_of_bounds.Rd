% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds.R
\name{drop_out_of_bounds}
\alias{drop_out_of_bounds}
\title{Delete values outside their declared bounds}
\usage{
drop_out_of_bounds(
  con,
  tbl = "obs",
  mt = NULL,
  dataset_key = NULL,
  type_col = "measurement_type",
  value_col = "measurement_value",
  quiet = FALSE
)
}
\arguments{
\item{con}{DuckDB connection}

\item{tbl}{table or view to check (default \code{"obs"}). Works on any long-format
table: the per-dataset \verb{\{dataset\}_measurement} during wrangling, the emitted
\code{obs}, or \code{sample_measurement}.}

\item{mt}{the measurement registry: a data.frame, a path to
\code{measurement_type.csv}, or \code{NULL} (default) to read a \code{measurement_type}
table from \code{con}.}

\item{dataset_key}{optional \code{dataset_key} to filter to, when \code{tbl} holds more
than one dataset. Ignored if \code{tbl} has no \code{dataset_key} column.}

\item{type_col, value_col}{column names (default \code{measurement_type} /
\code{measurement_value})}

\item{quiet}{suppress the summary message}
}
\value{
The pre-delete tally from \code{\link[=check_measurement_bounds]{check_measurement_bounds()}}, invisibly,
restricted to the \code{out_of_range} rows that were acted on. \code{n_bad} is what was
deleted per type.
}
\description{
Enforcement, kept separate from \code{\link[=check_measurement_bounds]{check_measurement_bounds()}} so that a bound
must be \emph{agreed} before it is allowed to delete data. Run the check first, put
anything surprising to the provider as a question, and call this only for
bounds you are confident describe the impossible.
}
\details{
DELETE rather than flag, for the same reason the \code{-99} sentinel is deleted: in
a long-format table a row IS an assertion that a value was measured. A pH of
-10 left in place silently corrupts every mean, minimum and anomaly a consumer
computes downstream, and there is no in-band way to mark it as not-a-value.

Bounds are meant to be \strong{generous} — impossible, not merely unusual — so this
drops nothing an oceanographer would want to see. If it removes something
interesting, the bound is wrong, not the reading.
}
\examples{
\dontrun{
oob <- drop_out_of_bounds(con, "ctd_measurement",
                          mt = here::here("metadata/measurement_type.csv"))
}
}
\concept{validate}
