% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{append_sample}
\alias{append_sample}
\title{Append event rows into the core \code{sample} dimension}
\usage{
append_sample(con, select_sql, sample_tbl = "sample")
}
\arguments{
\item{con}{a DuckDB connection (open via \code{\link[=get_duckdb_con]{get_duckdb_con()}})}

\item{select_sql}{a SELECT producing the canonical \code{obs} columns by name}

\item{sample_tbl}{target table (default \code{"sample"})}
}
\value{
(invisibly) the total row count of \code{sample_tbl} after the append
}
\description{
\code{select_sql} must yield \code{sample_key}, \code{sample_type}, \code{parent_sample_key},
\code{root_sample_key}, \code{dataset_key}, \code{grid_key}, \code{cruise_key}, \code{latitude},
\code{longitude}, \code{datetime}, \code{depth_min_m}, \code{depth_max_m} by name; \code{geom} is minted
here as \code{ST_Point(longitude, latitude)}. Prefer \code{\link[=build_sample_reference]{build_sample_reference()}} for the
central Phase-2 build; use this for per-dataset (Phase 3) appends.
}
\concept{model}
