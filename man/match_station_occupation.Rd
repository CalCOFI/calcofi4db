% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{match_station_occupation}
\alias{match_station_occupation}
\title{Stamp \code{sample.station_uuid}: the SWFSC station occupation an event belongs to}
\usage{
match_station_occupation(con, sample_tbl = "sample", tolerance_hours = 24)
}
\arguments{
\item{con}{DBI connection holding \code{sample_tbl} (with \code{source_uuid},
\code{root_sample_key}, \code{cruise_key}, \code{site_key}, \code{order_occ}, \code{datetime}).}

\item{sample_tbl}{Table name (default \code{"sample"}).}

\item{tolerance_hours}{Hours a candidate's \code{datetime} may differ from the
root's before it stops counting (default 24).}
}
\value{
Invisibly, a tibble: \code{dataset_key}, \code{method} (\code{"self"} |
\code{"order_occ"} | \code{"datetime"} | \code{"none"}), \code{n} — over ROOT samples only
(\code{"parent"} never appears here: by construction it only ever labels a
NON-root row).
}
\description{
Every \code{sample} root (an event with no parent within its own dataset — a
bottle cast, a CTD cast, an ichthyo site, ...) is matched to the ichthyo
\code{site} row (its \code{source_uuid}) representing the same SWFSC station
occupation, in priority order:
\enumerate{
\item \strong{self} — the root itself IS an ichthyo site (\code{dataset_key = 'swfsc_ichthyo'}): its own \code{source_uuid}.
\item \strong{order_occ} — exactly one ichthyo site shares \verb{(cruise_key, site_key, order_occ)}.
\item \strong{datetime} — exactly one ichthyo site at \verb{(cruise_key, site_key)}
has a \code{datetime} within \code{tolerance_hours} of the root's (whether it is
the only candidate at all, or the only one inside the window).
\item otherwise \code{NULL} (\code{station_uuid_method} \code{NULL} too).
}
The match is computed once per ROOT and copied to every row sharing its
\code{root_sample_key}, which is what makes the crab's examined subsamples
(parented DIRECTLY to an ichthyo site via \code{parent_sample_key} /
\code{root_sample_key} — they never enter the match SQL, whose \code{roots} CTE only
sees rows where \code{sample_key = root_sample_key}) inherit that site's
\code{station_uuid} for free, with no separate matching logic needed. Their
\code{station_uuid_method} is relabeled \strong{\code{"parent"}} rather than \code{"self"} in
that copy step, purely so a consumer can tell "this row IS the SWFSC
station occupation" (\code{"self"}, ichthyo's own site/tow/net) apart from "this
row is a foreign dataset's row directly under one" (\code{"parent"}).
}
\details{
Rebuilds \code{sample_tbl} (DuckDB cannot \code{UPDATE} a table with a CRS-tagged
\code{geom} column), and asserts afterward that its row count and \code{sample_key}
uniqueness are unchanged — the v2026.08.25 lesson (a join that fans out is a
bug in the match, never data to accept).
}
\concept{keys}
