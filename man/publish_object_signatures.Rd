% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_object_signatures}
\alias{publish_object_signatures}
\title{Per-dataset row signatures of a release's objects}
\usage{
publish_object_signatures(
  con,
  catalog,
  tables,
  owners = NULL,
  cache = NULL,
  base_url = "https://storage.googleapis.com/calcofi-db"
)
}
\arguments{
\item{con}{a DuckDB connection with \code{httpfs} loaded when \code{base_url} is remote}

\item{catalog}{the parsed (\code{simplifyVector = FALSE}) \code{catalog.json}}

\item{tables}{table names to sign}

\item{owners}{optional named list, table -> the dataset_keys that read it (from
\code{datasets.json}'s \code{tables[]}); a table read by exactly one dataset is never scanned}

\item{cache}{optional CSV path of the signature cache (created if absent)}

\item{base_url}{prefix joined to each object's \code{path} (\code{/}-separated)}
}
\value{
A data frame: \code{table}, \code{content_hash}, \code{dataset_key} (\code{"*"} for a whole
object), \code{signature}. A table absent from the catalog gives one row with
\code{content_hash = "<missing>"}.
}
\description{
For each object of each table in \code{tables} (read off \code{catalog.json}'s \code{objects[]}),
one row per dataset holding rows in it:
}
\details{
\itemize{
\item an object hive-partitioned by \code{dataset_key} — its own \code{content_hash}, no read;
\item an object of a table only one dataset reads (\code{owners}), or with no \code{dataset_key}
column at all (a vocabulary such as \code{measurement_type}) — \code{dataset_key = "*"}
and its \code{content_hash}: the whole object is an input of every dataset that reads
the table;
\item any other object — read once, \verb{GROUP BY dataset_key}, each group's row signature
(the one \code{freeze_plan()} uses to decide \code{upload}/\code{copy}), cached in \code{cache} under
the object's \code{content_hash} so an unchanged object is never read again.
}
}
\seealso{
\code{\link[=publish_data_parts]{publish_data_parts()}}, \code{\link[=publish_fingerprint]{publish_fingerprint()}}
}
\concept{publish}
