% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keys.R
\name{check_cruise_key_integrity}
\alias{check_cruise_key_integrity}
\title{Fail (or ratchet) the release on a \code{cruise_key} that does not hold up}
\usage{
check_cruise_key_integrity(
  con,
  tolerance_days = 31L,
  known_outside_span = character(),
  manifest_ichthyo = NULL,
  ratchets = list(span_overlaps_max = 2L, derived_max = 152L, key_null_max =
    c(calcofi_dic = 3255L, `sio_pic-zooplankton` = 5087L, `cdfw_dungeness-crab` = 1639L,
    calcofi_bottle = 49L)),
  halt = TRUE,
  sample_tbl = "sample",
  obs_tbl = "obs",
  cruise_tbl = "cruise",
  ship_tbl = "ship"
)
}
\arguments{
\item{con}{DBI connection holding \code{sample_tbl}, \code{obs_tbl}, \code{cruise_tbl},
\code{ship_tbl}.}

\item{tolerance_days}{Days a \code{'swfsc'} cruise's span may be exceeded by one
of its own events before check 6 flags it (default 31 — 99.97\% of the
21,987 outside-span events measured at v2026.08.25 are within this).}

\item{known_outside_span}{\code{sample_key}s exempt from check 6 (named or not;
only the values are used) — an UNLISTED violator still fails.}

\item{manifest_ichthyo}{A single integer: the ichthyo ingest's own
\code{mismatches$cruise_uuid} \code{n_mismatch} count (from \code{manifest.json}), or
\code{NULL} if not supplied (check 7 then fails, naming the gap).}

\item{ratchets}{A list with \code{span_overlaps_max}, \code{derived_max} (both single
integers) and \code{key_null_max} (a named integer vector by \code{dataset_key};
an un-named dataset's allowance is 0, so a first NULL there fails).}

\item{halt}{\code{stop()} on any failing hard check or exceeded ratchet
(default \code{TRUE}).}

\item{sample_tbl, obs_tbl, cruise_tbl, ship_tbl}{Table names.}
}
\value{
A tibble: \code{check}, \code{dataset_key} (or a table/scope label where the
check is not per-dataset), \code{n}, \code{mode} (\code{"fail"} | \code{"ratchet"}),
\code{finding}.
}
\description{
Ten checks over \code{cruise_key} as it is actually used, not as it is assumed to
behave — see the WS-B design memo for the incidents each one guards against.
Checks 1-5 and 7 are hard failures from the first run (\code{n} must be exactly
0); check 6 is hard with \code{known_outside_span} as named exceptions (an
UNLISTED violator still fails); checks 8-10 are ratchets — the current count
must not exceed the allowance, which may only ever be lowered.
}
\details{
\enumerate{
\item \code{cruise_key} matches \verb{^\\\\d\{4\}-(0[1-9]|1[0-2])-[A-Za-z0-9]\{4\}$} on
\code{cruise_tbl}, \code{sample_tbl}, \code{obs_tbl}.
\item \code{cruise.date_ym}'s \code{YYYY-MM} equals the key's own.
\item the key's NODC segment equals \code{ship.ship_nodc} of \code{cruise.ship_key}.
\item every non-NULL \code{sample.cruise_key} / \code{obs.cruise_key} names a real
\code{cruise} row (run \code{\link[=complete_cruise_reference]{complete_cruise_reference()}} first, or this is the
153,306-row finding it exists to close).
\item \code{cruise_key_method = 'swfsc'} rows have a unique non-NULL
\code{cruise_uuid}; \code{'derived'} rows have none.
\item every event's date falls within \verb{[date_min - tolerance_days, date_max + tolerance_days]} of its cruise — for \code{'swfsc'} rows only (a
\code{'derived'} row's span is its own events' min/max by construction, so it
cannot be violated); \code{known_outside_span} names exempt \code{sample_key}s.
\item the ichthyo notebook's own \code{cruise_uuid} vs \code{cruise_key} check
(\code{manifest_ichthyo}, its \code{mismatches$cruise_uuid} count) is 0, and every
ichthyo \code{site}/\code{tow}/\code{net} row has a non-NULL \code{source_uuid}.
\item (ratchet \code{span_overlaps_max}) event spans of two cruises of one ship
overlap by more than 3 days.
\item (ratchet \code{derived_max}) \code{cruise_key_method = 'derived'} row count.
\item (ratchet \code{key_null_max}, per \code{dataset_key}) root samples with a
\code{NULL} \code{cruise_key}.
}
}
\concept{keys}
