% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet.R
\name{export_parquet}
\alias{export_parquet}
\title{Export a DuckDB Table or Query to Parquet}
\usage{
export_parquet(con, table, path, compression = "snappy")
}
\arguments{
\item{con}{DuckDB connection}

\item{table}{Table name or SQL query (wrapped in parentheses for queries)}

\item{path}{Output parquet file path}

\item{compression}{Compression codec (default: "snappy")}
}
\value{
Invisibly returns the output path
}
\description{
Uses DuckDB's native \verb{COPY TO} which handles GEOMETRY columns (as WKB),
compression, and hive partitioning. Preferred over \code{arrow::write_parquet()}
for any table that may contain geometry.
}
\examples{
\dontrun{
# export a table
export_parquet(con, "ship", "output/ship.parquet")

# export a query
export_parquet(con, "(SELECT * FROM ship WHERE ship_key = 'ZZ')",
  "output/ship_new.parquet")
}
}
\concept{parquet}
