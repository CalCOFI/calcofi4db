% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{build_dataset_catalog}
\alias{build_dataset_catalog}
\title{Build the dataset catalog record — \code{datasets.json}}
\usage{
build_dataset_catalog(
  meta,
  coverage,
  catalog,
  registries,
  version = NULL,
  erddap = NULL,
  netcdf = NULL,
  since = NULL,
  source_accessed = NULL,
  spatial_layers = NULL,
  bathymetry = NULL,
  workflows_base = "https://calcofi.io/workflows/"
)
}
\arguments{
\item{meta}{the release \code{metadata.json} (path or parsed list)}

\item{coverage}{the release \code{coverage.json}}

\item{catalog}{the release \code{catalog.json}}

\item{registries}{from \code{\link[=read_catalog_registries]{read_catalog_registries()}}}

\item{version}{the release version (default: the catalog's)}

\item{erddap}{the table from \code{\link[=fetch_erddap_datasets]{fetch_erddap_datasets()}} (NULL: no ERDDAP rows)}

\item{netcdf}{the list from \code{\link[=fetch_netcdf_manifests]{fetch_netcdf_manifests()}}}

\item{since}{a named character vector \verb{dataset_key -> first version}
(\code{\link[=dataset_since_versions]{dataset_since_versions()}})}

\item{source_accessed}{a named character vector \code{dataset_key -> YYYY-MM-DD}
(the release \code{dataset} table's measured column)}

\item{spatial_layers}{the release \code{spatial_layers.json} (for \code{reference[]})}

\item{bathymetry}{the \code{bathymetry/gebco_2025.json} manifest (for \code{reference[]})}

\item{workflows_base}{the URL prefix of the rendered notebooks}
}
\value{
A list ready for \code{\link[=write_dataset_catalog]{write_dataset_catalog()}} / \code{jsonlite::write_json(auto_unbox = TRUE)}.
}
\description{
One record per \code{dataset_key} (plan § D-1, Appendix A), joined from the
release sidecars, the registries and the measured endpoints:
}
\details{
\itemize{
\item identity, description, attribution, links and \code{tables[]} from the
\code{metadata.json} dataset block, with \code{provider} and \code{category} expanded
from their registries and the descriptive sidecar's \code{keywords},
\code{creators[]}, \code{funding} and \code{visibility};
\item \code{coverage} rolled up from \code{coverage.json}: \code{years[]} (the sparkline),
\code{n_stations}, \code{n_variables}, \code{n_taxa}, the depth span, \code{variables[]},
\code{life_stages[]} (when the coverage carries them) and \code{contributes_to[]}
(env-realm variables homed in another category);
\item \code{objects[]} from \code{catalog.json}: the dataset's \verb{dataset_key=} partitions
and the whole tables attributed to it, each with \code{bytes}, \code{sha256}, \code{since}
and an absolute URL; \code{since_version} from \code{since};
\item \code{distributions[]} (\code{\link[=dataset_distributions]{dataset_distributions()}}): parquet, netCDF, the
ERDDAP ids that exist now, the notebook, the calcofi.org page, the source
and the curated \code{distribution.csv} rows;
\item \code{registrations[]} from \code{dataset_status.csv}'s \verb{publish_*} columns — with
ERDDAP and OBIS \emph{measured} (a served id / a curated OBIS row wins over the
registry cell) and Zenodo from the release DOI;
\item \code{status} from \code{dataset_status.csv} plus the open/proposed questions.
}

\code{holdings[]} are the sidecars with \code{status: planned | external | archived}
(a dataset without a release, § D-11), and \code{reference[]} the cruise, ship,
grid and spatial tables, the boundary layers and the bathymetry (Decision
20). Deterministic: no wall clock, sorted by key.
}
\seealso{
\code{\link[=check_dataset_catalog]{check_dataset_catalog()}}, \code{\link[=write_dataset_catalog]{write_dataset_catalog()}}
}
\concept{catalog}
