% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bounds.R
\name{check_measurement_bounds}
\alias{check_measurement_bounds}
\title{Check measured values against the registry's declared bounds}
\usage{
check_measurement_bounds(
  con,
  tbl = "obs",
  mt = NULL,
  dataset_key = NULL,
  type_col = "measurement_type",
  value_col = "measurement_value",
  depth_col = NULL,
  include_undeclared = TRUE
)
}
\arguments{
\item{con}{DuckDB connection}

\item{tbl}{table or view to check (default \code{"obs"}). Works on any long-format
table: the per-dataset \verb{\{dataset\}_measurement} during wrangling, the emitted
\code{obs}, or \code{sample_measurement}.}

\item{mt}{the measurement registry: a data.frame, a path to
\code{measurement_type.csv}, or \code{NULL} (default) to read a \code{measurement_type}
table from \code{con}.}

\item{dataset_key}{optional \code{dataset_key} to filter to, when \code{tbl} holds more
than one dataset. Ignored if \code{tbl} has no \code{dataset_key} column.}

\item{type_col, value_col}{column names (default \code{measurement_type} /
\code{measurement_value})}

\item{depth_col}{optional depth column enabling the depth-window check against
\code{valid_depth_min_m} / \code{valid_depth_max_m} — the depth over which a type is
\emph{defined}. A non-null value outside that window is a finding: it means the
type was emitted where the registry says it does not exist.}

\item{include_undeclared}{report types with no declared bound (default TRUE).
Set FALSE for a violations-only view.}
}
\value{
A \link[tibble:tibble]{tibble}, one row per measurement type present,
ordered worst-first (violations by count, then undeclared by count):
\describe{
\item{\code{status}}{\code{out_of_range} (declared and violated), \code{undeclared}
(nothing declared), \code{ok} (declared and respected)}
\item{\code{n_total}, \code{n_bad}, \code{pct_bad}}{rows checked, rows outside, percent}
\item{\code{n_low}, \code{n_high}}{split by which bound was broken}
\item{\code{v_min}, \code{v_max}}{observed range, for proposing a bound}
\item{\code{valid_min}, \code{valid_max}}{what the registry declares}
\item{\code{n_outside_depth}}{present only when \code{depth_col} is given}
\item{\code{finding}}{a one-line summary, ready to paste into the \code{context}
column of a \code{questions.csv} row}
}
}
\description{
The standard bounds check for an ingest notebook and for the release. Compares
every value in a long-format measurement table against \code{valid_min} /
\code{valid_max} from \code{metadata/measurement_type.csv}, and reports the types that
violate a bound \strong{alongside the types that declare none} — see the note in
\code{R/bounds.R} for why the second matters at least as much as the first.
}
\details{
Read-only: it deletes and rewrites nothing. Enforce with
\code{\link[=drop_out_of_bounds]{drop_out_of_bounds()}}, and only once the bound is agreed.

Bounds may be one-sided. \code{valid_min = 0} with no \code{valid_max} is the useful case
for abundances and counts — "never negative" is agreeable without knowing the
ceiling — and a type is \code{undeclared} only when \emph{both} are missing.
}
\examples{
\dontrun{
b <- check_measurement_bounds(
  con, "ctd_measurement",
  mt = here::here("metadata/measurement_type.csv"))
bounds_datatable(b)
}
}
\seealso{
\code{\link[=drop_out_of_bounds]{drop_out_of_bounds()}} to enforce, \code{\link[=bounds_datatable]{bounds_datatable()}} to render,
\code{\link[=read_measurement_type]{read_measurement_type()}} for the registry, \code{\link[=register_measurement_types]{register_measurement_types()}}
to declare a new bound.
}
\concept{validate}
