% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_measurements.R
\name{check_measurements_catalog}
\alias{check_measurements_catalog}
\title{Check a \code{measurements.json} against the release it was built from}
\usage{
check_measurements_catalog(
  record,
  con = NULL,
  dataset_record = NULL,
  measurement_type = NULL
)
}
\arguments{
\item{record}{from \code{\link[=build_measurements_catalog]{build_measurements_catalog()}} (or a path or URL)}

\item{con}{a DBI connection holding \code{obs_env}; NULL to check only what the
record can prove about itself}

\item{dataset_record}{the \code{datasets.json} record (or its path) whose
\code{datasets[]} every \code{dataset_key} must appear in; NULL to skip that check}

\item{measurement_type}{the registry every \verb{series[].measurement_type} must be
in (a data frame, or NULL to skip)}
}
\value{
A \link[tibble:tibble]{tibble}: \code{check}, \code{level}, \code{ok}, \code{expected},
\code{observed}, \code{detail}.
}
\description{
One row per check with an \code{ok} flag, so a release chunk can print the table
whether or not it passes. The counts are re-measured against \code{obs_env} when
\code{con} is given (the release's own connection, or a promoted release read
back); without it the arithmetic that lives inside the record is still
checked.
}
\seealso{
\code{\link[=assert_measurements_catalog]{assert_measurements_catalog()}}, \code{\link[=measurements_catalog_checks]{measurements_catalog_checks()}}
}
\concept{release}
