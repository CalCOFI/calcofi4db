% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{read_relationships_json}
\alias{read_relationships_json}
\title{Read Relationships JSON and Optionally Apply to dm}
\usage{
read_relationships_json(path, dm = NULL)
}
\arguments{
\item{path}{Path to \code{relationships.json} file}

\item{dm}{Optional \code{dm} object to apply relationships to}
}
\value{
If \code{dm} is provided, returns the dm with PKs/FKs applied.
Otherwise returns the parsed JSON as a list.
}
\description{
Reads a \code{relationships.json} file. If a \code{dm} object is provided,
applies the PKs and FKs from the JSON to it. Otherwise returns the parsed
list.
}
\examples{
\dontrun{
# read and apply to dm
d <- dm::dm_from_con(con, learn_keys = FALSE)
d <- read_relationships_json("relationships.json", dm = d)
dm::dm_draw(d)

# read as raw list
rels <- read_relationships_json("relationships.json")
}
}
\concept{wrangle}
