% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_decide}
\alias{publish_decide}
\title{Reuse a dataset's previous build, or rebuild it?}
\usage{
publish_decide(fp, prior = NULL, outputs = character())
}
\arguments{
\item{fp}{this run's fingerprint, from \code{\link[=publish_fingerprint]{publish_fingerprint()}}}

\item{prior}{the fingerprint recorded with the previous build — a list with
\code{hash} and \code{parts} (as stored in a manifest), or NULL when there is none}

\item{outputs}{paths the previous build must still have on disk to be reused}
}
\value{
A list: \code{action} (\code{"reuse"} | \code{"build"}), \code{changed} (the input names that
differ, via \code{\link[=changed_inputs]{changed_inputs()}}; every input when there is no prior), and
\code{reason} (one line for the notebook to print).
}
\description{
Reuse a dataset's previous build, or rebuild it?
}
\concept{publish}
