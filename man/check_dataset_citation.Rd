% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{check_dataset_citation}
\alias{check_dataset_citation}
\title{Check every dataset's citation, license and DOI, structurally and against its authority}
\usage{
check_dataset_citation(
  ingest_yaml,
  network = TRUE,
  cache_dir = NULL,
  license_csv = NULL,
  refresh = FALSE,
  fetch = NULL,
  timeout = 30
)
}
\arguments{
\item{ingest_yaml}{named list from \code{\link[=read_ingest_yaml]{read_ingest_yaml()}}}

\item{network}{fetch from the authorities (default TRUE); FALSE runs the
structural half and compares against whatever is already cached}

\item{cache_dir}{the \verb{metadata/} root: holds \code{license.csv}, each dataset's
\code{questions.csv} and its \code{citation_authority.json} cache. Defaults to
\verb{metadata/} beside the first ingest's \code{.qmd}.}

\item{license_csv}{path to the license registry (default
\code{{cache_dir}/license.csv})}

\item{refresh}{ignore the cache and fetch again (default FALSE)}

\item{fetch}{the HTTP function, \verb{function(url, accept = NULL, method = "GET", …)} returning \code{list(status, content, url)}; the default uses curl. The tests
inject one that serves saved responses.}

\item{timeout}{seconds per request}
}
\value{
A \link[tibble:tibble]{tibble}: \code{dataset_key}, \code{finding}, \code{detail},
\code{authority}, \code{authority_citation}, \code{checked}, plus \code{level}
(\code{ok}/\code{error}/\code{warn}, see \code{\link[=citation_findings]{citation_findings()}}), \code{exempt} and \code{question}.
}
\description{
One row per (dataset, finding); a clean dataset has a single \code{ok} row. The
structural half always runs: \code{citation_main} must be non-empty
(\code{missing_citation}), carry a 4-digit year (\code{no_year}) and a locator — a DOI,
a URL in the string, or a \code{link_data_source} (\code{no_locator}); \code{license} must be
an active id in \code{metadata/license.csv} (\code{missing_license} when empty or
\code{unknown}, \code{license_unregistered} otherwise), and \code{custom} needs a
\code{license_url} (\code{license_custom_no_url}); \code{doi} must be bare (\verb{10.…/…}).
}
\details{
The network half (\code{network = TRUE}, i.e. not \code{CALCOFI_SKIP_LINK_CHECK}) asks
the source's own authority, chosen by \code{link_data_source}: EDI's cite service
(\verb{packageid=<scope>.<id>.<rev>}, or the newest revision when only
\code{scope}/\code{identifier} are given), an NCEI landing page ("Cite as"), an ERDDAP
\code{.das} (globals), and DataCite for any DOI (its \code{rightsList} SPDX id, and
doi.org content negotiation for the formatted citation). A declared \code{doi} must
answer 200/30x at doi.org (\code{doi_unresolved}). Every fetch is cached in
\code{{cache_dir}/{provider}/{dataset}/citation_authority.json} (\code{authority}, \code{url},
\code{citation}, \code{license}, \code{creator}, \code{title}, \code{checked}, …) so a re-run costs
nothing; pass \code{refresh = TRUE} to fetch again. A cached authority is compared
even when \code{network = FALSE}. A fetched citation (or SPDX license) that differs
from the declared one after \code{\link[=normalize_citation]{normalize_citation()}} is \code{authority_drift}, with
both strings in \code{detail}; a resolver that cannot be reached is
\code{authority_unavailable}. \strong{Nothing is ever written into a notebook's YAML} —
the author's string is the record, the authority is a proposal.

A finding is \code{exempt} when the dataset's \code{questions.csv}
(\code{{cache_dir}/{provider}/{dataset}/questions.csv}) holds an \code{open} or
\code{proposed} row with \code{related_table = dataset} whose \code{related_field} is empty or
names the field the finding is about (\code{citation_main}, \code{license}, \code{doi});
\code{question} carries the row's label. \code{\link[=assert_dataset_citation]{assert_dataset_citation()}} stops on any
non-exempt \code{error}-level row.
}
\seealso{
\code{\link[=read_license_registry]{read_license_registry()}}, \code{\link[=assert_dataset_citation]{assert_dataset_citation()}},
\code{\link[=release_citation]{release_citation()}}
}
\concept{registry}
