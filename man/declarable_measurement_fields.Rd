% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{declarable_measurement_fields}
\alias{declarable_measurement_fields}
\title{The columns \code{\link[=declare_measurement_fields]{declare_measurement_fields()}} is allowed to touch}
\usage{
declarable_measurement_fields()
}
\value{
Character vector of the allowed field names.
}
\description{
\code{category} / \code{variable} were the original two (the Explorer's \emph{Browse} tab);
\code{derivation} / \code{is_canonical} were added in calcofi4db 3.29.0 so the bottle
\verb{r_*} pre-QC types could record "interpolated to standard depth, not an input
for further interpolation" and flip \code{is_canonical} to FALSE without a bare
\code{write_csv()} (WS-G, 2026-09-03). Both new columns are treated as character —
\code{is_canonical} is stored in the CSV as the literal string \code{"TRUE"}/\code{"FALSE"},
matching how \code{\link[=read_measurement_type]{read_measurement_type()}} reads it, so no type coercion happens
here that the registry's own round trip does not already do.
}
\details{
\code{nerc_p01} / \code{units_nerc_p06} followed in 3.32.0 (WS-H2, pre-release decision
D-S2): the controlled-vocabulary ids a portal export needs — OBIS/DwC eMoF's
\code{measurementTypeID} (NERC BODC Parameter Usage Vocabulary P01) and
\code{measurementUnitID} (NERC P06). Both hold the \strong{full concept URI}, and both
are filled only on an exact vocabulary match, so an empty cell means "no
concept states exactly what this type is", never "not looked at".
}
\keyword{internal}
