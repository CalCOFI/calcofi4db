% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{check_obs_pair_parity}
\alias{check_obs_pair_parity}
\title{Assert that \code{obs_bio} + \code{obs_env} hold exactly the rows of \code{obs}}
\usage{
check_obs_pair_parity(con, obs = "obs", bio = "obs_bio", env = "obs_env")
}
\arguments{
\item{con}{DuckDB connection holding \code{obs}, \code{bio} and \code{env}.}

\item{obs, bio, env}{table names.}
}
\value{
Invisibly, a tibble with one row per \verb{(realm, dataset_key)}: \code{n_obs}, \code{n_pair},
\code{n_id_pair}, \code{sig_ok}, \code{n_depth_filled}, \code{n_depth_changed}, \code{ok}.
}
\description{
The gate behind D-S1: before \code{obs} can be served as \code{\link[=obs_view_sql]{obs_view_sql()}} over the pair, the pair
must reproduce it. Per \verb{(realm, dataset_key)} this compares the row count, the number of
distinct \code{obs_id}s and an order-independent signature (\code{bit_xor(hash(...))}) of every column
except depth between \code{obs} and the view run over the pair, and — joining the two on \code{obs_id} —
counts the rows whose depth the pair \strong{filled} (NULL in \code{obs}, the sample's span in the pair:
the documented fallback of \code{\link[=build_obs_slim]{build_obs_slim()}}) and the rows whose non-NULL depth it \strong{changed}
(never allowed). Any group on one side only, any count / signature mismatch, or any changed
depth is an error naming the group.
}
\concept{release}
