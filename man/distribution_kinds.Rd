% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{distribution_kinds}
\alias{distribution_kinds}
\alias{distribution_portals}
\alias{distribution_statuses}
\alias{registration_statuses}
\alias{holding_statuses}
\alias{visibility_values}
\title{The controlled vocabularies of the dataset catalog registries}
\usage{
distribution_kinds()

distribution_portals()

distribution_statuses()

registration_statuses()

holding_statuses()

visibility_values()
}
\value{
Character vectors.
}
\description{
\code{distribution_kinds()} — what a \code{metadata/distribution.csv} row (and a
\code{distributions[]} entry) is: \code{download} (bytes you can fetch), \code{service} (a
queryable endpoint), \code{mirror} (the same rows served by someone else),
\code{source} (where the ingest read from), \code{archive} (a DOI-minting deposit), and
— record-only, derived — \code{page} (a calcofi.org page) and \code{notebook} (the
ingest). \code{distribution_portals()} — the host families a URL is classified
into by \code{\link[=classify_portal]{classify_portal()}}. \code{distribution_statuses()} — \code{current} (answers
today), \code{superseded} (a newer record exists: see \code{superseded_by}), \code{retired}
(the authority no longer serves it), \code{external} (a portal we do not run,
listed as declared) and \code{planned}. \code{registration_statuses()} — what a
\code{registrations[]} row says: \code{published}, \code{planned}, \code{n/a}.
\code{holding_statuses()} — a dataset without a release: \code{planned}, \code{external},
\code{archived}. \code{visibility_values()} — \code{public} | \code{internal} (Decision 25: an
internal dataset is in the record and flagged; every public surface skips it).
}
\concept{catalog}
