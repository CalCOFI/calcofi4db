% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_measurements.R
\name{build_measurements_catalog}
\alias{build_measurements_catalog}
\title{Build the measurements catalog record (\code{measurements.json})}
\usage{
build_measurements_catalog(
  con,
  record,
  measurement_type,
  variable = NULL,
  category,
  release_version = NULL,
  release_date = NULL,
  underway_datasets = "calcofi_mets",
  supplemental_tables = c(ctd_raw = "obs_ctd_full", mets_measurement = "obs_mets_full"),
  supplemental_rows = NULL,
  registries = NULL,
  anomaly = TRUE,
  anomaly_trend = CC_MEASUREMENT_ANOMALY_TREND,
  anomaly_min_cruises = CC_MEASUREMENT_ANOMALY_MIN_CRUISES
)
}
\arguments{
\item{con}{a DBI connection holding the release table \code{obs_env} (and,
optionally, \code{climatology} and the \verb{obs_*_full} supplementals)}

\item{record}{the \code{datasets.json} record — a path or the list from
\code{\link[=build_dataset_catalog]{build_dataset_catalog()}} — read for \verb{datasets[].dataset_name_short},
\code{color} and \code{category}, and for the catalog order of \code{datasets[]}}

\item{measurement_type}{the measurement vocabulary, from
\code{\link[=read_measurement_type]{read_measurement_type()}} (\code{metadata/measurement_type.csv})}

\item{variable}{the label registry (\code{metadata/variable.csv}): a data frame
with at least \code{variable} and \code{label}. \code{NULL} (the default) means no registry
is available and every measurement falls back to its canonical series'
description with the \code{no_label} flag.}

\item{category}{the category registry (\code{metadata/category.csv}): \code{category},
\code{order}, \code{realm}, \code{icon} (and optionally \code{key})}

\item{release_version}{the release version (default: the record's)}

\item{release_date}{the release date, \code{YYYY-MM-DD} (default: the record's)}

\item{underway_datasets}{dataset keys whose series are underway intakes
rather than casts, which is what makes a shared P01 \code{underway_vs_cast}.
Named here rather than inferred, because nothing in the release states it.}

\item{supplemental_tables}{named character vector mapping a registry
\verb{_source_table} to the full-resolution release table its non-released series
live in. Only a registry row from one of these source tables can be a
\code{full_resolution_only[]} row: a row from anywhere else that never reaches
\code{obs_env} is simply not released, not "full resolution only".}

\item{supplemental_rows}{named numeric vector, release table -> row count,
used for any \code{supplemental_tables} entry the connection does not carry (a
promoted release read through \code{cc_get_db()} does not attach them). Read it
from that release's own \code{catalog.json}; never type it. \code{full_rows} is
\code{counts$obs_env_rows} plus these, and is \code{NA} when a supplemental is neither
on the connection nor supplied.}

\item{registries}{the five face registries of \code{measurements.json} 1.1 (plan
2026-09-11 "Measurement faces", Appendix A) — \code{NULL} (the default), a
\verb{metadata/} directory holding \verb{measurement_\{chem,method,scale,why,face\}.csv},
or a named list of any subset of \code{chem}, \code{method}, \code{scale}, \code{why}, \code{face} as
data frames, which is how a caller passes
\code{read_measurement_chem()} and its four siblings. Every field they add is
\strong{additive}: a key with no row in a registry carries no such field, and a
build with \code{registries = NULL} writes exactly the 1.0 record.
\code{kind = "computed"} rows of the scale registry are recomputed here from
their own \code{how} (\verb{gsw.<function>(<numbers>)}, resolved against TEOS-10's
\pkg{gsw}) and never read as typed numbers; a mark whose \code{how} this package
cannot evaluate — carbonate chemistry, which has no R equivalent installed —
passes through with \code{computed_at_build: false}.}

\item{anomaly}{compute the per-band \verb{anomaly\{\}} block (default \code{TRUE}) — the
yearly departure from the release's own \code{climatology}, per depth band, for
every key the climatology covers. \code{FALSE}, or a connection carrying no
usable \code{climatology}, leaves the field out.}

\item{anomaly_trend}{the two years the least-squares trend is fitted between,
inclusive (default 1984-2021).}

\item{anomaly_min_cruises}{the cruises a year needs before it may set the
trend, the extremes or the shared \code{ymax} (default 2). Thinner years stay in
the series — the page draws them faded — but never steer a fitted line.}
}
\value{
A list ready for \code{\link[=write_measurements_catalog]{write_measurements_catalog()}} /
\code{jsonlite::write_json(auto_unbox = TRUE)}, validating against
\code{inst/schema/measurements.schema.json}.
}
\description{
One entry per measurement key the release's \code{obs_env} carries — the registry's
\code{variable} where set, else the \code{measurement_type} — with its series (one per
\verb{measurement_type × dataset}), each series' counts by year, calendar month,
depth band and quality code, its observed quantiles, the registry's declared
bounds, the source and flag columns, the NERC ids, and the other keys sharing
its P01 concept that are deliberately kept apart.
}
\details{
Everything is read: \code{obs_env} (and, when present, \code{climatology} and the
\verb{obs_*_full} supplementals) on \code{con}, the dataset names, colours, categories
and order from \code{record} (the \code{datasets.json} the release has just written, so
a dataset dot cannot disagree between \verb{/datasets/} and \verb{/measurements/}), and
the vocabulary from the registries. Nothing is authored and nothing is
fetched.

Six grouped queries do the counting — never one per key — and every per-series
lookup is a split index, so the builder is linear in the number of \code{obs_env}
groups.

\strong{The label.} \code{label} comes from \code{metadata/variable.csv} when that registry
carries a row for the key. Absent a row it falls back to the canonical series'
registry \code{description} and the measurement carries the \code{no_label} flag: a
column note is not a title, and the builder does not invent one.
}
\seealso{
\code{\link[=write_measurements_catalog]{write_measurements_catalog()}}, \code{\link[=validate_measurements_catalog]{validate_measurements_catalog()}},
\code{\link[=check_measurements_catalog]{check_measurements_catalog()}}, \code{\link[=measurement_series_flags]{measurement_series_flags()}}
}
\concept{release}
