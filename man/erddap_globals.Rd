% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eml.R
\name{erddap_globals}
\alias{erddap_globals}
\title{The ERDDAP global attributes of one dataset, from the same record}
\usage{
erddap_globals(record)
}
\arguments{
\item{record}{one dataset record}
}
\value{
A named character vector of ERDDAP global attributes (absent values
are omitted, never blank).
}
\description{
Plan § D-8: ERDDAP's globals, the DwC-A's EML, the EDI package's EML and the
page's JSON-LD are rendered from one record, so none of them is typed twice.
\code{infoUrl} is the dataset page. \verb{creator_*} follow the same order \code{\link[=build_eml]{build_eml()}}
does (creators → pi_names → the provider organization); \code{creator_email} falls
back to \code{\link[=eml_contact_address]{eml_contact_address()}}.
}
\concept{eml}
