% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitemap.R
\name{check_sitemap}
\alias{check_sitemap}
\title{Check a generated sitemap}
\usage{
check_sitemap(
  d,
  network = !nzchar(Sys.getenv("CALCOFI_SKIP_LINK_CHECK")),
  probe = NULL
)
}
\arguments{
\item{d}{the tibble from \code{\link[=build_datasets_sitemap]{build_datasets_sitemap()}}}

\item{network}{probe every URL (default: off when \code{CALCOFI_SKIP_LINK_CHECK})}

\item{probe}{a function \verb{url -> integer status} (the tests inject one)}
}
\value{
A \link[tibble:tibble]{tibble} \code{loc}, \code{finding}, \code{level}, \code{detail}.
}
\description{
The structural half always runs (https, uniqueness, \code{lastmod} shape, the pages
leading); the network half asks every URL with a one-byte ranged GET, like the
index's link check, and is skipped when \code{CALCOFI_SKIP_LINK_CHECK} is set.
}
\concept{catalog}
