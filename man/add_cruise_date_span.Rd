% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cruise.R
\name{add_cruise_date_span}
\alias{add_cruise_date_span}
\title{Add the observed date span of each cruise to the cruise reference}
\usage{
add_cruise_date_span(con, event_sql, cruise_tbl = "cruise")
}
\arguments{
\item{con}{DBI connection to DuckDB.}

\item{event_sql}{A \code{SELECT} returning two columns named \code{cruise_key} and
\code{datetime} — one row per event (site, tow, cast, ...). Rows with a NULL in
either are ignored.}

\item{cruise_tbl}{Name of the cruise reference table (default \code{"cruise"}).}
}
\value{
Invisibly, a tibble with one row per cruise: \code{cruise_key},
\code{date_ym} (if present), \code{date_min}, \code{date_max}, \code{n_events}, \code{spills_month}
(span extends outside the designated month) and \code{overlaps} (the span
intersects another cruise of the same ship — a reference-data error that
would make span matching ambiguous; assert \code{sum(overlaps) == 0}).
}
\description{
Computes \code{date_min} / \code{date_max} (DATE) per \code{cruise_key} from an event query
and writes them onto \code{cruise_tbl}, so downstream ingests can match events to
cruises by containment rather than by calendar month (see
\code{\link[=resolve_cruise_key]{resolve_cruise_key()}}). Run this in the ingest that owns the \code{cruise}
reference (\code{ingest_swfsc_ichthyo.qmd}), after \code{cruise_key} has been propagated
to the event tables; every other ingest loads that shard read-only.
}
\concept{ship}
