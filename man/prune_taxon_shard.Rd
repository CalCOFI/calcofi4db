% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{prune_taxon_shard}
\alias{prune_taxon_shard}
\title{Prune the taxa references to one dataset's shard}
\usage{
prune_taxon_shard(con, dataset_key)
}
\arguments{
\item{con}{a DuckDB connection holding \code{taxon} / \code{dataset_taxon} (and
optionally \code{taxon_group}) as built by the three builders}

\item{dataset_key}{provider_dataset to keep}
}
\value{
(invisibly) a named list of the surviving row counts
}
\description{
\code{build_taxon_reference()} / \code{resolve_dataset_taxon()} / \code{build_taxon_group()}
read \emph{whichever} taxon sources are present in \code{con}, which inside an ingest is
normally just that dataset's vocabulary — but not always. An ingest may have
loaded another dataset's tables as references, and \code{swfsc_ichthyo} holds a
WoRMS \strong{lineage hierarchy} (\code{build_taxon_hierarchy()}) that is broader than
the taxa its own observations reach. This trims all three to this dataset's
shard so the release union stays small and the shards stay disjoint-ish.
}
\details{
Lineage \strong{ancestors are kept}: descendant expansion walks
\code{parent_taxon_key}, so dropping an ancestor would break the chain. The kept
set is therefore the transitive parent closure of \code{dataset_taxon.taxon_key},
not just the directly-referenced taxa.

Generic — there is nothing dataset-specific here beyond the \code{dataset_key}
argument. Call it after the three builders in an ingest that either holds a
hierarchy table or has other datasets' vocabulary tables in scope.
}
\concept{taxonomy}
