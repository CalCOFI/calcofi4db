% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_taxon_ids}
\alias{check_taxon_ids}
\title{The taxa that no authority resolved, per dataset — reported, and gated}
\usage{
check_taxon_ids(con, allow = character(), halt = TRUE, verbose = TRUE)
}
\arguments{
\item{con}{a DBI connection holding \code{taxon} and \code{dataset_taxon} (and \code{obs}, if
present, for the observation-level counts)}

\item{allow}{character vector of dataset-local \code{taxon_key}s that are known to
be unresolvable and are accepted as such}

\item{halt}{logical; \code{stop()} on an unallowlisted local key (default \code{TRUE})}

\item{verbose}{logical; message the summary}
}
\value{
a data.frame, one row per \code{dataset_key}, with the taxon- and
observation-level counts (invisibly when \code{verbose = FALSE})
}
\description{
A taxon that reaches the release without an authority id is invisible to any
consumer that filters or joins on one, and nothing used to say so. That is how
all 128 Farallon taxa and 64,956 observations became unreachable through
\code{db-viz-hex::get_sp()}'s \code{worms_id} join while every check in the pipeline
passed.
}
\details{
Two conditions, deliberately graded differently:
\itemize{
\item \strong{A dataset-local \code{taxon_key}} (no \verb{worms:} / \verb{itis:} prefix) means \emph{no
authority resolved this taxon at all}. This \strong{fails} unless the key is in
\code{allow} — the allowlist is where a genuinely non-taxonomic class such as
\code{cce-lter_zooscan:16} (naupliar stage) is declared, in the open, one key at
a time. A new unresolved taxon can then never hide among the known ones.
\item \strong{An authority key with no \code{worms_id}} is reported but does not fail:
WoRMS legitimately lacks some taxa (trinomial subspecies, mostly), and an
\verb{itis:}-keyed bird is correctly keyed either way.
}
}
\concept{check}
