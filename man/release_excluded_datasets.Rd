% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{release_excluded_datasets}
\alias{release_excluded_datasets}
\title{Datasets held out of the release by \code{calcofi.in_release: false}}
\usage{
release_excluded_datasets(workflow_dir, pattern = "^ingest_.*\\\\.qmd$")
}
\arguments{
\item{workflow_dir}{Directory containing the \code{ingest_*.qmd} files.}

\item{pattern}{Regular expression matching ingest filenames.}
}
\value{
Character vector of \code{provider_dataset} labels (the
\code{data/parquet/} subdirectory names) to exclude; empty when every
ingest is in the release.
}
\description{
An ingest under development can be run end-to-end — writing its own
\verb{data/parquet/\{provider\}_\{dataset\}/} outputs, manifest and sidecars — while
being kept out of the frozen release, by adding \code{in_release: false} to
its \code{calcofi:} YAML block. This returns those datasets' labels, which is
what every release-side discovery step filters on: the table registry
(\code{\link[=build_release_table_registry]{build_release_table_registry()}}), the core shard union
(\code{\link[=core_shard_paths]{core_shard_paths()}}), and \code{release_database.qmd}'s
\code{relationships.json} / \code{metadata.json} / \code{manifest.json} globs.
}
\details{
Omitting the key means the ingest IS in the release, so existing notebooks are
unaffected.
}
\examples{
\dontrun{
release_excluded_datasets("workflows")   # "dfw_dungeness-crab"
}
}
\concept{wrangle}
