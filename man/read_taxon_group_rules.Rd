% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{read_taxon_group_rules}
\alias{read_taxon_group_rules}
\title{Read the \code{taxon_group} rule registry (\code{metadata/taxon_group.csv})}
\usage{
read_taxon_group_rules(path)
}
\arguments{
\item{path}{path to the registry CSV}
}
\value{
a data.frame of rules (columns \code{taxon_group_key}, \code{description},
\code{rule}, \code{rule_value}, \code{dataset_key}, \code{match_column}, \code{match_value})
}
\description{
Strict read — every column character, an empty cell is \code{NA} (never the string
\code{"NA"}), and the shape is validated: \code{rule} is \code{class} (every vocabulary taxon
whose \code{class} equals \code{rule_value}, cross-dataset by construction) or
\code{dataset_taxon} (rows of one dataset's vocabulary matched on \code{match_column}
∈ \code{ds_taxa_code} / \code{ds_scientific_name} / \code{ds_common_name} = \code{match_value}).
}
\concept{taxonomy}
