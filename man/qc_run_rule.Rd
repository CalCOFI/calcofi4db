% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_run_rule}
\alias{qc_run_rule}
\title{Execute one rule, returning its findings}
\usage{
qc_run_rule(
  con,
  rule,
  limit = 500L,
  present_types = NULL,
  scope_values = list()
)
}
\arguments{
\item{con}{a DBI connection carrying the tables the rule targets}

\item{rule}{one row of \code{\link[=qc_read_rules]{qc_read_rules()}}}

\item{limit}{cap on rows returned. The \code{COUNT} is always computed over the full
result, so a truncated display never understates the problem — a rule that
silently showed 500 of 40,000 hits would read as "minor".}

\item{present_types}{output of \code{\link[=qc_present_types]{qc_present_types()}}; \code{NULL} disables the check}

\item{scope_values}{named list supplying scope parameters, e.g.
\code{list(cruise_key = "2023-11-33P4")}. A rule with \code{scope = "cruise"} reads the
full-resolution \code{obs_ctd_full} and is meaningless unscoped, so it SKIPS rather
than silently scanning everything.}
}
\value{
list with \code{rule_key}, \code{n}, \code{findings}, \code{elapsed_s}, \code{error}, \code{skipped},
\code{skip_reason}
}
\description{
PRECONDITIONS ARE CHECKED FIRST, and this is not a nicety. A rule whose input
measurement type is absent returns zero rows, which is indistinguishable from
"the data is clean" — a false pass. The three bottle-vs-sensor calibration rules
did exactly that against release \code{v2026.07.30}, which carries only \code{btl_ammonium}
because it predates the change making the other bottle-reference types canonical.
A QA/QC tool that reports green without having checked anything is worse than no
tool, so an unmet precondition is \code{skip}, never \code{pass}.
}
\concept{qc}
