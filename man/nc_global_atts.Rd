% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{nc_global_atts}
\alias{nc_global_atts}
\title{Build the CF/ACDD global attributes for a published dataset}
\usage{
nc_global_atts(
  dataset_key,
  dataset_meta = list(),
  release,
  shape = c("profile", "groups"),
  cf_scope = NULL,
  workflow_url = NULL,
  date_created = NULL,
  extra = list()
)
}
\arguments{
\item{dataset_key}{Provenance stamp, e.g. \code{"calcofi_ctd-cast"}.}

\item{dataset_meta}{The ingest's \code{dataset_meta} list (\code{dataset_name},
\code{description}, \code{citation_main}, \code{coverage_temporal}, \code{coverage_spatial},
\code{license}, …). Missing keys simply omit their attribute. A \code{title} or
\code{description} here wins, so a caller overrides the derived text by merging
its \code{calcofi.netcdf} block over \code{dataset_meta} before passing it.}

\item{release}{Database release the file was built from, e.g. \code{"v2026.07.30"}.}

\item{shape}{\code{"profile"} or \code{"groups"}, from \code{\link[=plan_dataset_netcdf]{plan_dataset_netcdf()}}.}

\item{cf_scope}{Override for the honesty statement about CF coverage;
defaults to text appropriate to \code{shape}.}

\item{workflow_url}{Rendered notebook URL, written as \code{references}.}

\item{date_created}{ACDD creation date. Defaults to the \strong{release} date
parsed from \code{release}, deliberately: see Details.}

\item{extra}{Named list of additional or overriding global attributes.}
}
\value{
Named list of global attributes, ready for \verb{ncdf4::ncatt_put(nc, 0, …)}.
}
\description{
Derives the file's self-description from the ingest notebook's
\code{calcofi.dataset_meta} YAML — the same block that feeds the release \code{dataset}
table — so the netCDF, the database and the schema site cannot describe the
same dataset differently. Per-file overrides go in \code{calcofi.netcdf}.
}
\details{
\strong{Why \code{date_created} is the release date, not \code{Sys.time()}.} The publisher
skips re-uploading a file whose sha256 matches an earlier release
(release-named paths, bytes written once). A wall-clock \code{date_created} puts a
fresh timestamp inside every build, so no rebuild is ever byte-identical and
that check can never fire — it silently degrades to "always re-upload". Tying
the attribute to the release makes a rebuild of the same release reproducible,
which is also the more useful claim: it dates the data product, not the run.
}
\concept{publish}
