% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{build_dataset_taxon}
\alias{build_dataset_taxon}
\title{Build the \code{dataset_taxon} crosswalk (per-dataset vocabulary -> \code{taxon})}
\usage{
build_dataset_taxon(
  con,
  measurement_taxon = NULL,
  overrides = NULL,
  tbl = "dataset_taxon"
)
}
\arguments{
\item{con}{a DuckDB connection with the per-dataset taxon tables loaded}

\item{measurement_taxon}{optional data.frame of the composite-type crosswalk
(\code{metadata/measurement_taxon.csv}) so cufes/phyllosoma/euphausiid taxa,
which live in \code{measurement_type} names not a taxon table, are included}

\item{overrides}{optional data.frame of manual id resolution
(\code{metadata/taxon_override.csv}) for coarse taxa (phyto groups, mammals)}

\item{tbl}{target table name (default \code{"dataset_taxon"})}
}
\value{
(invisibly) the row count written
}
\description{
One row per (dataset, local taxon): the dataset's own \code{ds_taxon_key}
(\code{"<dataset-or-known-list>:<local id>"}, all lowercase — e.g. \code{calcofi:19}
for the shared CalCOFI species list, \code{cce-lter_zoodb:3} otherwise), its
\code{ds_scientific_name} / \code{ds_common_name} / \code{ds_taxa_code}, and the global
\code{taxon_key} it resolves to. Deduped on \code{ds_taxon_key}.
}
\concept{taxonomy}
