% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{build_targets_list}
\alias{build_targets_list}
\title{Build Targets List from Quarto Frontmatter}
\usage{
build_targets_list(
  workflows_dir = here::here(),
  corrections = c("metadata/ship_renames.csv", "metadata/measurement_type.csv"),
  exclude = NULL,
  verbose = TRUE
)
}
\arguments{
\item{workflows_dir}{Path to workflows directory (default: \code{here::here()})}

\item{corrections}{Character vector of correction file paths relative to
\code{workflows_dir} (default: \code{c("metadata/ship_renames.csv", "metadata/measurement_type.csv")})}

\item{exclude}{Character vector of target names to exclude from the pipeline
(default: NULL). Excluded targets are also removed from other targets'
dependency lists. Useful for skipping large workflows like
\code{"ingest_calcofi_ctd-cast"}.}

\item{verbose}{Print parsed workflow table (default: TRUE)}
}
\value{
A \code{list()} of \code{tar_target()} objects ready for \verb{_targets.R}
}
\description{
Reads \verb{calcofi:} YAML frontmatter from all \code{.qmd} files in the workflows
directory and returns a \code{list()} of \code{targets::tar_target()} calls suitable
for use as the body of \verb{_targets.R}. Includes a \code{corrections_csv} target
that tracks \code{metadata/ship_renames.csv} and \code{metadata/measurement_type.csv},
so any edit to those files forces re-run of dependent ingest targets.
}
\details{
Workflows with \verb{dependency: [auto]} (typically \code{release_database}) auto-
depend on all targets whose \code{workflow_type} is \code{"ingest"} or \code{"spatial"}.
}
\examples{
\dontrun{
# in _targets.R:
library(targets)
devtools::load_all(here::here("../calcofi4db"))
build_targets_list()

# skip CTD ingest:
build_targets_list(exclude = "ingest_calcofi_ctd-cast")
}
}
\concept{workflow}
