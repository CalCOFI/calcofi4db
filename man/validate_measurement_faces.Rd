% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_measurement_face.R
\name{validate_measurement_faces}
\alias{validate_measurement_faces}
\title{Validate the five measurement-face registries against Appendix A}
\usage{
validate_measurement_faces(
  chem_path,
  method_path,
  scale_path,
  why_path,
  face_path,
  measurement_type_path = NULL
)
}
\arguments{
\item{chem_path, method_path, scale_path, why_path, face_path}{paths to the
five registries}

\item{measurement_type_path}{path to \code{metadata/measurement_type.csv}, used
to check \code{measurement_face} keys exist. \code{NULL} skips that check.}
}
\value{
a tibble of findings (\code{registry}, \code{key}, \code{rule}, \code{detail}); zero
rows means clean.
}
\description{
Returns findings rather than stopping (mirrors \code{check_measurement_bounds()}):
a workstream fills the registries, runs this, and resolves every row it
reports before calling itself done. Checks, one row of output each:
}
\details{
\itemize{
\item a \code{measurement_chem} / \code{measurement_method} / \code{measurement_scale} /
\code{measurement_face} row with an empty \code{source}
\item a value outside its column's Appendix A vocabulary (chem \code{role}/\code{via},
method \code{platform}, scale \code{kind}, why \code{kind}, face \code{face_kind})
\item a \code{measurement_why} key with zero, or more than one, \code{rank == 1} row
\item a \code{measurement_face} key absent from \code{measurement_type.csv}
\item a \code{nerc_l22} (method) that is not an exact-form L22 concept URI
}
}
\concept{registry}
