% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{dataset_since_versions}
\alias{dataset_since_versions}
\title{The first release each dataset appeared in}
\usage{
dataset_since_versions(
  versions,
  base = CC_RELEASES_HTTPS,
  fetch = NULL,
  known = NULL
)
}
\arguments{
\item{versions}{the parsed \code{versions.json} (the list under \code{versions}, or the
whole object)}

\item{base}{the HTTPS releases prefix}

\item{fetch}{the HTTP function}

\item{known}{a named character vector from a previous \code{datasets.json}
(\code{dataset_key -> since_version}); those keys are not re-derived}
}
\value{
A named character vector \code{dataset_key -> version}.
}
\description{
Walks \code{versions.json} oldest → newest and reads each version's
\code{metadata.json} (retired versions keep their sidecars), recording the first
version whose \code{datasets} block names the key. ~30 small fetches at release
time; the tests inject a \code{fetch} over fixtures.
}
\concept{catalog}
