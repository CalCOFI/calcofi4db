% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_dataset_taxon}
\alias{check_dataset_taxon}
\title{The ingest asserts its own taxon crosswalk (taxon plan D6)}
\usage{
check_dataset_taxon(
  con,
  dataset_key,
  allow = character(),
  halt = TRUE,
  codes = NULL,
  verbose = TRUE
)
}
\arguments{
\item{con}{a DBI connection holding \code{dataset_taxon} (and \code{taxon})}

\item{dataset_key}{the dataset whose crosswalk is checked}

\item{allow}{character vector of \code{taxon_key}s accepted as-is: dataset-local
keys of non-taxonomic classes, or a \verb{worms:} key for an Aves taxon with no
TSN}

\item{halt}{logical; \code{stop()} on any finding (default \code{TRUE})}

\item{codes}{optional character vector of the \code{ds_taxa_code}s the
observations reference (e.g. \verb{DISTINCT species_code} of the source
observation table); every one must be in the vocabulary}

\item{verbose}{logical; message the summary}
}
\value{
a data.frame with one row per finding (\code{check}, \code{ds_taxon_key},
\code{ds_taxa_code}, \code{taxon_key}, \code{detail}); zero rows when clean. Invisible
when \code{verbose = FALSE}.
}
\description{
Call it after \code{\link[=resolve_dataset_taxon]{resolve_dataset_taxon()}} and before \code{append_obs()}. Three
findings, each a row of the returned report:
}
\details{
\itemize{
\item \strong{\code{missing_code}} — a code the observations reference (\code{codes}) that is not
in this dataset's \code{dataset_taxon}. Farallon's \code{MEGU} (the pre-split Mew Gull
code, present in the observations and absent from the species list) is the
case that motivated it: an \code{obs} projection joining on the code would drop
or NULL those rows with no error anywhere.
\item \strong{\code{unresolved}} — a \code{dataset_taxon} row with no authority \code{taxon_key}
(\verb{worms:} / \verb{itis:}), unless its dataset-local key is in \code{allow} — the
ingest's own declaration of a genuinely non-taxonomic class (zooscan
"nauplii", phyto "undefined code"), one key at a time, with a comment.
\item \strong{\code{aves_not_itis}} — a taxon whose class is Aves that did not key \verb{itis:}
(no accepted TSN resolved; see \code{\link[=taxon_key_of]{taxon_key_of()}}). Checked here because this
is where it is cheap to fix — a TSN in \code{taxon_override.csv}. An ingest that
accepts the \verb{worms:} key lists that key in \code{allow}. Needs \code{taxon} in \code{con}
(\code{\link[=build_taxon_reference]{build_taxon_reference()}}) for the class; skipped without it.
}

\code{release_database.qmd}'s \code{\link[=check_taxon_ids]{check_taxon_ids()}} stays as the backstop.
}
\concept{check}
