% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{taxa_rank_reference}
\alias{taxa_rank_reference}
\title{The canonical taxonomic rank ordering (\code{taxa_rank})}
\usage{
taxa_rank_reference()
}
\value{
a data.frame of \code{taxonRank} + \code{rank_order}
}
\description{
One row per rank, ordered kingdom-down, so \code{taxon.rank_order} sorts a
hierarchy without a consumer hard-coding rank names.
}
\details{
This used to be a vector inside \code{\link[=build_taxon_hierarchy]{build_taxon_hierarchy()}}, which exactly one
ingest calls — so the \code{taxa_rank} lookup existed in the \code{swfsc_ichthyo}
connection and nowhere else, and \code{build_taxon_reference()}'s left join to it
produced \code{rank_order = NA} for every other dataset's taxa. In release
v2026.08.06 that was \strong{100\% of ITIS-keyed taxa} (all 169, i.e. every seabird
and marine mammal) plus 252 WoRMS-keyed ones — 172 species, 83 genera and 49
families with no sortable rank.

The vocabulary spans BOTH authorities. WoRMS and ITIS do not use the same rank
set, and eight ranks the release actually carries were absent from the old
vector — \code{Gigaclass}, \code{Infrakingdom}, \code{Megaclass}, \code{Parvphylum},
\code{Phylum (Division)}, \code{Subphylum (Subdivision)}, \code{Subterclass}, \code{Superdomain} —
so those taxa had no \code{rank_order} even where the lookup was present.

Ordering is by nesting depth, not by a strict Linnaean canon: what a consumer
needs is "does this rank sit above or below that one", and ties are harmless.
}
\examples{
head(taxa_rank_reference())
}
\concept{taxonomy}
