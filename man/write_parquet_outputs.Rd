% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{write_parquet_outputs}
\alias{write_parquet_outputs}
\title{Write Tables to Parquet Files}
\usage{
write_parquet_outputs(
  con,
  output_dir,
  tables = NULL,
  partition_by = NULL,
  sort_by = NULL,
  primary_keys = NULL,
  strip_provenance = TRUE,
  compression = "zstd",
  mismatches = NULL,
  supplemental = NULL
)
}
\arguments{
\item{con}{DuckDB connection}

\item{output_dir}{Directory for parquet files}

\item{tables}{Optional vector of table names to export. If NULL, exports all tables.}

\item{partition_by}{Named list mapping table names to partition column(s),
e.g. \code{list(ctd_measurement = "cruise_key")}. Creates hive-partitioned
subdirectories.}

\item{sort_by}{Named list mapping table names to sort column(s) for
optimized row group statistics. For Hilbert-curve spatial sorting,
use \code{"hilbert:lon_col,lat_col"}. Example:
\code{list(ctd_measurement = c("measurement_type", "depth_m"), site = "hilbert:longitude,latitude")}.}

\item{primary_keys}{Named list mapping table names to their primary-key
column (e.g. from a \code{relationships.json} \code{primary_keys} block). Appended
as a final ORDER BY tiebreaker so output is a unique total order and thus
\strong{byte-identical across re-runs of unchanged data} — letting
\code{\link[=sync_to_gcs]{sync_to_gcs()}} skip re-uploading parquet that did not actually change.
Without it, ties in \code{sort_by} break nondeterministically under parallel
writes, changing the bytes (and crc32c) on every run.}

\item{strip_provenance}{Remove provenance columns (\emph{source}*, _ingested_at) (default: TRUE)}

\item{compression}{Parquet compression codec (default: "snappy")}

\item{mismatches}{Optional named list of mismatch tibbles to include in
manifest.json. Expected names: \code{ships}, \code{measurement_types}, \code{cruise_keys}.
Each element should be a tibble (or data frame) describing unresolved
entities. Zero-row tibbles are recorded as empty arrays.}

\item{supplemental}{Character vector of table names that are supplemental
outputs (e.g. wide-format tables for ERDDAP). These are written to parquet
and listed in manifest.json under \code{"supplemental"}, but are excluded by
default from downstream database loading via \code{\link[=load_prior_tables]{load_prior_tables()}}.}
}
\value{
Tibble with export statistics (table, rows, file_size, path)
}
\description{
Exports all tables from a DuckDB connection to parquet files.
Optionally strips provenance columns for public releases.
}
\examples{
\dontrun{
con <- get_duckdb_con("calcofi.duckdb")
stats <- write_parquet_outputs(con, "output/parquet")
}
}
\concept{wrangle}
