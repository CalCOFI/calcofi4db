% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{build_versions_json}
\alias{build_versions_json}
\title{Build the \code{versions.json} register of every release under a prefix}
\usage{
build_versions_json(
  bucket,
  prefix = CC_RELEASE_PREFIX,
  consolidated = character(),
  current = NULL
)
}
\arguments{
\item{bucket}{GCS bucket}

\item{prefix}{release prefix (\code{ducklake/releases}, or a staging prefix)}

\item{consolidated}{character vector of consolidated versions}

\item{current}{optional record for the release being written right now
(takes precedence over what the bucket holds for that version)}
}
\value{
list of release records, newest first
}
\description{
Discovers each \code{{prefix}/{version}/catalog.json} in the bucket and returns
the list \code{release_database.qmd} writes as \code{versions.json} — one record per
release with \code{version}, \code{release_date}, \code{tables}, \code{total_rows}, \code{size_mb},
plus the archive-policy fields: \code{consolidated} (parquet kept indefinitely,
from \code{metadata/release_policy.yml}) and, when the version's parquet has been
removed by \code{scripts/thin_releases.R}, \code{retired} (\verb{\{retired_utc, to, reason\}}
read from its \code{retired.json}). Both the release notebook and the thinning
script call this, so a re-run of the release cannot drop the policy fields.
}
\concept{release}
