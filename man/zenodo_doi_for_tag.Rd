% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/citation.R
\name{zenodo_doi_for_tag}
\alias{zenodo_doi_for_tag}
\alias{zenodo_record_for_tag}
\title{Find the Zenodo record (and DOI) minted for a release tag}
\usage{
zenodo_doi_for_tag(
  tag,
  repo = "CalCOFI/workflows",
  concept_doi = CC_ZENODO_CONCEPT_DOI,
  fetch = NULL
)

zenodo_record_for_tag(json, tag, repo = "CalCOFI/workflows")
}
\arguments{
\item{tag}{the release tag, e.g. \code{"v2026.09.03"}}

\item{repo}{the GitHub repository}

\item{concept_doi}{the concept DOI whose versions are listed as a fallback}

\item{fetch}{the HTTP function (see \code{\link[=check_dataset_citation]{check_dataset_citation()}})}

\item{json}{a Zenodo records search response (text)}
}
\value{
A list — \code{doi}, \code{concept_doi}, \code{record_id}, \code{version}, \code{url},
\code{title} — or \code{NULL}.
}
\description{
Zenodo's GitHub integration archives this repository at each release tag and
records \verb{https://github.com/\{repo\}/tree/\{tag\}} as a related identifier.
\code{zenodo_doi_for_tag()} searches for that identifier, then falls back to the
concept's version listing matched on \code{metadata.version}. \code{NULL} when no
record exists yet (the DOI arrives minutes after the GitHub release).
}
\concept{release}
