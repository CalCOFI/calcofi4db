% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{dataset_distributions}
\alias{dataset_distributions}
\title{Every endpoint of one dataset, measured and curated}
\usage{
dataset_distributions(
  key,
  ds,
  objects,
  erddap = NULL,
  netcdf = NULL,
  curated = NULL,
  version = NULL,
  workflow_url = NULL
)
}
\arguments{
\item{key}{the \code{dataset_key}}

\item{ds}{the dataset's \code{metadata.json} block}

\item{objects}{the dataset's \code{objects[]} (from the builder)}

\item{erddap}{the table from \code{\link[=fetch_erddap_datasets]{fetch_erddap_datasets()}}, or NULL}

\item{netcdf}{the list from \code{\link[=fetch_netcdf_manifests]{fetch_netcdf_manifests()}}, or NULL}

\item{curated}{the \code{distribution.csv} rows for this key (tibble, may be empty)}

\item{version}{the release version (selects the netCDF entry)}

\item{workflow_url}{the ingest notebook URL}
}
\value{
A list of rows, each a named list (\code{kind}, \code{url}, …).
}
\description{
The \code{distributions[]} of a record (plan § D-1): the parquet objects that
belong to the dataset (its \verb{dataset_key=} partitions and the whole tables
attributed to it), the CF netCDF from its \code{manifests.json}, the ERDDAP ids
that exist on erddap.calcofi.io now (by \code{dataset_key} prefix, with the ISO
19115 record of the primary id), the ingest notebook, the calcofi.org page,
the source portal (\code{link_data_source}, classified by host) and the curated
rows of \code{metadata/distribution.csv} for the key — a legacy ERDDAP id is one
of those, listed with \code{legacy: true} and whether it is still \code{live}.
}
\concept{catalog}
