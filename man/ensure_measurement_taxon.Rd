% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{ensure_measurement_taxon}
\alias{ensure_measurement_taxon}
\title{Stage the \verb{_measurement_taxon} crosswalk in a connection}
\usage{
ensure_measurement_taxon(
  con,
  measurement_taxon = NULL,
  dataset_key = NULL,
  tbl = "_measurement_taxon"
)
}
\arguments{
\item{con}{a DuckDB connection}

\item{measurement_taxon}{the crosswalk data.frame (or NULL for an empty table)}

\item{dataset_key}{restrict to this dataset, so an ingest never stages another
dataset's rows}

\item{tbl}{target table name}
}
\value{
(invisibly) \code{tbl}
}
\description{
Materializes \code{metadata/measurement_taxon.csv} as the \verb{_measurement_taxon} table
an \code{obs}/\code{obs_attribute} projection INNER JOINs to split a taxon-bearing
measurement_type name (\code{sardine_eggs}, \code{phyllosoma_stage_3}) into
\verb{(taxon_key, canonical type, life_stage, bin_value)}.
}
\details{
Exported because a dataset's projection lives in its own ingest notebook, and
the derived \code{taxon_key} is the part you must not hand-roll: it is
\code{\link[=taxon_key_of]{taxon_key_of()}} over \code{worms_id}/\code{itis_id}, so a \code{'worms:' || worms_id} string
built inline silently mis-keys any ITIS-resolved taxon.
}
\concept{model}
