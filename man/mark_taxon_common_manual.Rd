% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_names.R
\name{mark_taxon_common_manual}
\alias{mark_taxon_common_manual}
\title{Tag the hand-picked rows of the registry as \code{source = "manual"}}
\usage{
mark_taxon_common_manual(cache_csv, verbose = TRUE)
}
\arguments{
\item{cache_csv}{path to the registry}

\item{verbose}{report how many rows were tagged}
}
\value{
the number of rows newly tagged \code{manual}, invisibly
}
\description{
The registry never carried a literal \code{"manual"}: every fetched row was stamped
\code{source = "worms"} whether the value was auto-filled or hand-picked. The two
are told apart by construction — \code{\link[=ensure_taxon_common]{ensure_taxon_common()}} only ever auto-fills
the one candidate WoRMS offered — so a filled row whose value is not that
single candidate was necessarily a human edit. This writes that
reconstruction into the registry once (it is idempotent), and
\code{\link[=ensure_taxon_common]{ensure_taxon_common()}} keeps the tag from then on. \code{\link[=apply_taxon_common]{apply_taxon_common()}}
reads it as rank 1 of the precedence.
}
\concept{taxa}
