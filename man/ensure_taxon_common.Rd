% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_names.R
\name{ensure_taxon_common}
\alias{ensure_taxon_common}
\title{Fetch and cache vernacular (common) names from WoRMS}
\usage{
ensure_taxon_common(
  taxa,
  cache_csv = NULL,
  refresh = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{taxa}{data frame with \code{taxon_key}, \code{worms_id} and \code{scientific_name}
(extra columns ignored). Rows with no \code{worms_id} are skipped — there is
nothing to ask WoRMS about.}

\item{cache_csv}{path to the registry. Required: this is pointless without a
place to record the choice.}

\item{refresh}{re-query taxa already cached (hand-picked names still survive).}

\item{sleep}{seconds between WoRMS calls.}

\item{verbose}{report progress and how many await a choice.}
}
\value{
the registry, invisibly.
}
\description{
\strong{This is where a multi-vernacular choice is made, and a human makes it.}
WoRMS returns English vernaculars as an unordered bag with no preferred-name
flag, so:
}
\details{
\itemize{
\item \strong{exactly one} English name — taken automatically, since there is no choice;
\item \strong{two or more} — \code{common_name} is left empty, every candidate is written to
\code{candidates_en}, and someone picks by editing the cell. Nothing is guessed,
and an unresolved taxon simply publishes no common name;
\item \strong{none} — recorded with \code{n_candidates_en = 0} so it is not re-queried.
}

A re-run never overwrites a non-empty \code{common_name}, so a hand-picked value is
permanent even under \code{refresh = TRUE}.
}
\examples{
\dontrun{
ensure_taxon_common(taxa, cache_csv = here("metadata/taxon_common.csv"))
}
}
\concept{taxa}
