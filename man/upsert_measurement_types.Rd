% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{upsert_measurement_types}
\alias{upsert_measurement_types}
\title{Replace a measurement type's definition while keeping its curated columns}
\usage{
upsert_measurement_types(
  d,
  new_types,
  preserve = c("valid_min", "valid_max", "valid_depth_min_m", "valid_depth_max_m")
)
}
\arguments{
\item{d}{the current registry (a data.frame, e.g. from
\code{\link[=read_measurement_type]{read_measurement_type()}})}

\item{new_types}{data.frame of definitions to upsert; needs \code{measurement_type}}

\item{preserve}{columns to carry forward from the existing row when
\code{new_types} does not supply a non-\code{NA} value. Defaults to the bound columns —
the ones an ingest never authors and a provider has agreed.}
}
\value{
The updated registry, sorted by \code{measurement_type}.
}
\description{
Ten ingests "register" their types by deleting the existing row and binding a
freshly-built literal in its place:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{d_meas_type |> filter(measurement_type != "euphausiid_abundance") |>
  bind_rows(euph_types)          # <- literal, no valid_min/valid_max
}\if{html}{\out{</div>}}

Every column the literal omits is destroyed on each re-run. That is how a
provider-agreed \code{valid_min} silently disappeared from \code{euphausiid_abundance}
and the four picoplankton types during the v2026.08.08 re-render: the ingests
had not changed, but a curated column had been added underneath them. Only
\code{ingest_calcofi_mets.qmd} did the preserve-and-merge dance by hand.

Use this instead of \code{filter(... != x) |> bind_rows(new)}. It replaces the
definition columns the ingest owns and carries the curated ones forward from
the row being replaced, so a re-run cannot quietly narrow the registry.
}
\examples{
\dontrun{
d_meas_type <- upsert_measurement_types(d_meas_type, euph_types)
readr::write_csv(d_meas_type, meas_type_csv, na = "")
}
}
\seealso{
\code{\link[=declare_measurement_bounds]{declare_measurement_bounds()}} to set a bound,
\code{\link[=register_measurement_types]{register_measurement_types()}} to append a genuinely new type.
}
\concept{registry}
