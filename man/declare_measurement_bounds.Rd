% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{declare_measurement_bounds}
\alias{declare_measurement_bounds}
\title{Declare \code{valid_min} / \code{valid_max} on measurement types that already exist}
\usage{
declare_measurement_bounds(bounds, path, overwrite = FALSE, quiet = FALSE)
}
\arguments{
\item{bounds}{data.frame with \code{measurement_type} and at least one of
\code{valid_min}, \code{valid_max}, \code{valid_depth_min_m}, \code{valid_depth_max_m}. \code{NA}
leaves that bound undeclared; supply only the side you can defend.}

\item{path}{path to \code{metadata/measurement_type.csv}}

\item{overwrite}{allow replacing a bound that is already declared (default
FALSE). A declared bound has been agreed with a provider, so changing it is
a deliberate act, not a side effect of re-running an ingest.}

\item{quiet}{suppress the summary message}
}
\value{
The full updated registry, invisibly if nothing changed.
}
\description{
\code{\link[=register_measurement_types]{register_measurement_types()}} only ever \emph{appends} — by design, so an ingest
cannot silently rewrite a type another dataset relies on. That leaves no way to
do the thing the bounds convention asks for most often: put a bound on a type
that is already registered without one. All 73 unbounded types at v2026.08.07
were in exactly that state, so "declare it with \code{register_measurement_types()}"
was advice that could not be followed.
}
\details{
This is the narrow, auditable counterpart: it changes \strong{only} the four bound
columns, only on rows that already exist, and it refuses an unknown
\code{measurement_type} rather than quietly adding one — a typo would otherwise
create a bound-carrying orphan that no observation ever matches.
}
\examples{
\dontrun{
declare_measurement_bounds(
  data.frame(measurement_type = "zooscan_abundance", valid_min = 0),
  here::here("metadata/measurement_type.csv"))
}
}
\seealso{
\code{\link[=check_measurement_bounds]{check_measurement_bounds()}}, which is what consumes these.
}
\concept{registry}
