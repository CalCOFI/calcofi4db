% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor_pair.R
\name{combine_sensor_pair}
\alias{combine_sensor_pair}
\alias{combine_sensor_pair_sql}
\title{Combine a CTD sensor pair by the provider's quality flags}
\usage{
combine_sensor_pair(v1, v2, q1 = NA, q2 = NA)

combine_sensor_pair_sql(v1, v2, q1, q2)
}
\arguments{
\item{v1, v2}{numeric: the two sensors' values (sensor 1 = primary).}

\item{q1, q2}{their quality codes (character or numeric; \code{NA} = good).}
}
\value{
\code{combine_sensor_pair()}: a numeric vector; \code{combine_sensor_pair_sql()}: a
length-one character SQL expression.
}
\description{
The rule, in precedence order:
\enumerate{
\item a sensor flagged \strong{8} (questionable) or \strong{9} (bad) is dropped;
\item a \strong{1} (use primary) on either sensor selects sensor 1 alone, a \strong{2} (use
secondary) selects sensor 2 alone — when both say the same thing; if they
disagree (one says 1, the other 2) neither is trusted over the other and the
mean is used;
\item otherwise the \strong{mean} of the two; one alone when the other is \code{NULL};
\code{NULL} when neither survives.
}
}
\details{
Flags are compared as the source writes them — \code{"8"}, \code{"8.0"}, \code{8}, \code{NA} and \code{""}
all mean what the vocabulary says — so a consumer can pass \code{measurement_qual}
straight from \code{obs}. \code{combine_sensor_pair_sql()} returns the same rule as one SQL
expression over four column names or expressions, for use in a DuckDB query
(the transect builder, the Explorer's SQL).
}
\examples{
combine_sensor_pair(c(10, 10, 10, 10, NA, 10), c(12, 12, 12, 12, 12, 12),
                    q1 = c(NA, "8", "1", "2", NA, "9"), q2 = c(NA, NA, NA, NA, NA, "9.0"))
# 11 10 10 12 12 NA
combine_sensor_pair_sql("salinity_1_corr", "salinity_2_corr", "q1", "q2")
}
\concept{ctd}
