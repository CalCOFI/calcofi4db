% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{format_bbox}
\alias{format_bbox}
\title{Format a Bounding Box as a Human-Readable Extent}
\usage{
format_bbox(lat_min, lat_max, lon_min, lon_max, digits = 1)
}
\arguments{
\item{lat_min, lat_max, lon_min, lon_max}{Bounds in decimal degrees. Any
non-finite value (\code{NA}, \code{NaN}, \verb{±Inf}) yields \code{NA_character_} — a partial
box is not a box.}

\item{digits}{Decimal places to show.}
}
\value{
Length-1 character, e.g. \code{"29.8–37.8°N, 126.5–117.3°W"}, or \code{NA}.
}
\description{
Renders a decimal-degree bounding box the way a data catalog writes one:
unsigned magnitudes carrying a hemisphere suffix, in geographic order
(south to north, west to east).
}
\details{
Geographic order is preserved rather than numeric order, so a western
longitude span reads \code{"126.5–117.3°W"} — west edge first — instead of the
signed \code{"-126.5 to -117.3"}. When a span crosses the equator or the prime
meridian the two ends carry their own suffix (\code{"3.2°S–12.7°N"}), because a
single trailing hemisphere would silently mislabel half the range.
}
\examples{
format_bbox(29.8, 37.8, -126.5, -117.3)
}
\concept{release}
