% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrity.R
\name{check_release_relationships}
\alias{check_release_relationships}
\title{Measure every declared primary and foreign key on the frozen tables}
\usage{
check_release_relationships(
  con,
  rels,
  path = NULL,
  version = NULL,
  halt = TRUE
)
}
\arguments{
\item{con}{DBI connection holding the frozen tables.}

\item{rels}{Path to a \code{relationships.json}, or the list it parses to
(\code{primary_keys}, \code{foreign_keys}).}

\item{path}{Optional path to write \code{integrity.json} to.}

\item{version}{Release version stamped into the sidecar (no wall clock: the
file is deterministic for unchanged inputs, like the release stamp).}

\item{halt}{\code{stop()} on any duplicate or \code{NULL} primary key, or any foreign-key
orphan (default \code{TRUE}). A skipped key never halts.}
}
\value{
Invisibly, a list: \code{primary_keys} and \code{foreign_keys} data frames,
\code{ok}, and \code{n_skipped}.
}
\description{
The release has always \emph{declared} its keys (\code{relationships.json}, from
\code{\link[=core_relationships]{core_relationships()}} and \code{metadata/relationships_cross.csv}) and gated a
few of them — \code{\link[=check_core_pk_unique]{check_core_pk_unique()}} on the core primary keys,
\code{\link[=check_cruise_key_integrity]{check_cruise_key_integrity()}} on the \code{cruise_key} edges — while the other
~50 declared foreign keys were true by construction and never measured. This
walks the whole declaration on the assembled tables and writes the result
as the \code{integrity.json} sidecar beside \code{catalog.json}, so a consumer (the
schema browser, the docs) can show "unique, measured" and "0 orphans" per
key instead of trusting the diagram.
}
\details{
For each primary key: rows, distinct key values, duplicates, rows with a
\code{NULL} in any key column. For each foreign key: rows, rows whose key is
\code{NULL} (permitted — a nullable edge is one where the source has nothing to
point at, e.g. \code{obs.taxon_key} on an env row), and \strong{orphans}: non-\code{NULL}
values with no match in the referenced column. A key whose table or column
is not in \code{con} is reported as \code{skipped}, never silently dropped.
}
\concept{validation}
