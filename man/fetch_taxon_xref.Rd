% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xref.R
\name{fetch_taxon_xref}
\alias{fetch_taxon_xref}
\title{Fetch (and cache) the WoRMS <-> ITIS cross-reference for a set of taxa}
\usage{
fetch_taxon_xref(
  itis_ids = integer(),
  worms_ids = integer(),
  names = character(),
  cache_csv = NULL,
  refresh = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{itis_ids}{integer ITIS TSNs to crosswalk (NA/duplicates dropped)}

\item{worms_ids}{integer WoRMS AphiaIDs to backfill an \code{itis_id} for}

\item{names}{character source names; cleaned with \code{\link[=clean_taxon_name]{clean_taxon_name()}} first}

\item{cache_csv}{path to the cross-reference cache CSV (\code{metadata/taxon_xref.csv});
read if it exists, rewritten when anything new is fetched. \code{NULL} fetches
everything and caches nothing.}

\item{refresh}{logical; re-fetch queries already cached (and re-date them)}

\item{sleep}{seconds between API calls (rate limit)}

\item{verbose}{logical; report what was cached vs fetched}
}
\value{
a data.frame of cross-reference rows for the requested queries
}
\description{
Resolves each requested identifier through the authority that can answer it,
and returns one row per query with the \strong{accepted} ids, the authority's real
\code{status}, and the date it was checked:
}
\details{
\itemize{
\item \code{itis_ids} — exact TSN -> AphiaID crosswalk via
\code{worrms::wm_record_by_external(type = "tsn")}, plus the ITIS-accepted TSN
via \code{taxize::itis_acceptname()}. This is where a bird gains its \code{worms_id}
without losing its \verb{itis:} key.
\item \code{worms_ids} — the reverse direction, \code{worrms::wm_external(type = "tsn")},
backfilling \code{itis_id} on WoRMS-keyed taxa.
\item \code{names} — \code{worrms::wm_records_name()} on \code{\link[=clean_taxon_name]{clean_taxon_name()}} output, the
fallback for taxa carrying neither id.
}

Queries already present in \code{cache_csv} are not re-fetched, so a re-run is free
and offline. \code{notes} accumulates datestamped lines and is never rewritten.
}
\concept{taxonomy}
