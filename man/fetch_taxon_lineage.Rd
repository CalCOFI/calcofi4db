% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{fetch_taxon_lineage}
\alias{fetch_taxon_lineage}
\title{Fetch (and cache) the WoRMS/ITIS lineage for a set of taxon ids}
\usage{
fetch_taxon_lineage(
  worms_ids = integer(),
  itis_ids = integer(),
  cache_csv = NULL,
  refresh = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{worms_ids}{integer vector of AphiaIDs to resolve (NA/duplicates dropped)}

\item{itis_ids}{integer vector of ITIS TSNs to resolve (the Aves-keyed taxa)}

\item{cache_csv}{path to the lineage cache CSV; read if it exists, rewritten
when anything new is fetched. \code{NULL} fetches everything and caches nothing.}

\item{refresh}{logical; re-fetch ids already cached}

\item{sleep}{seconds between API calls (rate limit)}

\item{verbose}{logical; report what was cached vs fetched}
}
\value{
a data.frame of lineage rows for the requested ids
}
\description{
One row per (requested taxon, ancestor-or-self), which is what both halves of
a usable hierarchy need: the ancestor rows so \code{parent_taxon_key} chains
resolve, and the ranks so \code{kingdom}/\code{phylum}/\code{class}/\code{order_taxon}/\code{family} can
be flattened onto each taxon.
}
\details{
Ids already present in \code{cache_csv} are not re-fetched, so a re-run is free and
offline. The cache is a reviewable registry like the others under \verb{metadata/};
it is written with \code{na = ""} (see the round-trip trap in the workflows
\code{CLAUDE.md} — \code{readr}'s default turns an empty cell into the two-character
string \code{"NA"}, which DuckDB then reads as data).
}
\concept{taxonomy}
