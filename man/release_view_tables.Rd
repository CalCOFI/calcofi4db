% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{release_view_tables}
\alias{release_view_tables}
\alias{substitute_view_tables}
\title{The tables a catalog view reads, and the SQL with them resolved}
\usage{
release_view_tables(sql)

substitute_view_tables(sql, rp = function(table) paste0("\\"", table, "\\""))
}
\arguments{
\item{sql}{a view's SQL carrying \code{{{table}}} tokens.}

\item{rp}{\verb{function(table) -> character(1)}; default quotes the name.}
}
\value{
\code{release_view_tables()}: the distinct table names, in order of first appearance;
\code{substitute_view_tables()}: the SQL with every token replaced.
}
\description{
A view in \code{catalog.json} names its source tables as \code{{{table}}} tokens so that the SQL is
storage-agnostic. \code{release_view_tables()} lists them; \code{substitute_view_tables()} replaces each
with \code{rp(table)} — a quoted identifier by default, or whatever the caller reads a table through.
}
\examples{
release_view_tables(obs_view_sql())
}
\concept{release}
