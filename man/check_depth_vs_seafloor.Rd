% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/depth.R
\name{check_depth_vs_seafloor}
\alias{check_depth_vs_seafloor}
\title{Find samples deeper than the seafloor at their position}
\usage{
check_depth_vs_seafloor(
  con,
  seafloor,
  sample_tbl = "sample",
  obs_tbl = "obs",
  tolerance_m = 10
)
}
\arguments{
\item{con}{DBI connection holding \code{sample_tbl} (with \code{parent_sample_key},
\code{root_sample_key}) and optionally \code{obs_tbl}.}

\item{seafloor}{Result of \code{\link[=sample_seafloor]{sample_seafloor()}} (or a GEBCO tif path, in which
case it is computed).}

\item{sample_tbl, obs_tbl}{Table names; \code{obs_tbl} may be absent.}

\item{tolerance_m}{Metres a sample may exceed the neighbourhood-deepest cell
before it is a finding (default 10).}
}
\value{
A tibble of violators — \code{sample_key}, \code{dataset_key}, \code{sample_type},
\code{cruise_key}, \code{longitude}, \code{latitude}, \code{depth_m}, \code{seafloor_depth_m},
\code{seafloor_max3x3_m}, \code{excess_m}, \code{on_land} — worst first, with attribute
\code{summary}: per-dataset \code{n_root}, \code{n_unknown}, \code{n_over} and \code{max_excess_m}.
}
\description{
For every root sample (no parent) takes the deepest depth attributed to it —
its own \code{depth_max_m}/\code{depth_min_m}, its descendants' and its observations' —
and compares it with the deepest GEBCO cell in the 3x3 neighbourhood of its
position plus \code{tolerance_m}. Positions outside the raster are \code{unknown}, not
violations.
}
\concept{validation}
