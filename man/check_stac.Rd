% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac.R
\name{check_stac}
\alias{check_stac}
\title{Validate a written STAC catalog}
\usage{
check_stac(dir, network = !nzchar(Sys.getenv("CALCOFI_SKIP_LINK_CHECK")))
}
\arguments{
\item{dir}{the directory \code{\link[=build_stac]{build_stac()}} wrote}

\item{network}{run the external validator (it fetches the STAC JSON schemas);
defaults to off when \code{CALCOFI_SKIP_LINK_CHECK} is set}
}
\value{
A \link[tibble:tibble]{tibble} \code{document}, \code{finding}, \code{level}, \code{detail} —
one \code{ok} row per document that passed.
}
\description{
Runs \code{stac-validator} (pip, \code{stac_validator}) over every written document when
it is on \code{PATH}, and always runs a structural check: that every document parses, carries \code{type}/\code{stac_version}/\code{id}, that
every \code{child}/\code{item} link resolves to a file that was written, and that every
asset has an \code{href} and a \code{type}. The structural half always runs, so a
machine without the validator still fails on a broken catalog.
}
\concept{catalog}
