% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{check_release_complete}
\alias{check_release_complete}
\title{Assert a frozen release is structurally complete}
\usage{
check_release_complete(
  version,
  bucket = "calcofi-db",
  required = RELEASE_REQUIRED_OBJECTS,
  halt = TRUE
)
}
\arguments{
\item{version}{release version (e.g. \code{"v2026.08.14"})}

\item{bucket}{GCS bucket holding \verb{ducklake/releases/}}

\item{required}{object names that must exist directly under the release}

\item{halt}{logical; \code{stop()} when something is missing (default \code{TRUE})}
}
\value{
invisibly, a data.frame with \code{object} and \code{exists}
}
\description{
Passing the consumer-contract suite says the \strong{data} is right. It says
nothing about whether the release is \strong{readable}, and those are different
questions — \code{test_release.qmd} asked only the first until 2026-08-14, when it
passed 28/28 against genuinely-good parquet and promoted \code{latest.txt} to a
release with no \code{catalog.json}. That is the file \code{cc_get_db()} opens, so every
consumer resolving through \code{latest} got a 404 while the tests were green.
}
\details{
The cause was ordering: \code{upload_frozen} pushes parquet first and the JSON
sidecars after, and the render died in between. The parquet was complete and
valid, which is precisely why the tests passed.
}
\concept{cloud}
