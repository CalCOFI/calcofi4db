% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{build_relationships_json}
\alias{build_relationships_json}
\title{Build Relationships JSON from dm Object}
\usage{
build_relationships_json(dm, output_dir, provider = NULL, dataset = NULL)
}
\arguments{
\item{dm}{A \code{dm} object with PKs and FKs defined}

\item{output_dir}{Directory to write \code{relationships.json}}

\item{provider}{Data provider identifier (e.g. "swfsc")}

\item{dataset}{Dataset identifier (e.g. "ichthyo")}
}
\value{
Path to the created \code{relationships.json} file
}
\description{
Extracts primary keys and foreign keys from a \code{dm} object and writes
a \code{relationships.json} sidecar file alongside parquet outputs. Since
parquet files cannot store table relationships natively, this provides a
machine-readable source of truth for PKs and FKs.
}
\examples{
\dontrun{
dm_all <- dm::dm_from_con(con, learn_keys = FALSE) |>
  add_cc_keys()
build_relationships_json(
  dm         = dm_all,
  output_dir = "data/parquet/swfsc_ichthyo",
  provider   = "swfsc",
  dataset    = "ichthyo")
}
}
\concept{wrangle}
