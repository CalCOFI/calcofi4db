% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calcofi_coords.R
\name{cc_calcofi_to_lonlat}
\alias{cc_calcofi_to_lonlat}
\title{Convert CalCOFI line/station to longitude/latitude}
\usage{
cc_calcofi_to_lonlat(line, station)
}
\arguments{
\item{line, station}{numeric vectors of CalCOFI line and station, recycled to a
common length. \code{NA} in either yields \code{NA} in both outputs.}
}
\value{
a data.frame with \code{longitude} and \code{latitude} (WGS 84), one row per input
}
\description{
The CalCOFI station plan is a coordinate system in its own right, and PROJ
ships it as \code{+proj=calcofi} — so this is a projection, not a lookup against
\code{grid}. That distinction matters: a lookup only resolves stations that exist
in the grid table, while the transform resolves \strong{any} line/station pair,
including the historical inshore stations and the Gulf of California and Baja
lines that the modern pattern dropped.
}
\details{
Use it to recover a position for a row that records where it was in CalCOFI
terms but carries no lon/lat. Once a position exists, \code{hex_id} and \code{grid_key}
follow from it in the usual way (\code{.hex_expr()}, \code{assign_grid_key()}), so a
recovered row becomes a full participant in spatial rollups rather than an
ungridded remainder.
}
\examples{
\dontrun{
cc_calcofi_to_lonlat(90, 60)   # -119.96, 32.42 — off San Diego
}
}
\concept{spatial}
