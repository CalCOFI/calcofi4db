% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lineage.R
\name{ensure_taxon_lineage}
\alias{ensure_taxon_lineage}
\title{Materialize the WoRMS/ITIS lineage \code{build_taxon_reference()} reads}
\usage{
ensure_taxon_lineage(
  con,
  measurement_taxon = NULL,
  overrides = NULL,
  cache_csv = NULL,
  tbl = "taxon",
  refresh = FALSE,
  sleep = 0.3,
  verbose = TRUE,
  xref_cache_csv = .xref_csv_beside(cache_csv)
)
}
\arguments{
\item{con}{a DuckDB connection holding this dataset's taxon vocabulary tables}

\item{measurement_taxon}{the composite crosswalk (\code{metadata/measurement_taxon.csv}),
already filtered to this dataset}

\item{overrides}{the manual id registry (\code{metadata/taxon_override.csv})}

\item{cache_csv}{path to the shared lineage cache
(\code{metadata/taxon_lineage.csv})}

\item{tbl}{hierarchy table to write (default \code{"taxon"} — the name
\code{\link[=build_taxon_reference]{build_taxon_reference()}} reads)}

\item{refresh}{logical; re-fetch ids already cached}

\item{sleep}{seconds between API calls (rate limit)}

\item{verbose}{logical; report what was cached vs fetched}

\item{xref_cache_csv}{path to the cross-reference cache
(\code{\link[=fetch_taxon_xref]{fetch_taxon_xref()}}), used to top up \verb{_taxon_xref} for the lineage
ANCESTORS discovered here — \code{\link[=ensure_taxon_xref]{ensure_taxon_xref()}} runs first and can only
see the dataset's own vocabulary. Defaults to \code{taxon_xref.csv} sitting
beside \code{cache_csv}, which is the layout every ingest uses; \code{NULL} skips it.}
}
\value{
(invisibly) a list with \code{n_ids}, \code{n_rows} and \code{n_unresolved}
}
\description{
Resolves every authority id this dataset's vocabulary reaches — from its own
taxon tables \emph{and} from \code{measurement_taxon.csv}, which is where the taxa that
had no lineage at all came from — fetches their classification (cached), and
writes it into \code{con} as the DwC-shaped \code{taxon} hierarchy table.
}
\details{
Call it \strong{before} \code{\link[=build_taxon_reference]{build_taxon_reference()}}, which reads that table as its
rank / parent / classification authority. An existing hierarchy is merged, not
replaced, so \code{swfsc_ichthyo} (which builds its own via
\code{\link[=build_taxon_hierarchy]{build_taxon_hierarchy()}}) keeps what it has and gains only what is missing.
}
\concept{taxonomy}
