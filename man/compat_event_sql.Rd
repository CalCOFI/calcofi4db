% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{compat_event_sql}
\alias{compat_event_sql}
\title{Rebuild a per-dataset event table as a VIEW over the core \code{sample}}
\usage{
compat_event_sql(
  dataset_key,
  sample_type,
  id_col,
  parent_col = NULL,
  cols = character(),
  measures = character(),
  sample_tbl = "sample"
)
}
\arguments{
\item{dataset_key}{provider_dataset the rows carry}

\item{sample_type}{the \code{sample_type} the rows carry (\code{site}, \code{tow}, \code{net}, …)}

\item{id_col}{name for the recovered source id column}

\item{parent_col}{optional name for the recovered parent FK column}

\item{cols}{named character vector \verb{c(<out name> = <sample column>)} of
straight passthrough columns}

\item{measures}{named character vector \verb{c(<out name> = <measurement_type>)} of
effort columns to pivot back out of \code{sample_measurement}}

\item{sample_tbl}{name of the core \code{sample} table to read}
}
\value{
a SQL SELECT string
}
\description{
The source id is recovered from the namespaced \code{sample_key}
(\code{'<dataset_key>:<sample_type>:<id>'} -> field 3), the containment FK from
\code{parent_sample_key}, and the event-level effort columns by pivoting
\code{sample_measurement} back out of long form.
}
\details{
Exported because each dataset's compat VIEWs are declared in its own ingest
notebook — this is the reusable \emph{shape}, not a per-dataset projection.
}
\examples{
compat_event_sql("swfsc_ichthyo", "tow", "tow_uuid", "site_uuid",
                 c(tow_type_key = "tow_type", datetime_start_utc = "datetime"))
}
\concept{model}
