% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{read_catalog_registries}
\alias{read_catalog_registries}
\title{Read every registry the dataset catalog joins}
\usage{
read_catalog_registries(metadata_dir)
}
\arguments{
\item{metadata_dir}{the \code{workflows/metadata} directory}
}
\value{
A list: \code{category}, \code{provider}, \code{license}, \code{dataset_status},
\code{distribution}, \code{portal} (tibbles), \code{sidecars} (named list),
\code{questions(dataset_key)} (a function returning the open/proposed rows on
\code{related_table = dataset}, or NULL) and \code{metadata_dir}.
}
\description{
One call for \code{category.csv}, \code{provider.csv}, \code{license.csv},
\code{dataset_status.csv}, \code{distribution.csv}, \code{portal.csv}, the descriptive
sidecars and a reader for each dataset's \code{questions.csv} — validated as they
are read, so an unregistered value fails here rather than in a page.
}
\concept{catalog}
