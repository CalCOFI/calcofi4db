% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_cast_base}
\alias{qc_cast_base}
\title{Strip the direction suffix from a CTD cast \code{sample_key}}
\usage{
qc_cast_base(sample_key)
}
\arguments{
\item{sample_key}{character vector of cast keys}
}
\value{
the keys with a single trailing \code{d}/\code{u} removed; keys without one are
returned unchanged
}
\description{
CTD \code{sample_key}s end in the cast direction — \verb{…:cast:9802_008d} /
\verb{…:cast:9802_008u} — so a station occupation that logged both directions is two
\code{sample} rows sharing a base.
}
\details{
THE OBVIOUS IMPLEMENTATION IS WRONG. \code{sub("d$", "", x)} is fine, but
\code{gsub("d", "", x)} or \code{replace(x, 'd', '')} also eats the \code{d} in the
\code{calcofi_ctd-cast} prefix and silently returns a key that matches nothing. The
same trap is called out in \code{ctd_updown_disagreement.sql}, which is where it was
first hit.
}
\examples{
qc_cast_base("calcofi_ctd-cast:cast:9802_008d")
}
\concept{qc}
