% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/questions.R
\name{read_questions}
\alias{read_questions}
\title{Read a dataset's \code{questions.csv}, validated and ranked}
\usage{
read_questions(path, validate = TRUE)
}
\arguments{
\item{path}{path to a \code{questions.csv}}

\item{validate}{error on an unknown \code{status}/\code{priority}, a duplicate \code{label},
or a \code{label} that disagrees with \code{id} (default TRUE)}
}
\value{
A \link[tibble:tibble]{tibble}, all columns character, ranked.
}
\description{
The single reader for the provider-question registry. Reads strictly
(\code{na = ""}, everything character, so a date or an id like \code{01} is never
silently retyped), checks the controlled vocabulary, and returns the questions
ranked \code{blocker} → \code{low} then by \code{label}.
}
\details{
Two identifiers, deliberately:
\itemize{
\item \strong{\code{id}} — \verb{\{provider\}_\{dataset\}_\{nn\}}, globally unique and durable. This is
what a cross-dataset reference or an issue tracker cites.
\item \strong{\code{label}} — the short form (\code{Q15}), unique \emph{within} the dataset. This is
what prose in a notebook says, and what the rendered table shows first, so
"see Q15" resolves for a reader.
}
}
\examples{
\dontrun{
read_questions("metadata/calcofi/ctd-cast/questions.csv")
}
}
\concept{registry}
