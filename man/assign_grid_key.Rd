% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{assign_grid_key}
\alias{assign_grid_key}
\title{Assign Grid Key via Spatial Join}
\usage{
assign_grid_key(con, table, geom_col = "geom", grid_table = "grid")
}
\arguments{
\item{con}{DBI connection to DuckDB}

\item{table}{Character. Table name to update.}

\item{geom_col}{Character. Geometry column in \code{table} (default: "geom").}

\item{grid_table}{Character. Grid table name (default: "grid").}
}
\value{
Data frame with columns \code{status} (in_grid / not_in_grid) and \code{n}.
}
\description{
Uses DuckDB spatial SQL to add a \code{grid_key} column to a table by
intersecting its geometry with a grid table. Returns a summary of
how many rows fell inside vs outside the grid.
}
\examples{
\dontrun{
grid_stats <- assign_grid_key(con, "casts")
grid_stats |> datatable()
}
}
\concept{spatial}
