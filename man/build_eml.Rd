% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eml.R
\name{build_eml}
\alias{build_eml}
\title{Build one dataset's EML 2.2 document from the catalog record}
\usage{
build_eml(
  record,
  sidecar = NULL,
  meta = NULL,
  coverage = NULL,
  release = NULL,
  gear = NULL
)
}
\arguments{
\item{record}{one dataset record (an element of \code{datasets.json}'s \code{datasets[]})}

\item{sidecar}{the dataset's \code{dataset_meta.yml} as a list (the narrative
fields — \code{methods_md}, \code{study_extent}, \code{sampling_description},
\code{quality_control_md}, \code{maintenance}, \code{associated_parties} — live only there)}

\item{meta}{the release \code{metadata.json} (path or parsed list), for \verb{columns\{\}}}

\item{coverage}{the release \code{coverage.json} (path or parsed list), for \code{taxa[]}}

\item{release}{the \code{release} block of \code{datasets.json} (version, date, citation);
defaults to the record's own \code{release} element when it carries one}

\item{gear}{the tibble from \code{\link[=read_gear_registry]{read_gear_registry()}}, or NULL}
}
\value{
A named list ready for \code{EML::write_eml()}, carrying an
\code{"eml_notes"} attribute (which fallbacks were used) that \code{\link[=check_eml]{check_eml()}} reads.
}
\description{
Plan § D-8: \verb{eml/\{dataset_key\}.xml} in every release, generated from the
record and the descriptive sidecar so the DwC-A, the EDI package, ERDDAP's
globals and the page's JSON-LD all read one document. The mapping, field by
field:
}
\details{
\tabular{ll}{
   EML \tab from \cr
   \code{packageId} / \code{system} \tab \verb{\{dataset_key\}.\{release version\}} / \code{calcofi.io} \cr
   \code{dataset/alternateIdentifier} \tab \code{attribution.doi} (as a DOI URL) and \code{links.page} \cr
   \code{dataset/shortName} · \code{title} \tab \code{dataset_name_short} · \code{dataset_name} \cr
   \code{dataset/creator} \tab \code{attribution.creators[]}, else \code{pi_names} with the provider organization, else the provider organization alone \cr
   \code{dataset/pubDate} \tab the release \code{release_date} \cr
   \code{dataset/language} \tab \code{eng} \cr
   \code{dataset/abstract} \tab \code{description_md}, rendered to paragraphs \cr
   \code{dataset/keywordSet} \tab \code{keywords} — the GCMD terms under their thesaurus, plus the category and the observed variable names \cr
   \code{dataset/intellectualRights} · \code{licensed} \tab \code{attribution.license} / \code{license_name} / \code{license_url} (\code{metadata/license.csv}) \cr
   \code{dataset/distribution} \tab \code{links.page} \cr
   \code{dataset/coverage} \tab geographic from \code{coverage.bbox} + \code{coverage.spatial}; temporal from \code{coverage.year_min/max}; taxonomic from \code{coverage.json}'s \code{taxa[]} for this dataset (WoRMS / ITIS ids) \cr
   \code{dataset/maintenance} \tab the sidecar's \code{maintenance} \cr
   \code{dataset/contact} \tab \code{attribution.contact}, else a creator email, else \code{\link[=eml_contact_address]{eml_contact_address()}} \cr
   \code{dataset/methods} \tab the sidecar's \code{methods_md}, \code{quality_control_md}, \code{study_extent}, \code{sampling_description}, with \code{metadata/gear.csv}'s \code{dwc_samplingProtocol} sentences for the dataset's \code{tow_type}s \cr
   \code{dataset/project} \tab \code{dataset_name} + \code{attribution.funding} (else \code{acknowledgement}), personnel from the creators \cr
   \code{dataset/dataTable[]} \tab the record's \code{tables[]}, \code{attributeList} from \code{metadata.json}'s \verb{columns\{\}} (label, definition, units, storage type), \code{physical} from the record's \code{objects[]} \cr
   \code{additionalMetadata} \tab the release citation, the dataset citation and the record's own provenance \cr
}


Absent optional fields are omitted; a missing \strong{required} field is a
\code{\link[=check_eml]{check_eml()}} finding, never a placeholder.
}
\seealso{
\code{\link[=check_eml]{check_eml()}}, \code{\link[=write_eml_files]{write_eml_files()}}
}
\concept{eml}
