% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{build_coverage_stations}
\alias{build_coverage_stations}
\title{The per-station coverage card: n obs by dataset x year and by dataset x month, for one station}
\usage{
build_coverage_stations(con, version)
}
\arguments{
\item{con}{DuckDB connection holding \code{obs} (with \code{dataset_key}, \code{grid_key}, \code{datetime}, \code{taxon_key},
\code{life_stage}) and \code{sample_root}; \code{taxon} (for \code{taxa[]}) and \code{measurement_type} (for the two
variable fields) when present.}

\item{version}{the release version string.}
}
\value{
A list \verb{\{version, stations: [\{grid_key, datasets: [\{dataset_key, n_obs, year_min, year_max, years, months\}]\}]\}}.
}
\description{
What db-viz-station draws when a station is clicked — every dataset sampled there, its rows per
year (\code{years}: \verb{[[year, n], …]}) and per month (\code{months}: twelve counts) — for all 218 stations.
Kept out of \code{coverage.json} so the first paint stays small; fetched when a station is selected.
}
\concept{release}
