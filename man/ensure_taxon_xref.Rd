% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xref.R
\name{ensure_taxon_xref}
\alias{ensure_taxon_xref}
\title{Materialize the authority cross-reference \code{.taxon_norm_sources()} reads}
\usage{
ensure_taxon_xref(
  con,
  measurement_taxon = NULL,
  overrides = NULL,
  cache_csv = NULL,
  tbl = "_taxon_xref",
  refresh = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{con}{a DuckDB connection holding this dataset's taxon vocabulary tables}

\item{measurement_taxon}{the composite crosswalk
(\code{metadata/measurement_taxon.csv}), already filtered to this dataset}

\item{overrides}{the manual id registry (\code{metadata/taxon_override.csv})}

\item{cache_csv}{path to the shared cross-reference cache
(\code{metadata/taxon_xref.csv})}

\item{tbl}{staging table to write (default \code{"_taxon_xref"})}

\item{refresh}{logical; re-fetch queries already cached (and re-date them)}

\item{sleep}{seconds between API calls (rate limit)}

\item{verbose}{logical; report what was cached vs fetched}
}
\value{
(invisibly) a list with \code{n_queries}, \code{n_resolved}, \code{n_rekeyed}
}
\description{
Works out which identifiers this dataset's vocabulary reaches, resolves them
(cached), and stages the result in \code{con} as \verb{_taxon_xref}. Every taxon builder
then picks it up automatically: \code{worms_id} is filled on ITIS-keyed taxa,
\code{itis_id} on WoRMS-keyed ones, deprecated ids are replaced by their accepted
form, and \code{taxonomic_status} / \code{status_checked} / \code{notes} are carried through.
}
\details{
Call it \strong{before} \code{\link[=ensure_taxon_lineage]{ensure_taxon_lineage()}} and the three builders — the
lineage fetch should ask about the accepted id, not the deprecated one.
}
\concept{taxonomy}
