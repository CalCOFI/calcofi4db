% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{export_release_parquet}
\alias{export_release_parquet}
\title{Deterministic parquet export of one released table}
\usage{
export_release_parquet(
  con,
  table,
  path,
  order_by,
  partition_by = NULL,
  writer = CC_PARQUET_WRITER,
  strip_provenance = TRUE
)
}
\arguments{
\item{con}{DuckDB connection.}

\item{table}{table (or view) name.}

\item{path}{output file (single) or directory (partitioned).}

\item{order_by}{character vector of ORDER BY terms (may carry \verb{NULLS LAST}).}

\item{partition_by}{optional partition column (Hive layout, one file per value).}

\item{writer}{list of writer options; see \link{CC_PARQUET_WRITER}.}

\item{strip_provenance}{drop \verb{_source_*} / \verb{_ingested_at} style columns.}
}
\value{
Invisibly, a tibble of files written: \code{rel_path}, \code{bytes}.
}
\description{
Writes \verb{SELECT <non-provenance cols> FROM table ORDER BY <order_by>} with the
pinned writer options (\link{CC_PARQUET_WRITER}) and a single writer thread, so the
same rows always produce the same bytes. Refuses to write if \code{order_by} is not
a unique key of the table.
}
\concept{release}
