% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_absence_rule}
\alias{dwc_absence_rule}
\title{Which \code{occurrenceStatus} rule a dataset falls under}
\usage{
dwc_absence_rule(con, dataset_key)
}
\arguments{
\item{con}{a DBI connection to the release}

\item{dataset_key}{the dataset}
}
\value{
\code{"zeros_recorded"} or \code{"positive_only"}.
}
\description{
Measured from the data, never declared:
}
\details{
\itemize{
\item \code{"zeros_recorded"} — the dataset has zero-valued \code{obs_bio} rows, so a sample
that was examined and held none of a taxon is already in the release. Those
rows become \code{occurrenceStatus = "absent"} and nothing is derived.
\item \code{"positive_only"} — the dataset has no zero rows: a surveyed-empty sample
simply has no row. \code{occurrenceStatus} is \code{"present"} for every row it does
have, and an absence exists only if the protocol sorted every sample for the
whole vocabulary — a claim about the protocol that the release cannot make.
\code{\link[=dwc_occurrence]{dwc_occurrence()}} therefore derives absences ONLY when asked
(\code{absences = "sample_root"}).
}
}
\concept{dwc}
