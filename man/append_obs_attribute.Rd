% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{append_obs_attribute}
\alias{append_obs_attribute}
\title{Append sub-occurrence attribute rows into the core \code{obs_attribute} table}
\usage{
append_obs_attribute(con, select_sql, tbl = "obs_attribute")
}
\arguments{
\item{con}{a DuckDB connection (open via \code{\link[=get_duckdb_con]{get_duckdb_con()}})}

\item{select_sql}{a SELECT producing the canonical \code{obs} columns by name}

\item{tbl}{target table (default \code{"obs_attribute"})}
}
\value{
(invisibly) the total row count of \code{tbl} after the append
}
\description{
Generalizes the former \code{obs_freq}: holds any within-occurrence attribution —
length-frequency, stage-frequency, and categorical breakdowns like seabird
behavior. \code{select_sql} must yield \code{dataset_key}, \code{sample_key}, \code{taxon_key},
\code{life_stage}, \code{measurement_type} (the attribute, e.g. \code{body_length}/\code{stage}/
\code{behavior}), \code{bin_value} (numeric bin / stage no.), \code{bin_label} (category
label), \code{count}, \code{measurement_qual} by name.
}
\concept{model}
