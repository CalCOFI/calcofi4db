% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{build_sample_reference}
\alias{build_sample_reference}
\title{Build the shared \code{sample} event dimension from the per-dataset event tables}
\usage{
build_sample_reference(con, sample_tbl = "sample", datasets = NULL)
}
\arguments{
\item{con}{a DuckDB connection with the per-dataset event tables loaded}

\item{sample_tbl}{target table name (default \code{"sample"})}

\item{datasets}{optional character vector of \code{dataset_key}s to restrict which
arms build (default \code{NULL} = every dataset whose event tables are present).
Use in an ingest that has other datasets' event tables loaded as references
so only this dataset's \code{sample} rows are built.}
}
\value{
(invisibly) the row count of the built \code{sample} table
}
\description{
Materializes the adjacency-list \code{sample} dimension (one row per physical
sampling event, at its native grain) from whichever per-dataset event tables are
present in \code{con} — subsuming \code{site}/\code{tow}/\code{net}/\code{casts}/\code{ctd_cast}/\code{dic_sample}/
\code{cufes_sample}/\verb{*_tow}/\verb{*_sample}/\code{bird_mammal_transect}/\code{phyto_sample} into one
table. Every \code{sample_key} is namespaced \code{"<dataset_key>:<sample_type>:<id>"};
\code{parent_sample_key}/\code{root_sample_key} encode the \code{site->tow->net} and
\code{cast->bottle} hierarchies (a flat adjacency list with no attribute inheritance).
\code{geom} is minted from \code{latitude}/\code{longitude}. Only arms whose source tables exist
are included. Errors if the resulting \code{sample_key} is not unique.
}
\concept{model}
