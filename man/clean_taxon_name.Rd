% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xref.R
\name{clean_taxon_name}
\alias{clean_taxon_name}
\title{Normalize a source taxon name for an authority lookup}
\usage{
clean_taxon_name(x)
}
\arguments{
\item{x}{character vector of source names}
}
\value{
character vector of cleaned names (NA in, NA out)
}
\description{
Strips the open-nomenclature and qualifier noise that source spreadsheets
carry in their column headers and species lists, so the name reaches WoRMS in
a form it can match. \code{"Bathophilus sp."} becomes \code{"Bathophilus"} (WoRMS holds
the genus, never the \code{sp.} form), \code{"Phaeocystis cf pouchetti"} becomes
\code{"Phaeocystis pouchetti"}, \code{"indistinguished Pterosperma spp."} becomes
\code{"Pterosperma"}.
}
\details{
\strong{Use the result as the lookup query only — never as \code{ds_taxa_code}.} For
\code{sio_mesopelagic-fish} the local code \emph{is} the verbatim column header and is
the join key from \code{obs}; rewriting it would silently orphan every observation.

This generalizes the hand-maintained \code{name_query} column that
\code{metadata/calcofi/phytoplankton/taxon_worms.csv} already carries for one
dataset.
}
\examples{
clean_taxon_name(c("Bathophilus sp.", "Phaeocystis cf pouchetti",
                   "indistinguished Pterosperma spp.", "Uria aalge"))
}
\concept{taxonomy}
