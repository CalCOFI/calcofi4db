% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_upload.R
\name{read_sbe_btl}
\alias{read_sbe_btl}
\title{Read a Sea-Bird \code{.btl} bottle summary}
\usage{
read_sbe_btl(path, statistic = "avg")
}
\arguments{
\item{path}{file path}

\item{statistic}{tag to keep; \code{"avg"} is the only complete layout}
}
\value{
a tibble with a \code{sbe_header} attribute
}
\description{
Each bottle contributes several rows tagged \code{(avg)} / \code{(sdev)} / \code{(min)} /
\code{(max)}. Only \code{(avg)} is read, for two reasons: the others describe the scatter
of the scans the bottle was fired over rather than separate observations, and
structurally they are not the same table — the \code{(sdev)} row omits the bottle
number and carries a time where the \code{(avg)} row carries a date, so it does not
share the column layout.
}
\concept{upload}
