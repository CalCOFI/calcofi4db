% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eml.R
\name{eml_findings}
\alias{eml_findings}
\title{The findings \code{\link[=check_eml]{check_eml()}} can report, with their level}
\usage{
eml_findings()
}
\value{
A named character vector, finding -> level.
}
\description{
\code{error} findings fail the release unless exempt (an open/proposed
\code{questions.csv} row on \code{related_table = dataset} naming the field, or naming
none, covers it — the same rule \code{\link[=check_dataset_catalog]{check_dataset_catalog()}} applies to
\code{no_citation}); \code{warn} findings never block but are always printed, because
each one names a real gap in the record.
}
\details{
\itemize{
\item \code{invalid_eml} — \code{EML::eml_validate()} rejected the written document.
\item \code{no_title}, \code{no_abstract}, \code{no_pub_date}, \code{no_geographic_coverage},
\code{no_temporal_coverage}, \code{no_data_table} — an element EDI's EML checklist
requires that the record could not supply.
\item \code{no_creator} — no \code{creators[]}, no \code{pi_names} and no registered provider.
\item \code{no_license} — no \code{license} on the record (exempt while a licence question is open).
\item \code{short_abstract} — under 20 words (EDI's guidance; the record's own text, not a stub).
\item \code{creator_from_provider} — the creator is the provider organization, because
the record names no person.
\item \code{contact_role_address} — the contact is \code{\link[=eml_contact_address]{eml_contact_address()}}, because no
provider address is on record.
\item \code{creator_no_organization}, \code{no_keywords}, \code{no_methods},
\code{no_taxonomic_coverage}, \code{undocumented_attributes}, \code{custom_units} — a gap
that weakens the document without invalidating it.
}
}
\concept{eml}
