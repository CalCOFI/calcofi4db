% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{integrate_to_working_ducklake}
\alias{integrate_to_working_ducklake}
\title{Integrate Ingest Outputs into Working DuckLake}
\usage{
integrate_to_working_ducklake(
  ingests,
  gcs_bucket = "calcofi-db",
  ducklake_path = "ducklake/working"
)
}
\arguments{
\item{ingests}{List of ingest result lists (from \code{write_ingest_outputs()})
or a tibble from \code{list_ingest_outputs()}}

\item{gcs_bucket}{GCS bucket (default: "calcofi-db")}

\item{ducklake_path}{Path to Working DuckLake within bucket
(default: "ducklake/working")}
}
\value{
List with DuckLake path and table info
}
\description{
Reads parquet files from multiple ingest outputs and integrates them into
the Working DuckLake. Tables from different ingests with the same name are
combined (e.g., cruise tables from different sources). Provenance columns
(\verb{_ingest_provider}, \verb{_ingest_dataset}) are added to track origin.
}
\examples{
\dontrun{
# from targets pipeline
result <- integrate_to_working_ducklake(
  ingests = list(ingest_swfsc, ingest_bottle))

# from existing GCS ingests
existing <- list_ingest_outputs()
result <- integrate_to_working_ducklake(existing)
}
}
\concept{workflow}
