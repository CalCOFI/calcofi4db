% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{publish_table_signatures}
\alias{publish_table_signatures}
\title{Per-dataset row signatures of tables already in a connection}
\usage{
publish_table_signatures(con, tables, via = list())
}
\arguments{
\item{con}{a DuckDB connection holding the tables}

\item{tables}{table (or view) names}

\item{via}{named list, table -> \code{c(from, key)}: sign \code{table}'s rows joined to the
distinct (\code{dataset_key}, \code{key}) pairs of table \code{from}, e.g.
\code{list(taxon = c("obs_bio", "taxon_key"), cruise = c("sample", "cruise_key"))}}
}
\value{
The data frame \code{\link[=publish_object_signatures]{publish_object_signatures()}} returns, \code{content_hash} empty.
}
\description{
The local twin of \code{\link[=publish_object_signatures]{publish_object_signatures()}}, for a publisher that materializes
the core before building (the OBIS and netCDF publishers do, to join across tables):
each table's rows grouped by \code{dataset_key} and signed. A table with no \code{dataset_key}
column is signed per dataset through \code{via} — the rows a dataset actually reaches by
a key — so a vocabulary row another dataset added does not rebuild this one; without
a \code{via} it is signed whole (\code{dataset_key = "*"}). A table absent from the connection
contributes nothing, which \code{\link[=publish_data_parts]{publish_data_parts()}} reports as \code{"<absent>"}.
}
\concept{publish}
