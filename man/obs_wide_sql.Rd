% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{obs_wide_sql}
\alias{obs_wide_sql}
\title{SQL that widens long \code{obs} rows into one column per measurement type}
\usage{
obs_wide_sql(
  dataset_key,
  measurement_types,
  obs_tbl = "obs",
  grain = c("sample_key", "depth_min_m", "taxon_key", "life_stage"),
  carry = character(),
  order_by = grain,
  count_col = NULL,
  value_col = "measurement_value"
)
}
\arguments{
\item{dataset_key}{Dataset provenance stamp.}

\item{measurement_types}{Character vector of types to widen into columns,
normally \code{plan_dataset_netcdf()$measurement_types} (the union across all
partitions).}

\item{obs_tbl}{Table or view to read.}

\item{grain}{Columns defining one output row. \strong{The default includes
\code{taxon_key} and \code{life_stage} deliberately} — see Details.}

\item{carry}{Columns functionally determined by \code{sample_key} (position, time,
\code{cruise_key}, \code{grid_key}) to carry through with \code{any_value()}.}

\item{order_by}{Columns for the \verb{ORDER BY}; defaults to \code{grain}. A contiguous
ragged array requires the instance column to sort first.}

\item{count_col}{Optional name for a \verb{COUNT(*)} column, so the caller can see
how many long rows collapsed into each wide row and assert that none were
silently dropped.}

\item{value_col}{Column holding the measured value.}
}
\value{
A single SQL string.
}
\description{
The database keeps every quantity in a single \code{measurement_value} column, which
CF forbids — a variable has one unit and one \code{standard_name}. This builds the
pivot, at the \strong{occurrence grain} rather than the event grain.
}
\details{
\strong{Why the grain includes \code{taxon_key}.} For a biological dataset \code{obs} is one
row per (event, taxon, life stage, measurement). Grouping by \code{sample_key}
alone therefore collapses every taxon in a sample into one row: on
\code{cce-lter_zooscan} that turns 34,109 occurrences over 23 taxa into 1,483 rows
and loses 96\% of the data, with \code{MAX()} silently choosing one taxon's value.
The loss is invisible in the output — the file is well-formed and the
variables have plausible values.

A \code{measurement_type} outside \verb{[A-Za-z0-9_]}, or colliding with a coordinate
name the writers create, is rejected rather than quoted: both are also invalid
or ambiguous as netCDF variable names.
}
\examples{
obs_wide_sql("cce-lter_zooscan", c("abundance", "biomass"),
             carry = c("latitude", "longitude"))
}
\concept{publish}
