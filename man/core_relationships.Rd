% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{core_relationships}
\alias{core_relationships}
\title{PK/FK spec for the consolidated core tables}
\usage{
core_relationships(tables)
}
\arguments{
\item{tables}{character vector of tables the ingest writes (typically the
result of \code{\link[=core_output_tables]{core_output_tables()}})}
}
\value{
a list with \code{primary_keys} and \code{foreign_keys}
}
\description{
Every ingest now emits the same core shape, so every ingest declares the same
relationships. This returns that one spec in \code{\link[=build_relationships_json]{build_relationships_json()}}'s
\code{rels} form, restricted to the tables actually present in \code{tables} — so an
ingest that emits no \code{obs_attribute} does not advertise an edge to it.
}
\concept{model}
