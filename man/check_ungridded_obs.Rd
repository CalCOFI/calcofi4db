% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_ungridded_obs}
\alias{check_ungridded_obs}
\title{Observations that resolve no CalCOFI grid cell — reported, never dropped}
\usage{
check_ungridded_obs(con, obs_tbl = "obs", verbose = TRUE)
}
\arguments{
\item{con}{a DBI connection holding \code{obs}}

\item{obs_tbl}{name of the observation table (default \code{"obs"})}

\item{verbose}{logical; message the headline}
}
\value{
a data.frame, one row per \code{dataset_key}: \code{n_obs}, \code{n_ungridded},
\code{pct_ungridded}, \code{n_no_position} (ungridded AND no lat/lon at all), and
\code{finding}, a sentence ready to paste into a \code{questions.csv} \code{context} cell
}
\description{
Until v2026.08.11 every ingest's core projection filtered
\verb{WHERE grid_key IS NOT NULL}, so an observation whose event did not land on a
station grid cell never reached \code{obs} at all — while the \code{sample} arm kept the
event. That asymmetry is what let four \code{calcofi_mets} cruises reach a release
as 11,762 underway samples with zero observations, their 1.7M measurements
reachable only through the supplemental table.
}
\details{
Excluding them was also inconsistent with the pipeline's own reasoning:
\code{obs_mets_full} had already been deliberately gated on \emph{a position} rather
than on \code{grid_key}, because "a ship on transit is legitimately outside the
CalCOFI station grid" — and \code{calcofi_phytoplankton} is region-pooled and has
emitted ungridded \code{obs} from the start. The headline table now agrees with
both: no grid cell is not a reason to delete an observation.

It IS a reason to ask. An ungridded observation is one of three things and the
pipeline cannot tell them apart: a genuinely off-grid position (transit, an
historical station outside the modern pattern), a coarser spatial notion (a
region-pooled sample with no point at all), or \strong{a coordinate error} — the
sign-flipped \code{Longitude_W} that put five CalCOFI cruises in the Taiwan Strait
was invisible precisely because being off-grid silently removed the rows. So
this reports every dataset's share and is meant to drive a \code{questions.csv}
entry per dataset, not to be quietly tolerated.
}
\concept{check}
