% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_measurement_types}
\alias{register_measurement_types}
\title{Append new measurement types to the shared registry, safely}
\usage{
register_measurement_types(new_types, path, quiet = FALSE)
}
\arguments{
\item{new_types}{data.frame of candidate rows; must have a \code{measurement_type}
column. Columns absent from the registry are dropped with a warning, so a
stray column cannot silently widen the registry.}

\item{path}{path to \code{metadata/measurement_type.csv}}

\item{quiet}{suppress the "added N type(s)" message}
}
\value{
The full updated registry (invisibly if nothing changed), suitable for
\code{dbWriteTable(con, "measurement_type", ...)}.
}
\description{
Replaces the read / \code{bind_rows} / \code{write_csv} cycle that each ingest used to
hand-roll. Reads and validates the registry, appends only types not already
present, and writes with \code{na = ""} so empty cells stay empty. Writes nothing
when there is nothing new to add.
}
\examples{
\dontrun{
d_meas_type <- register_measurement_types(
  my_new_types, here::here("metadata/measurement_type.csv"))
dbWriteTable(con, "measurement_type", as.data.frame(d_meas_type), overwrite = TRUE)
}
}
\concept{registry}
