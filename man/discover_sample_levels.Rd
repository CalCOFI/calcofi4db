% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{discover_sample_levels}
\alias{discover_sample_levels}
\title{Discover a dataset's sampling hierarchy from the core \code{sample} table}
\usage{
discover_sample_levels(con, dataset_key)
}
\arguments{
\item{con}{DuckDB connection carrying a core \code{sample} table}

\item{dataset_key}{Dataset provenance stamp, e.g. \code{"swfsc_ichthyo"}}
}
\value{
A tibble, one row per \code{sample_type}, ordered root-first:
\describe{
\item{sample_type}{the level name}
\item{n}{rows at this level}
\item{parent_sample_type}{the parent level, or \code{NA} for a root}
\item{depth}{0 for a root, 1 for its children, …}
\item{n_orphan}{rows whose \code{parent_sample_key} does not resolve}
}
Returns a zero-row tibble when the dataset has no \code{sample} rows.
}
\description{
Walks the \code{parent_sample_key} adjacency list to recover the sampling levels
and their nesting order, without any per-dataset configuration. This is the
generic replacement for the hand-written level lists in the per-dataset
\verb{publish_*_to-netcdf.qmd} notebooks.
}
\details{
Levels are returned in \strong{topological order} (roots first), which is the order
a netCDF-4 file must define them in: a child's \code{parent_index} points into its
parent's dimension, so the parent must already exist.

A level's parent is determined by majority vote over the resolved parents of
its rows: \code{sample_type} is a categorical label, and a single mislabelled row
should not invent a whole extra level. Rows whose parent does not resolve are
counted in \code{n_orphan} rather than dropped, because an orphan is a data problem
the caller must see — silently discarding it is how a level's row count stops
matching the table it came from.
}
\examples{
\dontrun{
con <- cc_get_db()
discover_sample_levels(con, "swfsc_ichthyo")
#> sample_type n      parent_sample_type depth n_orphan
#> site        13108  NA                     0        0
#> tow         26216  site                   1        0
#> net         52432  tow                    2        0
}
}
\concept{publish}
