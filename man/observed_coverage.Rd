% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{observed_coverage}
\alias{observed_coverage}
\title{Measure Observed Temporal and Spatial Coverage per Dataset}
\usage{
observed_coverage(con, tables = c("sample", "obs"), digits = 1)
}
\arguments{
\item{con}{DuckDB connection holding the assembled core.}

\item{tables}{Tables to measure, in order. Each contributes whichever of
\code{datetime} / \code{latitude} / \code{longitude} it actually has; a table absent from
the connection is skipped rather than erroring.}

\item{digits}{Decimal places for the formatted bbox label.}
}
\value{
Tibble, one row per \code{dataset_key}, sorted by key:
\code{time_min}/\code{time_max} (\code{"YYYY-MM"}), \code{lat_min}/\code{lat_max}/\code{lon_min}/
\code{lon_max} (numeric), and the display labels
\code{coverage_temporal_observed} / \code{coverage_spatial_observed}.
}
\description{
Derives each dataset's real extent from the assembled core (\code{sample} +
\code{obs}) instead of the \code{coverage_temporal} / \code{coverage_spatial} strings an
ingest asserts in its \code{calcofi.dataset_meta} YAML.
}
\details{
\strong{Why measure rather than assert.} A hand-written extent cannot help going
stale — it is authored once and the data grows underneath it. Checked
against release \code{v2026.08.06}, the asserted temporal string was wrong for 7
of 15 datasets: \code{cce-lter_zoodb} claimed coverage through 2021-05 when its
data ends 2015-04, \code{calcofi_phyllosoma} stopped a year short of its own
rows, and three datasets said \code{"present"} while in fact stalling in 2019,
2022 and 2023.

\strong{\code{NaN} is not \code{NULL}.} A \code{NaN} coordinate survives \verb{IS NOT NULL}, and
\code{min()}/\code{max()} propagate it, so a single poisoned row would blow a
dataset's whole bounding box out to \code{NaN} while every nullity check passed.
The coordinate filter is \code{isfinite()}, which rejects \code{NaN} and \verb{±Inf} alike.
See the same trap in \code{\link[=append_sample]{append_sample()}}, which normalizes these at write time.

\strong{Absent beats invented.} A dataset with no usable datetimes gets \code{NA} for
the temporal half, not a guess — \code{calcofi_phytoplankton} is region-pooled and
carries coordinates but no \code{datetime}, so it legitimately measures spatially
and not temporally. Callers fall back to a declared static value there.
}
\concept{release}
