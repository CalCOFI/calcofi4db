% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_upload.R
\name{qc_upload_con}
\alias{qc_upload_con}
\title{A connection an uploaded cast can be QC'd on}
\usage{
qc_upload_con(core, dir_workflows, gebco_tif = NULL)
}
\arguments{
\item{core}{output of \code{\link[=ctd_upload_to_core]{ctd_upload_to_core()}}}

\item{dir_workflows}{root of the workflows checkout, for \code{\link[=qc_stage_reference]{qc_stage_reference()}}}

\item{gebco_tif}{optional bathymetry raster}
}
\value{
a DBI connection; the caller closes it
}
\description{
The upload becomes \code{obs} and \code{sample} — the names every rule uses — so the
registry runs against it verbatim. \code{obs_ctd_full} is the same data: an uploaded
cast IS full resolution, so the profile rules apply to it directly rather than
skipping.
}
\details{
Nothing here touches a release. The connection is in-memory and dies with the
session.
}
\concept{upload}
