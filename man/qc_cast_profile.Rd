% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_cast_profile}
\alias{qc_cast_profile}
\title{Fetch one physical cast's profile, both directions}
\usage{
qc_cast_profile(
  con,
  sample_key,
  measurement_types = NULL,
  obs_tbl = "obs_ctd_full",
  cruise_key = NULL
)
}
\arguments{
\item{con}{a DBI connection carrying \code{obs_ctd_full} (or \code{obs_tbl}) and \code{sample}}

\item{sample_key}{any one direction's key; both are returned}

\item{measurement_types}{restrict to these types; \code{NULL} for all}

\item{obs_tbl}{source table (\code{"obs_ctd_full"}, or \code{"obs"} for the thinned set)}

\item{cruise_key}{partition to prune to; \code{NULL} looks it up from \code{sample}}
}
\value{
a data frame of \code{sample_key}, \code{cast_dir} (\code{down}/\code{up}), \code{depth_m},
\code{measurement_type}, \code{measurement_value}, \code{measurement_qual}, \code{datetime},
ordered by type, direction and depth
}
\description{
Returns the full-resolution scans for the cast a \code{sample_key} belongs to —
\strong{both} the down- and upcast, since the point of plotting a profile during
review is to see them overlaid. \code{obs} carries only one direction per physical
cast (that is what thinning does), so the default source is the supplemental
\code{obs_ctd_full}.
}
\details{
\code{cruise_key} is not a filter for the caller's convenience, it is a performance
precondition: \code{obs_ctd_full} is hive-partitioned by \code{cruise_key}, so supplying
it prunes ~212M rows to one cruise. When it is not supplied this looks it up
from \code{sample} — one cheap query, rather than letting a profile fetch scan the
whole archive.
}
\concept{qc}
