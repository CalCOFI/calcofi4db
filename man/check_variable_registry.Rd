% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{check_variable_registry}
\alias{check_variable_registry}
\title{Check \code{metadata/variable.csv} against \code{metadata/measurement_type.csv}}
\usage{
check_variable_registry(variable_csv, measurement_type_csv)
}
\arguments{
\item{variable_csv}{path to \code{metadata/variable.csv}}

\item{measurement_type_csv}{path to \code{metadata/measurement_type.csv}}
}
\value{
\code{TRUE}, invisibly, on success.
}
\description{
Three standing invariants a measurement page (D6) needs to hold, checked
together so a single run reports every violation rather than stopping at
the first:
}
\details{
\itemize{
\item every non-\code{NA} \code{measurement_type.variable} value has \strong{exactly one}
\code{variable.csv} row — no crosswalk key is orphaned
\item every \code{variable.csv} row has \strong{>= 1 member series} in
\code{measurement_type.csv} — no unused key ships
\item a row's \code{nerc_p01}, when set, equals \strong{every} member series' own
\code{nerc_p01} (an empty row value is always allowed — see
\code{\link[=register_variables]{register_variables()}} for why a genuine member disagreement is recorded
that way, not as an error)
\item a key's member series carry the \strong{same units}, or a known-equivalent
spelling (\code{PSS-78} / \code{PSU} — the same practical salinity scale under two
names; the CalCOFI bottle and the CTD's own processing software write it
differently)
}
}
\seealso{
\code{\link[=register_variables]{register_variables()}}, which enforces the \code{nerc_p01} half of this
at write time.
}
\concept{registry}
