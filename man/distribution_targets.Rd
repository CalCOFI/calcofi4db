% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{distribution_targets}
\alias{distribution_targets}
\title{Every external endpoint worth observing: the registry plus the holdings' links}
\usage{
distribution_targets(registry, sidecars = NULL)
}
\arguments{
\item{registry}{the tibble from \code{\link[=read_distribution_registry]{read_distribution_registry()}}}

\item{sidecars}{the named list from \code{\link[=read_dataset_sidecars]{read_dataset_sidecars()}}, or NULL}
}
\value{
A \link[tibble:tibble]{tibble} with the registry's columns.
}
\description{
A holding (\code{status: planned | external | archived} in its descriptive sidecar)
has no release objects, so its only endpoints are its \code{link_data_source} and
its DOI — plan § D-11 asks for them to be observed exactly like an ingested
dataset's. Rows are deduplicated on \verb{(dataset_key, url)}, the registry's row
winning (it carries the curated \code{id}, \code{title} and \code{status}).
}
\concept{catalog}
