% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{resolve_dataset_taxon}
\alias{resolve_dataset_taxon}
\alias{build_dataset_taxon}
\title{Fill \code{taxon_key} on the \code{dataset_taxon} crosswalk (per-dataset vocabulary -> \code{taxon})}
\usage{
resolve_dataset_taxon(
  con,
  measurement_taxon = NULL,
  overrides = NULL,
  tbl = "dataset_taxon"
)

build_dataset_taxon(
  con,
  measurement_taxon = NULL,
  overrides = NULL,
  tbl = "dataset_taxon"
)
}
\arguments{
\item{con}{a DuckDB connection with the staged vocabulary (and/or the
per-dataset taxon tables) loaded}

\item{measurement_taxon}{optional data.frame of the composite-type crosswalk
(\code{metadata/measurement_taxon.csv}) so cufes/phyllosoma/crab taxa,
which live in \code{measurement_type} names not a taxon table, are included}

\item{overrides}{optional data.frame of manual id resolution
(\code{metadata/taxon_override.csv}) for coarse taxa (phyto groups, mammals)}

\item{tbl}{target table name (default \code{"dataset_taxon"})}
}
\value{
(invisibly) the row count written
}
\description{
One row per (dataset, local taxon): the dataset's own \code{ds_taxon_key}
(\code{"<dataset-or-known-list>:<local id>"}, all lowercase — e.g. \code{calcofi:19}
for the shared CalCOFI species list, \code{cce-lter_zoodb:3} otherwise), its
\code{ds_scientific_name} / \code{ds_common_name} / \code{ds_taxa_code}, the source's own
claims as \code{ds_source_json}, and the global \code{taxon_key} it resolves to.
Deduped on \code{ds_taxon_key}.
}
\details{
Rows staged by \code{\link[=append_dataset_taxon]{append_dataset_taxon()}} are \strong{filled in place}: every column
but \code{taxon_key} comes back byte-identical, so a re-run over unchanged inputs
is a no-op. Datasets that have not staged are still read from their source
tables (the seven arms — coexistence during the migration). The key is minted
by \code{\link[=taxon_key_of]{taxon_key_of()}} from the resolved ids and the class the staged lineage
supplies, so call \code{\link[=ensure_taxon_xref]{ensure_taxon_xref()}} then \code{\link[=ensure_taxon_lineage]{ensure_taxon_lineage()}} first.

Renamed from \code{build_dataset_taxon()} in 3.29.0, which remains as a deprecated
alias: that name described a rebuild from the arms, which is exactly what an
ingest could not stage against.
}
\concept{taxonomy}
