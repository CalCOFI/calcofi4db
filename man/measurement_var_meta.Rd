% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{measurement_var_meta}
\alias{measurement_var_meta}
\title{CF variable metadata from the \code{measurement_type} registry}
\usage{
measurement_var_meta(mt)
}
\arguments{
\item{mt}{data.frame from \code{\link[=read_measurement_type]{read_measurement_type()}}. Only
\code{measurement_type} is required; \code{units}, \code{description}, \code{standard_name},
\code{is_canonical}, \code{valid_min} and \code{valid_max} are used when present.}
}
\value{
Named list keyed by \code{measurement_type}, each element
\code{list(units, long_name, standard_name, canonical, valid_min, valid_max)}.
An empty registry cell becomes \code{""} for \code{units} (never the string \code{"NA"})
and falls back to the type name for \code{long_name}, because a CF variable with
\code{long_name = "NA"} is worse than one with no \code{long_name}.
}
\description{
Turns the canonical measurement registry into the per-variable lookup the
netCDF writers use: a variable needs one \code{units}, one \code{long_name} and
optionally one \code{standard_name}. This is the widening half of publishing —
the database stores every quantity in a single \code{measurement_value} column, so
the units live in the registry rather than on the value.
}
\examples{
mt <- data.frame(measurement_type = "temperature_ave", units = "degree_C",
                 description = "average temperature", stringsAsFactors = FALSE)
measurement_var_meta(mt)$temperature_ave$units
}
\concept{publish}
