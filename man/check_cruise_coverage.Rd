% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_cruise_coverage}
\alias{check_cruise_coverage}
\title{Cruises that carry samples but no observations — the silent-loss guard}
\usage{
check_cruise_coverage(
  con,
  obs_tbl = "obs",
  max_orphan_cruises = 0L,
  halt = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{con}{a DBI connection holding \code{sample} and \code{obs}}

\item{obs_tbl}{name of the observation table (default \code{"obs"})}

\item{max_orphan_cruises}{integer allowance, or a named integer vector keyed by
\code{dataset_key} for a per-dataset ratchet. Use \code{0} where the correct answer is
known to be zero (an ingest asserting its own output); use the current counts
as a ratchet at release time so a \emph{new} orphan fails while a documented
backlog does not. May only ever be lowered.}

\item{halt}{logical; \code{stop()} when the allowance is exceeded (default \code{TRUE})}

\item{verbose}{logical; message the summary}
}
\value{
a data.frame, one row per \code{dataset_key}, with \code{cruises},
\code{cruises_no_obs}, \code{orphan_samples} and \code{emits_obs} (invisibly when
\code{verbose = FALSE})
}
\description{
A cruise can leave \code{obs} without leaving \code{sample}, and nothing about that
violates a foreign key: FK validation runs child -> parent, so every surviving
\code{obs} row still has a parent, and a parent with \strong{no children} breaks no
constraint. Release \code{v2026.08.08} shipped in exactly that state — 10
\code{calcofi_ctd-cast} cruises kept all 1,186 of their casts and lost all 874,000
of their observations, because a Google Drive placeholder read as zero rows and
the direction letter the thinning step needs came from the filename of a
conflict copy. No check anywhere looked at the parent side.
}
\details{
The grain is the \strong{cruise}, deliberately, and it is not the sample. A CTD
\code{sample} row is one physical cast \emph{per direction} while \code{obs} keeps a single
direction, so about half of \code{calcofi_ctd-cast}'s cast rows legitimately carry
no observations and a per-sample assertion would be wrong on arrival. A whole
cruise with none is never legitimate.

A dataset that emits \strong{no} observations at all is exempt rather than failing
587 times: \code{sio_pic-zooplankton} is a net-tow registry whose biovolumes are
still pending from the provider, so contributing \code{sample} alone is its designed
state. The rule is therefore relative — \emph{if a dataset contributes observations,
every one of its cruises must} — which needs no allowlist to say so.
}
\concept{check}
