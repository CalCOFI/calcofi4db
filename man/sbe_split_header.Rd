% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_upload.R
\name{sbe_split_header}
\alias{sbe_split_header}
\title{Recover the column names from a fixed-width Sea-Bird ASCII header}
\usage{
sbe_split_header(header, data_rows)
}
\arguments{
\item{header}{the header line}

\item{data_rows}{a few data lines to take column edges from}
}
\value{
character vector of column names
}
\description{
The \code{.asc} header is fixed-width, and in most CalCOFI files adjacent names
touch: \code{Sbeox0ML/L} and \code{Sbeox0Mm/Kg} arrive as \code{Sbeox0ML/LSbeox0Mm/Kg}.
Splitting on whitespace therefore produces the wrong number of columns and
silently mis-assigns every column after the collision — which in a QC tool is
worse than not reading the file at all.
}
\details{
Both the numbers and the header names are RIGHT-ALIGNED in their column, so the
aligned edge is each field's stop position. Those are taken from the data rows
(where fields are always separated) and the header is sliced at them.

It ERRORS rather than guessing when the result is not self-consistent — an
empty name, a name containing a space, or a count that does not match the data.
Measured on the CalCOFI archive this reads ~86\% of \code{.asc} files cleanly; the
rest report the problem and ask for the \code{.cnv}, whose header is unambiguous.
}
\concept{upload}
