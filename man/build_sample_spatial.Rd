% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{build_sample_spatial}
\alias{build_sample_spatial}
\title{Exact polygon membership of every root sample, one layer at a time}
\usage{
build_sample_spatial(con, layers = NULL, tbl = "sample_spatial")
}
\arguments{
\item{con}{DuckDB connection with the spatial extension, \code{sample_root} and \code{spatial}.}

\item{layers}{layers to compute (default: every layer in \code{spatial}).}

\item{tbl}{output table.}
}
\value{
A tibble with one row per layer: \code{layer}, \code{n_polys}, \code{n_roots}, \code{n_memberships}.
}
\description{
\code{ST_Intersects} between the root samples' points and each layer's polygons, chunked per layer so
the join never holds more than one layer in memory (the spatial join that OOM-ed the 16 GB server
when an app ran it over every layer at once). CRS tags are stripped on both sides through WKB
(\code{ST_Point} tags \code{OGC:CRS84}, \code{ST_Read} tags \code{EPSG:4326}, and DuckDB refuses to intersect across
them). Only polygon geometries take part: the maritime-limit layers are boundary \emph{lines} and the
ports are points, and a point never intersects either — a layer with no polygons is skipped and
reported with \code{n_polys = 0}. Asserts per layer that no \verb{(root_id, spatial_key)} pair repeats.
}
\concept{release}
