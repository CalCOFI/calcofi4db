% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shards.R
\name{supplemental_core_tables}
\alias{supplemental_core_tables}
\title{Supplemental full-resolution tables declared by the ingests}
\usage{
supplemental_core_tables(root = ".", which = TRUE)
}
\arguments{
\item{root}{workflows repo root.}

\item{which}{\code{TRUE} for all declared, \code{FALSE} for none, or an explicit
character vector (returned as given, so a caller can override).}
}
\value{
Character vector of table names, possibly empty.
}
\description{
Reads every ingest's \code{calcofi.tables_owned} and returns the tables flagged
\code{supplemental: true} — the full-resolution products that ship alongside the
thinned core (\code{obs_ctd_full}, \code{obs_mets_full}), hosted and tagged to the
release but hidden from the default table list and the ERD.
}
\details{
Only \code{obs}-shaped tables (named \verb{obs_*}) are returned. A supplemental table
that is not a slice of the core cannot be assembled by \code{\link[=assemble_core]{assemble_core()}},
which renumbers \code{obs_id} and orders by the core's columns — \code{calcofi_mets}
previously declared the raw \code{mets_measurement} here, which carried neither an
\code{obs_id} nor any coordinate and could not be published usefully.
}
\examples{
\dontrun{
supplemental_core_tables("workflows")   # "obs_ctd_full" "obs_mets_full"
}
}
\concept{shards}
