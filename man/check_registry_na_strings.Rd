% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{check_registry_na_strings}
\alias{check_registry_na_strings}
\title{Reject sentinel strings that should have been empty cells}
\usage{
check_registry_na_strings(
  df,
  path = NULL,
  sentinels = c("NA", "NaN", "NULL", "N/A", "na"),
  cols = NULL
)
}
\arguments{
\item{df}{a data.frame read from a registry CSV}

\item{path}{optional source path, used in the error message}

\item{sentinels}{character strings that must never appear as literal values
(default \code{"NA"}, \code{"NaN"}, \code{"NULL"}, \code{"N/A"}, \code{"na"})}

\item{cols}{character columns to check (default: all character columns)}
}
\value{
\code{df}, invisibly and unchanged, when clean
}
\description{
Guards a metadata registry against the \code{write_csv(na = "NA")} round-trip
described in \code{R/registry.R}. Errors listing the offending columns and rows
rather than returning quietly, because the whole failure mode is silence.
}
\examples{
\dontrun{
check_registry_na_strings(readr::read_csv("metadata/measurement_type.csv"))
}
}
\concept{registry}
