% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{observe_distributions}
\alias{observe_distributions}
\title{Ask every portal what it says about our external copies}
\usage{
observe_distributions(registry, portals = NULL, fetch = NULL, quiet = FALSE)
}
\arguments{
\item{registry}{the tibble from \code{\link[=read_distribution_registry]{read_distribution_registry()}}, or the union
from \code{\link[=distribution_targets]{distribution_targets()}}}

\item{portals}{the tibble from \code{\link[=read_portal_registry]{read_portal_registry()}}, or NULL (every row is
then observed by \code{http})}

\item{fetch}{the HTTP function (see \code{\link[=check_dataset_citation]{check_dataset_citation()}}); the tests
inject one over saved responses, so the suite never touches the network}

\item{quiet}{suppress the per-row progress line}
}
\value{
A \link[tibble:tibble]{tibble}: the registry's key columns plus \code{method},
\code{registry_status}, \code{status} (one of \code{\link[=observation_statuses]{observation_statuses()}}),
\code{observed_utc}, \code{http_status}, \code{revision}, \code{updated}, \code{note}.
}
\description{
One observer per \code{portal.csv} \code{observe_method} — \code{edi-pasta} (the newest
revision through EDI's cite service, since PASTA's \verb{/package/eml/\{scope\}/\{id\}}
answers 403 anonymously), \code{doi} (does doi.org resolve), \code{obis-api} (the
dataset's \code{updated}), \code{ncbi-esummary}, \code{zenodo-api}, \code{erddap-das}
(\code{date_modified} / \code{time_coverage_end}, and a 404 means the id is gone),
\code{caloos} / \code{http} (liveness). Nothing is written into the registry and no row
is ever dropped: the result is a parallel observation, and \code{retired} is a
\emph{status}, not a deletion.
}
\concept{catalog}
