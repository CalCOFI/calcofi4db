% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/release.R
\name{thin_plan}
\alias{thin_plan}
\title{Archive-thinning plan: which versions lose their parquet}
\usage{
thin_plan(versions, latest, consolidated, keep_latest = 2)
}
\arguments{
\item{versions}{list as returned by \code{\link[=build_versions_json]{build_versions_json()}} (any order)}

\item{latest}{the promoted version string}

\item{consolidated}{character vector of consolidated versions}

\item{keep_latest}{how many of the newest versions to keep (default 2)}
}
\value{
tibble \code{version}, \code{keep} (logical), \code{reason}, \code{to} (replacement for
candidates, \code{NA} for kept)
}
\description{
Pure policy over a \code{versions.json} list: keep the consolidated versions, the
promoted version and its \code{keep_latest - 1} predecessors; everything else not
already retired is a candidate. Each candidate is retired \emph{to} the nearest
consolidated (or kept) version at or after it — the closest data a reader
can substitute.
}
\concept{release}
