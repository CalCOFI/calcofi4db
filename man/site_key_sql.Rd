% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_key.R
\name{site_key_sql}
\alias{site_key_sql}
\title{The canonical \code{site_key} spelling, as a SQL expression}
\usage{
site_key_sql(expr = "site_key")
}
\arguments{
\item{expr}{a SQL expression or column name holding the string (default
\code{"site_key"}).}
}
\value{
a length-one character SQL expression.
}
\description{
Returns a DuckDB expression that rewrites any spelling of a CalCOFI line/station
pair into the one form the rest of the pipeline uses,
\code{printf('\%05.1f \%05.1f', line, station)} — the form
\code{\link[=standardize_site_key]{standardize_site_key()}} writes from numeric columns. The first two signed
decimal numbers in the string are the line and the station, whatever separates
them (a space, several, a comma) and however they are padded; a string with
fewer than two numbers becomes \code{NULL}. A negative station (ichthyo's
\code{"011.7 -02.6"}, inshore of the line origin) keeps its sign; the width-5 format
makes it \code{-02.6}, which is what \verb{\%05.1f} already produced for those rows.
}
\details{
Rounding is to one decimal, the grid's own resolution (0.1 station unit is
0.74 km along a line): the one source form with two decimals, \verb{090.0 27.76},
becomes \verb{090.0 027.8}.
}
\examples{
site_key_sql("site_key")
}
\seealso{
\code{\link[=normalize_site_key]{normalize_site_key()}} for the R vector form, \code{\link[=check_site_key_format]{check_site_key_format()}}
for the release gate, \code{\link[=append_sample]{append_sample()}} which applies this to every ingest.
}
\concept{keys}
