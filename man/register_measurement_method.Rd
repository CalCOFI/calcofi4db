% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry_measurement_face.R
\name{register_measurement_method}
\alias{register_measurement_method}
\title{Append-or-update \code{metadata/measurement_method.csv} by (dataset_key, measurement_type)}
\usage{
register_measurement_method(new_rows, path, quiet = FALSE)
}
\arguments{
\item{new_rows}{data.frame with \code{dataset_key}, \code{measurement_type} and any
other column of \code{.measurement_method_cols()}}

\item{path}{path to \code{metadata/measurement_method.csv}}

\item{quiet}{suppress the added/updated message}
}
\value{
the full updated registry
}
\description{
One row per series in \code{measurements.json} (D4): \code{platform} in \code{bottle | ctd | underway | lab | net | mast}; \code{nerc_l22} only on an exact device match (empty
otherwise, per the \code{metadata-registries} skill's exact-match rule); \code{steps}
and \code{bibkeys} are \code{" | "}-joined lists on one CSV cell. A method the source
cannot support ships with \code{principle} empty and \code{source = "not found"} —
never a guess.
}
\concept{registry}
