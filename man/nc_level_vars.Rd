% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netcdf.R
\name{nc_level_vars}
\alias{nc_level_vars}
\title{Build the variable definitions for one level of a nested dataset}
\usage{
nc_level_vars(
  group,
  df,
  dim,
  parent_dim = NULL,
  parent_index = NULL,
  var_meta = list(),
  strlen = 64L
)
}
\arguments{
\item{group}{Group name, e.g. \code{"tow"}, \code{"occurrence"}, \code{"length_bin"}.}

\item{df}{data.frame for this level, ordered so children are contiguous.}

\item{dim}{The \code{ncdim4} for this level.}

\item{parent_dim}{Parent level's \code{ncdim4}, or \code{NULL} at the root.}

\item{parent_index}{1-based index into the parent level, one per row of \code{df}.}

\item{var_meta}{Named list from \code{\link[=measurement_var_meta]{measurement_var_meta()}}.}

\item{strlen}{Fixed character length for string variables.}
}
\value{
Named list of \code{ncvar4} objects to pass to \code{ncdf4::nc_create()}. The
parent link, when present, is the element named \verb{__parent_index}.
}
\description{
Each sampling level becomes a netCDF-4 group. The link to the parent is an
explicit index variable, \strong{not} repetition of the parent's columns — that is
the whole reason for netCDF-4 here rather than a flat table: tow effort is
stored once, and a length-frequency bin points at the tow it came from.
Flattening instead repeats each net's \code{volume_sampled} onto every one of its
size bins, which turned 76,512 real ichthyo values into 369,978 repeated ones
and inflated any naive \code{SUM()} of effort by ~5x.
}
\concept{publish}
