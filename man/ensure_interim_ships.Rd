% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ship.R
\name{ensure_interim_ships}
\alias{ensure_interim_ships}
\title{Ensure Interim Ship Entries for Unmatched Ships}
\usage{
ensure_interim_ships(con, match_result, ship_tbl = "ship")
}
\arguments{
\item{con}{DBI connection to DuckDB with a \code{ship} table}

\item{match_result}{Tibble from \code{\link[=match_ships]{match_ships()}} with \code{match_type} and
\code{ship_code} columns. Rows with \code{match_type == "unmatched"} get interim
entries.}

\item{ship_tbl}{Name of ship table (default: \code{"ship"})}
}
\value{
Integer count of interim ships inserted
}
\description{
For any ship codes that remain unmatched after \code{\link[=match_ships]{match_ships()}}, inserts
interim placeholder rows into the \code{ship} table with \code{ship_nodc = "?SK?"}
(where \code{SK} is the 2-letter ship_key). This allows downstream operations
(cruise_key derivation, FK joins) to proceed without errors. Placeholder
ships are flagged via the \code{"?"} markers for later resolution via
\code{metadata/ship_renames.csv}.
}
\examples{
\dontrun{
result <- match_ships(unmatched, reference)
n_interim <- ensure_interim_ships(con, result)
}
}
\concept{ship}
