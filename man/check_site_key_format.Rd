% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_key.R
\name{check_site_key_format}
\alias{check_site_key_format}
\title{Fail unless every non-NULL \code{site_key} is in its canonical spelling}
\usage{
check_site_key_format(con, sample_tbl = "sample")
}
\arguments{
\item{con}{DuckDB connection holding the sample table.}

\item{sample_tbl}{the sample table (default \code{"sample"}).}
}
\value{
invisibly, a data.frame \code{dataset_key}, \code{n_rows}, \code{n_site}, \code{n_bad},
\code{example} (one offending value, or NA).
}
\description{
The release gate for \code{\link[=site_key_sql]{site_key_sql()}}: a \code{site_key} that is not equal to its own
normalisation would key a section or a climatology cell onto a station that
does not exist. NULL is allowed (underway, transect and region-pooled samples
have no station). Reports per dataset so the offending ingest is named.
}
\concept{validation}
