% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_datasets.R
\name{read_distribution_registry}
\alias{read_distribution_registry}
\title{Read \code{metadata/distribution.csv}, the curated endpoints per dataset}
\usage{
read_distribution_registry(path)
}
\arguments{
\item{path}{path to \code{metadata/distribution.csv}}
}
\value{
A \link[tibble:tibble]{tibble}, all columns character.
}
\description{
One row per external endpoint a dataset can be got or seen through that the
release cannot measure itself — a CoastWatch mirror, the EDI or NCEI record
the ingest read from, the OBIS dataset and its IPT resource, a legacy ERDDAP
id with its sunset — with \code{kind}, \code{portal}, \code{id}, \code{url}, \code{title}, \code{status}
(\code{current | superseded | retired | external | planned}), \code{superseded_by} (a
URL or a \code{dataset_key}), \code{observed_utc} (when the status was last confirmed;
\code{\link[=observe_distributions]{observe_distributions()}} will refresh it) and \code{notes}. Nothing is ever
deleted from it: a dead endpoint becomes \code{retired} with the date, so a page
can say "was at X until …". Every vocabulary column is validated; an unknown
value errors.
}
\concept{catalog}
