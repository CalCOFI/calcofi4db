% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{merge_dataset_meta}
\alias{merge_dataset_meta}
\title{Merge a notebook's structural \code{dataset_meta} with its descriptive sidecar}
\usage{
merge_dataset_meta(
  notebook_meta,
  sidecar,
  notebook = "notebook",
  sidecar_path = "sidecar",
  strict = FALSE
)
}
\arguments{
\item{notebook_meta}{the \code{calcofi.dataset_meta} list from the notebook YAML}

\item{sidecar}{the list from \code{\link[=read_dataset_sidecar]{read_dataset_sidecar()}}}

\item{notebook, sidecar_path}{names used in error messages}

\item{strict}{error on any descriptive key still present in the notebook}
}
\value{
The merged list.
}
\description{
The notebook keeps the structural keys (\code{\link[=dataset_meta_structural_keys]{dataset_meta_structural_keys()}}),
the sidecar the descriptive ones (\code{\link[=dataset_meta_descriptive_keys]{dataset_meta_descriptive_keys()}}); the
merge is their union. A descriptive key found in the notebook with a value
that differs from the sidecar's is an error (two truths); an identical value
is tolerated (and \code{strict = TRUE}, which \code{build_workflows_index.R} uses,
errors on any descriptive key left in the notebook at all). Keys the sidecar
carries that are not \code{dataset_meta} fields (\code{provider}, \code{dataset}, \code{path},
\code{status}, \code{visibility}, \code{creators}, …) come along, so a downstream reader can
see them.
}
\concept{catalog}
