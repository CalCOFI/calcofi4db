% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctd_upload.R
\name{ctd_map_columns}
\alias{ctd_map_columns}
\title{Map an uploaded file's columns onto measurement types}
\usage{
ctd_map_columns(
  cols,
  d_meas_type,
  d_sbe_map = NULL,
  format = c("csv", "cnv", "asc", "btl")
)
}
\arguments{
\item{cols}{column names of the uploaded data}

\item{d_meas_type}{the measurement registry (\code{\link[=read_measurement_type]{read_measurement_type()}})}

\item{d_sbe_map}{the Sea-Bird crosswalk (\code{metadata/sbe_name_map.csv})}

\item{format}{one of \code{"csv"}, \code{"cnv"}, \code{"asc"}, \code{"btl"}}
}
\value{
a tibble of \code{column}, \code{measurement_type}, \code{role}, \code{qual_column},
\code{units}, \code{note} — one row per uploaded column, \code{role = "unmapped"} where
nothing matched
}
\description{
Two vocabularies, one answer. A CalCOFI \code{.csv} maps through
\code{measurement_type.csv}'s \verb{_source_column}, which already holds exactly those
names; a Sea-Bird file maps through \code{metadata/sbe_name_map.csv}.
}
\details{
UNMAPPED COLUMNS ARE A RESULT, NOT AN ERROR. They are where a format change
announces itself — a renamed sensor, a new instrument, a column nobody has seen
before — so they are returned and shown rather than dropped quietly.
}
\concept{upload}
