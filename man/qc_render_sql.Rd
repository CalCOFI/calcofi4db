% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_render_sql}
\alias{qc_render_sql}
\title{Substitute \code{{{param}}} placeholders into a rule's SQL}
\usage{
qc_render_sql(sql, params)
}
\arguments{
\item{sql}{rule SQL text}

\item{params}{named list, e.g. from \code{\link[=qc_parse_params]{qc_parse_params()}}}
}
\value{
\code{sql} with every placeholder substituted
}
\description{
Errors on a placeholder with no matching param. Silently leaving \code{{{threshold}}}
in the query would produce a DuckDB parse error far from its cause, and worse, a
\emph{missing} threshold could otherwise render as an empty string and quietly change
the rule's meaning rather than failing.
}
\examples{
qc_render_sql("SELECT * FROM obs WHERE v > {{threshold}}", list(threshold = "3"))
}
\concept{qc}
