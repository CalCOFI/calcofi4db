% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_variables}
\alias{register_variables}
\title{Append new rows to \code{metadata/variable.csv}, safely}
\usage{
register_variables(new_vars, path, measurement_type_path = NULL, quiet = FALSE)
}
\arguments{
\item{new_vars}{data.frame with a \code{variable} column and any of \code{label},
\code{description}, \code{units}, \code{nerc_p01}, \code{category}, \code{is_unified}. Columns
absent from the registry are dropped with a warning.}

\item{path}{path to \code{metadata/variable.csv}}

\item{measurement_type_path}{path to \code{metadata/measurement_type.csv}, used
only to check \code{nerc_p01} agreement against member series (rows whose
\code{variable} equals the new row's \code{variable}). \code{NULL} skips the check (not
recommended — nothing else catches this).}

\item{quiet}{suppress the "added N variable(s)" message}
}
\value{
The full updated registry.
}
\description{
Mirrors \code{\link[=register_measurement_types]{register_measurement_types()}}: append-only (edit the CSV by hand to
change an existing row), refuses a duplicate \code{variable} key, writes with
\code{na = ""}.
}
\details{
It additionally refuses a row whose \code{nerc_p01} is set but disagrees with a
member series' own \code{nerc_p01} in \code{measurement_type.csv} — the whole point of
the crosswalk key is that agreement can be asserted once, and a silent
mismatch would ship the wrong concept URI to an OBIS/DwC export for every
series sharing the key but one. It does \strong{not} refuse a key whose member
series disagree with each OTHER (two series under the same \code{variable} that
carry two different NERC concepts) — that is a real fact about the data
(\code{sigma_theta}: the bottle's own computation is \code{SIGTEQ01}, the CTD's is
\code{SIGTPR01} — different P01 concepts, found WS-M1 2026-09-10), and the
correct way to record it is to leave the row's own \code{nerc_p01} empty, exactly
as an id is left empty anywhere else in these registries when no single
concept says exactly what a row is (\code{metadata-registries} skill). Passing a
non-empty \code{nerc_p01} for such a key is refused, because it would silently
pick a side.
}
\examples{
\dontrun{
register_variables(
  data.frame(variable = "temperature", label = "Temperature",
             units = "degC",
             nerc_p01 = "http://vocab.nerc.ac.uk/collection/P01/current/TEMPPR01/",
             category = "Physical Oceanography", is_unified = TRUE),
  here::here("metadata/variable.csv"),
  here::here("metadata/measurement_type.csv"))
}
}
\seealso{
\code{\link[=check_variable_registry]{check_variable_registry()}}, the standing invariant this enforces
at write time.
}
\concept{registry}
