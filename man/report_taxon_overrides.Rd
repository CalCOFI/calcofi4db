% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxa.R
\name{report_taxon_overrides}
\alias{report_taxon_overrides}
\title{What each \code{taxon_override.csv} row matched, applied to and skipped}
\usage{
report_taxon_overrides(con, overrides, tbl = "dataset_taxon", verbose = TRUE)
}
\arguments{
\item{con}{a DBI connection holding \code{dataset_taxon}}

\item{overrides}{the override registry (\code{metadata/taxon_override.csv})}

\item{tbl}{crosswalk table name (default \code{"dataset_taxon"})}

\item{verbose}{logical; message the per-dataset summary}
}
\value{
a data.frame (\code{dataset_key}, \code{match_column}, \code{match_value},
\code{override_key}, \code{n_matched}, \code{n_applied}, \code{n_skipped}, \code{skipped_codes},
\code{source_json_known}), one row per override row, invisibly when
\code{verbose = FALSE}
}
\description{
Recomputes the override rule (see \code{\link[=resolve_dataset_taxon]{resolve_dataset_taxon()}}) from
\code{dataset_taxon} alone — the dataset's vocabulary, its \code{ds_source_json} (what
the source supplied) and the registry — so the same report is available in
an ingest after the resolve and in \code{release_database.qmd}, where every
dataset is present and only \code{dataset_taxon} is. One row per override row:
}
\details{
\itemize{
\item \code{n_matched} — vocabulary rows whose \code{match_column} equals \code{match_value};
\item \code{n_applied} — of those, the rows the override keyed (a code match, or a
row whose source supplied no id);
\item \code{n_skipped} — of those, the rows that kept the source's own id, with the
first of their codes in \code{skipped_codes}. \code{NA} when \code{source_json_known} is
\code{FALSE}: no row of that dataset carries \code{ds_source_json}, so the shard
either predates calcofi4db 3.29.0 or its source supplied no ids at all, and
the release cannot tell which — the ingest's own message can.
}

\code{match_column} is one of \code{dataset_taxon}'s \code{ds_taxa_code} /
\code{ds_scientific_name} / \code{ds_common_name}; anything else errors, as it does at
ingest (the transitional aliases for the arms' own column names went with the
arms in 4.0.0). A row that matches nothing is reported with zeros, not dropped —
\code{\link[=check_taxon_registries]{check_taxon_registries()}} is where a dataset nobody supplies fails.
}
\concept{taxonomy}
