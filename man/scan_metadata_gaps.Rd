% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrangle.R
\name{scan_metadata_gaps}
\alias{scan_metadata_gaps}
\title{Scan a metadata sidecar for missing documentation}
\usage{
scan_metadata_gaps(
  metadata,
  unit_exempt =
    paste0("(_key|_id|_uuid|_seq|_md|_url|_qual|_flag|_code|_name|_type|_status)$",
    "|^(latitude|longitude|geom|datetime|date|time|notes|comments|description)$",
    "|^(measurement_value|measurement_prec|bin_value|condition_value)$",
    "|^_|^(realm|grain|units|is_canonical|life_stage|stage|order_occ|rank",
    "|rank_order|count|n|sample_type|taxonomic_status|provider|dataset)$")
)
}
\arguments{
\item{metadata}{Either the in-memory metadata list, or a path to a
\code{metadata.json} file.}

\item{unit_exempt}{Regular expression matched against the \strong{bare} column name
(table prefix stripped) for columns where a missing \code{units} is expected
rather than a gap — keys, identifiers, names, flags, timestamps, free text
and the long-format value columns are not unit-bearing measurements.}
}
\value{
An object of class \code{cc_metadata_gaps}: a list with \code{n_tables},
\code{n_columns}, and the character vectors \code{tables_no_desc}, \code{columns_no_desc}
and \code{columns_no_units}. Has a \code{print()} method, so a notebook chunk can just
call it.
}
\description{
Empty table/column descriptions and missing units are invisible until a
consumer hits them: they travel from an ingest's \code{metadata.json} into the
release sidecar and out through \link{calcofi4r} \code{cc_describe_table()} /
\code{cc_db_catalog()}, where they render as blank documentation. \code{\link[=build_metadata_json]{build_metadata_json()}}
calls this on every write.
}
\details{
\code{units} is only meaningful for a measured quantity, so reporting every
\verb{*_key}, name and timestamp as unit-less would bury the real gaps in noise —
which is why \code{unit_exempt} exists rather than a flat count. The exemption is
about \emph{reporting}: nothing here changes what is written.
}
\examples{
md <- list(
  tables  = list(obs = list(description_md = "")),
  columns = list(`obs.temperature` = list(description_md = "temp", units = NULL),
                 `obs.sample_key`  = list(description_md = "",     units = NULL)))
scan_metadata_gaps(md)
}
\concept{wrangle}
