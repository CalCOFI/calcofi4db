% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{sample_arm_self}
\alias{sample_arm_self}
\title{Build a \code{sample} arm for a single self-contained event table}
\usage{
sample_arm_self(
  dataset_key,
  tbl,
  id_col,
  sample_type,
  dt_col = "datetime_start_utc",
  grid_expr = "grid_key",
  site_expr = "NULL::VARCHAR",
  ord_expr = "NULL::INTEGER",
  depth_min = "0::DOUBLE",
  depth_max = "0::DOUBLE"
)
}
\arguments{
\item{dataset_key}{provider_dataset}

\item{tbl}{the event table}

\item{id_col}{its id column}

\item{sample_type}{core \code{sample_type} value}

\item{dt_col}{datetime column, or \code{"NULL"} for none}

\item{grid_expr, site_expr, ord_expr, depth_min, depth_max}{SQL expressions or bare
column names (bare names are alias-qualified for you)}
}
\value{
a SQL SELECT string for \code{\link[=append_sample]{append_sample()}}
}
\description{
The declarative shape most datasets need: one \code{sample} row per row of one event
table, keyed \code{dataset_key:sample_type:id}, with no parent. Exported because a
dataset's projection belongs in its own ingest notebook — this is what keeps that
a one-line declaration rather than copied SQL.
}
\concept{model}
