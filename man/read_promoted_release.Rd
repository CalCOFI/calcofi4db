% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloud.R
\name{read_promoted_release}
\alias{read_promoted_release}
\title{Read the currently promoted release, authoritatively}
\usage{
read_promoted_release(bucket = "calcofi-db", prefix = "ducklake/releases")
}
\arguments{
\item{bucket}{GCS bucket holding \code{{prefix}/latest.txt}}

\item{prefix}{bucket-relative releases prefix (default \code{ducklake/releases}; a staging run uses another)}
}
\value{
the promoted version string (e.g. \code{"v2026.08.14"}), or \code{NA_character_}
if the object cannot be read
}
\description{
\code{latest.txt} is the pointer every consumer resolves through, so reading it
over the public \verb{https://storage.googleapis.com/...} URL is not safe for a
control-flow decision: that URL is CDN-cached, and the object carried no
\code{Cache-Control} until 2026-08-14, so it inherited the 1-hour public default.
}
\details{
On 2026-08-14 that bit twice in one hour, and the second direction is the
dangerous one. A rollback to \code{v2026.08.11} took an hour to reach consumers;
then \code{release_database.qmd}'s republish guard — reading the same cached URL —
false-fired because it still saw the pre-rollback value. The mirror image is
worse: immediately after a promotion the cache still shows the \emph{previous}
version, so a guard comparing against it concludes \code{latest.txt} points
somewhere else and permits a run to overwrite the release consumers are
actively reading, which is the exact thing it exists to prevent. A one-hour
blind window after every promotion, failing open.

This reads the object through the authenticated API instead, which is never
cached.
}
\concept{cloud}
