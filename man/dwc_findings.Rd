% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_findings}
\alias{dwc_findings}
\title{The findings \code{\link[=dwc_check]{dwc_check()}} can report, with their level}
\usage{
dwc_findings()
}
\value{
A named character vector, finding -> level.
}
\description{
\code{error} findings mean \strong{no archive is written} for that dataset — the whole
point of gating: a broken archive at OBIS is worse than a missing one.
}
\details{
\itemize{
\item \code{orphan_event} — a \code{parentEventID} naming no event in the core (\code{obistools::check_eventids()}).
\item \code{orphan_occurrence} — an occurrence whose \code{eventID} is not in the core.
\item \code{orphan_emof} — an eMoF row whose \code{eventID} / \code{occurrenceID} is not in the archive.
\item \code{missing_required_field} — \code{obistools::check_fields()} at level \code{error} on
\strong{every} occurrence: nothing in the archive would index at OBIS, so writing it
would publish an empty dataset. \code{calcofi_phytoplankton} is here at v2026.09.05
— all 409 \code{region_pool} samples carry no \code{datetime}, so no occurrence has an
\code{eventDate}.
\item \code{incomplete_records} — the same check failing on SOME occurrences: those records
will not index at OBIS and the rest will, so the archive is written and the count
is reported. A gap in the release, not a fault of the mapping.
\item \code{bad_event_date} — \code{obistools::check_eventdate()} rejected a value.
\item \code{duplicate_id} — a repeated \code{eventID} or \code{occurrenceID}.
\item \code{no_occurrence} — the dataset produced no occurrence rows at all.
\item \code{no_scientific_name_id} — occurrences whose taxon has no WoRMS id, so
\code{scientificNameID} is empty (warn — never a guessed LSID).
\item \code{no_life_stage_id}, \code{no_measurement_type_id}, \code{no_measurement_unit_id} — a
registry states no exact concept for a value the archive emits (warn).
\item \code{dropped_no_taxon} — \code{obs_bio} rows with no taxon, which cannot be Occurrences (warn).
\item \code{no_event_date}, \code{no_coordinates} — events missing either (warn: OBIS accepts
the archive, a consumer will notice).
}
}
\concept{dwc}
