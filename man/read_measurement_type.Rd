% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{read_measurement_type}
\alias{read_measurement_type}
\title{Read \code{metadata/measurement_type.csv}, refusing a corrupted registry}
\usage{
read_measurement_type(path, validate = TRUE)
}
\arguments{
\item{path}{path to \code{metadata/measurement_type.csv}}

\item{validate}{error on sentinel strings (default TRUE). Only set FALSE to
inspect a file you already know is broken.}
}
\value{
A \link[tibble:tibble]{tibble} of the registry, with empty cells as \code{NA}.
}
\description{
The canonical measurement vocabulary, validated on the way in via
\code{\link[=check_registry_na_strings]{check_registry_na_strings()}} so a \code{write_csv(na = "NA")} round trip fails here
instead of silently reaching the release.
}
\details{
The read is deliberately \strong{strict}: \code{na = ""}, so only genuinely empty cells
become \code{NA}. \code{read_csv()}'s default is \code{na = c("", "NA")}, which converts the
literal string \code{"NA"} back to \code{NA} — meaning a default read \emph{cannot see} this
corruption, and no validator downstream of one ever could. DuckDB, which is not
so forgiving, is where the damage surfaces.
}
\concept{registry}
