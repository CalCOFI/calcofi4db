% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_archive}
\alias{dwc_archive}
\title{Write a Darwin Core Archive and its manifest}
\usage{
dwc_archive(
  dir,
  event,
  occurrence = NULL,
  emof = NULL,
  eml_path = NULL,
  dataset_key = NA_character_,
  version = NA_character_,
  ipt_resource = NULL,
  obis_dataset_id = NULL,
  zip_path = NULL
)
}
\arguments{
\item{dir}{the directory to write into (created)}

\item{event, occurrence, emof}{the checked frames}

\item{eml_path}{path to the release's \verb{eml/\{dataset_key\}.xml}, copied in as
\code{eml.xml}; NULL writes no metadata document (and says so)}

\item{dataset_key, version}{named in the manifest and the zip name}

\item{ipt_resource, obis_dataset_id}{the published copy's ids, from
\code{distribution.csv}; NULL keeps whatever an existing manifest holds}

\item{zip_path}{where to write the archive; defaults to
\verb{\{dirname(dir)\}/\{dataset_key\}_\{version\}.zip}}
}
\value{
A list: \code{zip}, \code{manifest} (the path), \code{content_hash}, \code{counts}.
}
\description{
Writes \code{event.csv}, \code{occurrence.csv}, \code{extendedMeasurementOrFact.csv}, \code{meta.xml}
and \code{eml.xml} into \code{dir}, zips them (flat, no directory entries — what the IPT
expects), and writes \code{manifest.json} beside the zip.
}
\details{
\strong{The manifest is how a dataset page knows an upload is due} (D-8): it records
\code{content_hash} (an md5 over the three CSVs, deterministic — the same rows always
hash the same), the release \code{version}, and the \code{ipt_resource} / \code{obis_dataset_id} /
\code{uploaded_utc} of the \emph{published} copy. Those last three are never invented here:
they are carried forward from an existing \code{manifest.json} (or supplied by the
caller from \code{distribution.csv}), so a freshly built archive that has never been
uploaded says \code{uploaded_utc: null}, and \code{registrations[]} reads that as
"not published" rather than as a date nobody set.

The upload itself is a deliberate manual act through the OBIS-USA IPT
(Decision 10, \code{docs/portals.qmd} § OBIS). Nothing in this function talks to a portal.
}
\seealso{
\code{\link[=dwc_check]{dwc_check()}}, \code{\link[=dwc_meta_xml]{dwc_meta_xml()}}
}
\concept{dwc}
