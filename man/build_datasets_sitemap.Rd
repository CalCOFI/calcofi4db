% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sitemap.R
\name{build_datasets_sitemap}
\alias{build_datasets_sitemap}
\title{Build the rows of \code{datasets/sitemap.xml} from the record}
\usage{
build_datasets_sitemap(
  record,
  observed = NULL,
  edited = NULL,
  include_holdings = TRUE,
  changefreq = "weekly"
)
}
\arguments{
\item{record}{\code{datasets.json}: a path/URL or the parsed list}

\item{observed}{the tibble from \code{\link[=read_distribution_observed]{read_distribution_observed()}}, or NULL — its
\code{updated} / \code{observed_utc} date is an external record's \code{lastmod}, and a row
observed \code{retired} is dropped even when the registry still calls it current}

\item{edited}{a named character vector \verb{dataset_key -> ISO date} of the
descriptive sidecars' last edit; a page's \code{lastmod} is the later of that and
the release date}

\item{include_holdings}{list the holdings' pages too (default TRUE — a holding
has a page at the same URL shape, plan § D-11)}

\item{changefreq}{the \code{changefreq} written for every URL}
}
\value{
A \link[tibble:tibble]{tibble}: \code{loc}, \code{lastmod}, \code{changefreq}, \code{kind}
(\code{page} + \code{portal = "calcofi.io"} for a dataset page, else the
distribution's own kind and portal),
\code{dataset_key}, \code{portal}, \code{title}. Pages first, in record order, then the
external records; every \code{loc} unique.
}
\description{
Build the rows of \code{datasets/sitemap.xml} from the record
}
\concept{catalog}
