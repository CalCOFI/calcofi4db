% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{qc_run_all}
\alias{qc_run_all}
\title{Run every rule in a registry, one at a time}
\usage{
qc_run_all(
  con,
  rules,
  limit = 500L,
  on_progress = NULL,
  present_types = qc_present_types(con),
  scope_values = list()
)
}
\arguments{
\item{con}{a DBI connection carrying the tables the rule targets}

\item{rules}{a rule registry from \code{\link[=qc_read_rules]{qc_read_rules()}}}

\item{limit}{cap on rows returned. The \code{COUNT} is always computed over the full
result, so a truncated display never understates the problem — a rule that
silently showed 500 of 40,000 hits would read as "minor".}

\item{on_progress}{optional \verb{function(i, n, rule_key)} callback}

\item{present_types}{output of \code{\link[=qc_present_types]{qc_present_types()}}; \code{NULL} disables the check}

\item{scope_values}{named list supplying scope parameters, e.g.
\code{list(cruise_key = "2023-11-33P4")}. A rule with \code{scope = "cruise"} reads the
full-resolution \code{obs_ctd_full} and is meaningless unscoped, so it SKIPS rather
than silently scanning everything.}
}
\value{
list of \code{\link[=qc_run_rule]{qc_run_rule()}} results, in registry order
}
\description{
Sequential on purpose: these are multi-GB scans and running them concurrently
against one DuckDB just contends for the same buffer pool.
}
\concept{qc}
