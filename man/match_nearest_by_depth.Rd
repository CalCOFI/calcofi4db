% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{match_nearest_by_depth}
\alias{match_nearest_by_depth}
\title{Match Records to the Nearest Reference Row Along a Continuous Axis}
\usage{
match_nearest_by_depth(
  con,
  data_tbl,
  ref_tbl,
  fk_col = "bottle_id",
  ref_pk = "bottle_id",
  parent_fk = "cast_id",
  axis_col = "depth_m",
  tolerance = 1,
  return_stats = TRUE
)
}
\arguments{
\item{con}{DBI connection to DuckDB.}

\item{data_tbl}{Character. Table to add the FK column to (e.g. "dic_sample").}

\item{ref_tbl}{Character. Reference table to match against (e.g. "bottle").}

\item{fk_col}{Character. FK column to create on \code{data_tbl} (default
"bottle_id").}

\item{ref_pk}{Character. Primary-key column on \code{ref_tbl} (default
"bottle_id").}

\item{parent_fk}{Character. Parent FK present on both tables that scopes the
match (default "cast_id"); only rows with a non-NULL parent are matched.}

\item{axis_col}{Character. Continuous column minimized in absolute difference
(default "depth_m").}

\item{tolerance}{Numeric. Maximum absolute difference on \code{axis_col}
(default 1.0).}

\item{return_stats}{Logical. If TRUE (default), return a stats list;
otherwise return invisible NULL.}
}
\value{
If \code{return_stats}, a list with \code{matched}, \code{eligible} (rows with a
non-NULL parent), and \code{pct}. Side effect: adds and populates \code{fk_col}.
}
\description{
Adds a foreign-key column to \code{data_tbl} and populates it with the primary
key of the \code{ref_tbl} row that is nearest along a continuous axis (e.g.
\code{depth_m}) within \code{tolerance}, restricted to rows sharing an already-matched
parent FK (e.g. \code{cast_id}). Use after \code{\link[=match_by_site_datetime]{match_by_site_datetime()}} to descend
from a parent match (cast) to a child match (Niskin bottle). Extracted from
\code{ingest_calcofi_dic.qmd}.
}
\examples{
\dontrun{
match_by_site_datetime(con, "dic_sample", "casts")
match_nearest_by_depth(con, "dic_sample", "bottle")
}
}
\concept{spatial}
