% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dwc.R
\name{dwc_occurrence}
\alias{dwc_occurrence}
\title{Build the Darwin Core Occurrence extension for one dataset}
\usage{
dwc_occurrence(
  con,
  dataset_key,
  life_stage = NULL,
  measurement_type = NULL,
  absences = c("none", "sample_root"),
  max_absences = 5e+06
)
}
\arguments{
\item{con}{a DBI connection to the release}

\item{dataset_key}{the dataset}

\item{life_stage}{the registry from \code{\link[=read_life_stage_registry]{read_life_stage_registry()}}, or NULL}

\item{measurement_type}{the registry from \code{\link[=read_measurement_type]{read_measurement_type()}}, or NULL}

\item{absences}{\code{"none"} or \code{"sample_root"}}

\item{max_absences}{refuse to derive more absences than this}
}
\value{
A data frame of Occurrence rows, all-NA columns dropped.
}
\description{
From \code{obs_bio} joined to \code{taxon}, with \code{lifeStage} from \code{life_stage.csv} and the
D8 denominator as \code{organismQuantity}:
}
\details{
\tabular{ll}{
   Darwin Core \tab core \cr
   \code{occurrenceID} \tab md5 of \verb{(sample_key, taxon_key, life_stage, measurement_type, depth_bin, ordinal)} — stable across releases (\code{obs_id} is not) \cr
   \code{eventID} \tab \code{obs_bio.sample_key}, or the root's \code{sample_key} for a derived absence \cr
   \code{scientificName} / \code{taxonID} / \code{taxonRank} / lineage \tab \code{taxon} \cr
   \code{scientificNameID} \tab the WoRMS LSID of \code{taxon.worms_id}; \strong{empty when the taxon has none} \cr
   \code{lifeStage} \tab \code{life_stage.csv} \code{dwc_lifeStage}, else the verbatim value where the registry gives it a \code{life_stage_parent}; a value the registry records as \emph{not a life stage} goes to \code{occurrenceRemarks} \cr
   \code{organismQuantity} / \code{organismQuantityType} \tab \code{density_per_10m2}, else \code{density_per_1000m3}, else \code{value} + its registry \code{units} \cr
   \code{occurrenceStatus} \tab \code{"present"} where \code{value > 0}, \code{"absent"} where \code{value = 0} \cr
}


\strong{The absence rule.} See \code{\link[=dwc_absence_rule]{dwc_absence_rule()}}. \code{absences = "none"} (the default)
emits only rows the release holds. \code{absences = "sample_root"} additionally emits
one \code{absent} row for every (surveyed root sample x observed taxon/stage) pair with
no positive row — the \code{sample_root} minus positives rule. It is correct only for a
dataset whose protocol sorts every sample for its whole vocabulary, so it is never
the default, and \code{max_absences} (5,000,000) stops a large vocabulary from turning
a survey into a hundred million assertions nobody made.
}
\seealso{
\code{\link[=dwc_event]{dwc_event()}}, \code{\link[=dwc_emof]{dwc_emof()}}, \code{\link[=dwc_absence_rule]{dwc_absence_rule()}}
}
\concept{dwc}
