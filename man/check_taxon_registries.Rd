% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_taxon_registries}
\alias{check_taxon_registries}
\title{Every dataset a taxon registry names must be one some dataset supplies}
\usage{
check_taxon_registries(
  con,
  overrides = NULL,
  group_rules = NULL,
  measurement_taxon = NULL,
  halt = TRUE
)
}
\arguments{
\item{con}{a DBI connection holding the assembled \code{dataset_taxon}}

\item{overrides}{the override registry (\code{metadata/taxon_override.csv}), or NULL}

\item{group_rules}{the group registry (\code{\link[=read_taxon_group_rules]{read_taxon_group_rules()}}), or NULL}

\item{measurement_taxon}{the composite crosswalk (\code{metadata/measurement_taxon.csv}),
whose \code{dataset_key}s count as supplied, or NULL}

\item{halt}{logical; \code{stop()} on an orphan (default \code{TRUE})}
}
\value{
(invisibly) a named list of the orphan \code{dataset_key}s per registry
}
\description{
\code{metadata/taxon_override.csv} and \code{metadata/taxon_group.csv} are read whole
by every ingest while each loads only its own vocabulary, so a row for
another dataset is normal there and cannot be validated. This is the check
for the place where every dataset IS present — the release, after
\code{assemble_core()} — replacing the hard-coded list of dataset names the
package used to validate against (taxon plan D5). The allowed set is the
\code{dataset_key}s present in \code{dataset_taxon} ∪ \code{measurement_taxon}; a row naming
anything else (a typo, a retired dataset) errors, because a registry row that
matches nothing is how a missing id hides.
}
\concept{check}
